# Architecture

## Module boundaries

- `src/main.ts`: bootstrapping, app-shell coordination, dependency wiring, standalone-player hotbar inventory state plus overlay sync and desktop/touch selection handling, standalone-player equipment state plus equipment-panel sync and starter-armor slot toggles, debug-panel item-and-recipe-catalog search-state sync plus hotbar spawn actions, debug-panel crafting-panel sync plus per-station status lines plus selected-recipe actions, session-owned standalone-player death-count telemetry ownership, hotbar stack and single-item drop actions that spawn world pickup entities, hidden-panel play-mode item-use resolution plus shared hotbar placement-range gating, session-owned starter-sword swing-state orchestration plus hostile-slime damage or defeat application and knockback handling plus gel-drop spawning through the shared dropped-item cascade, session-owned starter-spear thrust-state orchestration plus aimed hostile-slime damage or defeat application and directional knockback handling through that same dropped-item cascade, starter pickaxe swing-state orchestration plus dirt/stone/copper-ore-refund spawning for eligible broken terrain tiles, session-owned starter bug-net swing-state orchestration plus passive-bunny capture-to-inventory handling, selected-bunny release-to-entity handling through nearby-ground resolution, selected-acorn planting through shared grass-anchor evaluation, session-owned healing-potion cooldown ownership plus consume handling, heart-crystal consume handling for saved max-health upgrades, and solid-block plus torch/rope/workbench/furnace/anvil placement plus stack consumption, authoritative torch/rope/workbench/furnace/anvil-removal pickup refunds sourced from renderer tile-edit notifications including support-collapse clears, paused-session save export plus import/clear/restore runtime actions, paused-menu latest world-save or shell-setting activity selection, paused-menu shell-profile export plus validate-and-preview import plus preview-clear/apply runtime actions plus shell-hotkey remap persistence or current-session fallback and fan-out refresh into shell/touch UI, paused-menu world-session gameplay-state load/save plus peaceful-mode toggle callbacks that clear active hostile slimes and gate later hostile spawn or locomotion work, paused-menu world-session telemetry-catalog load/save plus collection or type toggle callbacks and debug-overlay/status-strip gating fan-out, renderer entity-pass submission assembly from entity-registry snapshots, fixed-step dropped-item proximity pickup orchestration plus hostile-slime spawn/despawn orchestration and hostile-slime locomotion or player-contact combat wiring with equipment-backed contact-damage reduction plus passive-bunny spawn/despawn orchestration and ambient hop-turn locomotion plus bug-net capture and bunny-release resolution through renderer-backed world queries, render-frame camera-follow alignment to interpolated standalone-player snapshots, and render-frame debug telemetry selection.
- `src/network/`: transport-agnostic networking protocol contracts plus authoritative same-tick chunk-diff batching helpers that group world-tile edits into deterministic y-major chunk-diff message arrays while collapsing repeated tile writes and omitting net no-op tiles, a tile-edit capture helper that records detached previous/next tile-plus-liquid notification state before draining those edits back through batching at tick boundaries, a per-tick replication-staging helper that validates one caller-supplied same-tick entity snapshot before draining tile edits and then returns a deterministic chunk-first/entity-last batch, a transport-facing baseline helper that wraps the replay replacement seam around one world replacement callback plus one entity-snapshot baseline and returns the applied entity replacement summary, a transport-facing baseline-summary helper that reduces that result into baseline tick metadata plus spawned, updated, and removed entity counts for resync logs, a transport-facing per-client resync diagnostics accumulator that folds those baseline summaries into running spawned, updated, and removed totals plus last-applied baseline metadata, client-side interest-set helpers that derive deterministic relevant chunk coords and entity ids from viewport bounds, interest-scoped snapshot-filter helpers that drop out-of-interest chunk diffs and trim entity snapshots before replay, a transport-facing batch-filter helper that maps those staged chunk/entity batches through the same interest filter and returns only forwarded messages plus per-message drop/trim diagnostics for one client tick, a deterministic batch-filter summary helper that reduces those diagnostics into staged tick metadata plus chunk/entity dropped/trimmed/forwarded counters, a transport-facing per-client send diagnostics accumulator that folds those send summaries into running chunk/entity dropped/trimmed/forwarded totals plus last-staged tick metadata, a replication-dispatch helper that applies that interest filter before guarded replay and reports per-message filter/replay diagnostics, a deterministic dispatch-summary helper that reduces those results into chunk/entity dropped/trimmed/applied/skipped counters, a transport-facing per-client diagnostics accumulator that folds those summaries into running totals plus last-processed tick metadata, a transport-facing combined diagnostics snapshot helper that clones those replay/send/resync accumulators into one JSON-safe log-ready object, a transport-facing reset helper that clears that combined snapshot back to zeroed per-section totals plus null metadata for reconnect or client replacement flows, a per-client diagnostics registry helper that stores detached combined snapshots by client id while supporting per-client reset and removal for reconnect or disconnect flows, a registry snapshot helper that exports those stored entries into deterministic ascending client-id order for periodic multi-client logging, an aggregate helper that reduces that ordered snapshot list into multi-client replay/send/resync totals plus client count for periodic log headers, a log-payload helper that combines those shared totals with the same ordered client snapshots into one JSON-safe periodic multi-client summary, aggregate-header and per-client log-line helpers that render those shared totals plus one ordered client snapshot entry into deterministic labeled lines for reusable sinks, a whole-log line helper that assembles the fixed header, aggregate section, client divider, and ordered client sections into one detached line array before final joining, a text-log formatter that renders that payload into deterministic multi-line console output with a fixed aggregate-first then client-by-client section order, a periodic log-emission helper that snapshots the registry, builds that payload, and returns the frozen payload beside detached lines, joined text, and the next due tick for the same emission, a cadence helper that tracks one transport-owned next-due tick, stays silent with null payload/line/text output before that boundary, and reuses the emission helper once fixed-step polling reaches or passes it so due results forward the same detached payload, lines, and text while periodic logs still reschedule from the emitted tick instead of replaying missed intervals, a sink helper that wraps that cadence, invokes one injected callback only on due polls, and clones emitted payload plus `logLines` before handing them to the sink so sink-side mutation cannot leak back into the returned cadence result, a declaration-ordered fan-out sink helper that forwards one due emission to multiple downstream sinks while cloning each sink's payload and line-array view, a structured payload logger adapter that forwards emitted detached `payload` through one injected callback while leaving joined text and lines available for other loggers, a line-array logger adapter that forwards emitted detached `logLines` through one injected callback while leaving joined text available for other loggers, a console-style text-logger adapter that forwards emitted joined text through one injected callback without consuming detached lines needed by other sinks, a mixed logger bundle helper that composes any subset of those text, line, and payload callbacks through one due-only fan-out sink for later runner and factory wiring, a runner helper that wraps one existing cadence plus one existing mixed logger bundle into one ready-to-poll diagnostics object while delegating cloning and due-only sink behavior to the shared log-sink wrapper, a nullable factory helper that accepts cadence settings plus optional text, line, and payload logger callbacks and either returns `null` when diagnostics logging is disabled or builds that same cadence-plus-bundle runner stack, a fixed-step poll helper that turns one nullable diagnostics logger runner into a no-op-or-poll callback over raw `tick` values for update-loop wiring, a reconfiguration helper that rebuilds one nullable diagnostics logger runner plus matching nullable fixed-step poll callback from updated cadence settings and optional logger callbacks for later holder reuse, and a state-holder helper that owns that nullable callback state while exposing `poll(tick)`, disabled-versus-next-due schedule snapshots, separate callback-presence snapshots, detached configuration snapshots that combine the active schedule cadence plus callback-presence state into one JSON-safe export, a configuration-snapshot JSON decoder that validates unknown schedule-plus-callback payloads back into that detached contract for persistence or transport handoff, a configuration-snapshot callback validator that filters supplied live text, line, and payload logger callbacks through detached callback-presence flags before restore or holder reapply, a transport-facing configuration-snapshot restore helper that decodes unknown payloads and reapplies them onto one existing holder through the same supplied registry plus live callback set while disabled snapshots stay disabled, a configuration-change summary helper that compares two detached configuration snapshots into grouped schedule and callback-presence change flags, a configuration-change line formatter that renders those detached diff flags into deterministic `ConfigurationChange`, `ConfigurationScheduleDiff`, and `ConfigurationCallbackDiff` lifecycle line arrays, a configuration-restore line formatter that renders one restored detached configuration snapshot into deterministic `ConfigurationRestore`, `ConfigurationRestoreSchedule`, and `ConfigurationRestoreCallbacks` lifecycle line arrays while inlining the shared schedule and callback diff labels beside restored values, a configuration-lifecycle text formatter that joins detached configuration-change and configuration-restore line arrays into deterministic multi-line console text without reordering either section, a configuration-lifecycle text logger helper that formats those shared line arrays for one injected callback while leaving the detached arrays available for sibling sinks, a restore-lifecycle emission helper that snapshots the previous detached configuration, restores one unknown payload through the shared holder reapply seam, diffs previous-versus-restored snapshots, and returns detached payload data beside change/restore line arrays plus joined text for caller-owned lifecycle logging, a restore-lifecycle payload logger helper that clones and forwards emitted lifecycle payload data through one injected callback while leaving the shared joined text and line arrays available for sibling sinks, a restore-lifecycle line logger helper that clones and forwards emitted lifecycle line arrays through one injected callback while leaving the shared payload data and joined text available for sibling sinks, a restore-lifecycle text logger helper that forwards emitted joined lifecycle text through one injected callback while leaving the shared payload data and line arrays available for sibling sinks, a restore-lifecycle fan-out sink helper that forwards one restore-lifecycle emission to multiple downstream sinks while cloning each sink's payload and lifecycle-line views before later restore-aware bundle wiring, a restore-lifecycle mixed logger bundle helper that composes any subset of those text, line, and payload callbacks through that shared restore-aware sink, a transport-facing restore-and-log helper that reuses the shared restore-lifecycle emission seam and optionally forwards the resulting detached emission through one restore-lifecycle mixed logger bundle, a restore callback factory that captures one holder, registry, live runtime callback set, and optional restore-lifecycle mixed logger bundle into a reusable unknown-snapshot restore function, a nullable restore-callback factory that either returns `null` when no restore-lifecycle text, line, or payload logger is configured or builds that shared restore-lifecycle bundle-plus-callback stack, a restore-callback invoker that turns one nullable restore callback into a no-op-safe unknown-snapshot restore function for transport lifecycle wiring, a restore-callback reconfiguration helper that rebuilds one nullable restore callback plus its matching no-op-safe invoker from updated holder, registry, live runtime callbacks, and optional restore-lifecycle loggers, a restore-callback state-holder helper that owns that nullable callback-plus-invoker pair while exposing `restoreConfigurationSnapshot(...)`, detached `hasRestoreCallback` presence snapshots, a holder-owned logger-refresh helper that reapplies live runtime plus restore-lifecycle loggers onto the stored target holder plus registry while restore wiring stays enabled, and a holder-owned `reconfigureFromPresenceSnapshot(...)` path plus transport-facing presence-snapshot reconfigure helper that let detached `hasRestoreCallback` flags enable, disable, or refresh restore wiring while filtering extra restore-lifecycle loggers through that detached installed-state flag, plus a transport-facing presence-change summary helper that compares previous-versus-next detached `hasRestoreCallback` snapshots into stable change flags for later restore-wiring lifecycle formatting, plus a transport-facing presence-change line formatter that renders those detached diff flags into deterministic `RestoreCallbackPresenceChange` and `RestoreCallbackPresenceDiff` lifecycle lines, plus a transport-facing presence-change text formatter that joins those detached restore-wiring lifecycle lines into deterministic console text, plus a transport-facing presence-change line logger helper that clones and forwards detached restore-wiring lifecycle lines through one injected callback while leaving source arrays available for sibling sinks, plus a transport-facing presence-change text logger helper that formats those shared restore-wiring lifecycle lines for one injected callback while leaving the detached arrays available for sibling sinks, plus create/decode helpers for versioned player-input, chunk-tile-diff, and entity-snapshot messages and replay helpers that map authoritative chunk diffs into liquid-aware tile-state targets while replacing a replicated entity snapshot store from entity snapshots; the authoritative replay wrapper now tracks last-applied ticks per chunk and per entity-snapshot stream so stale or duplicate messages are skipped, and exposes both an explicit replay-guard reset path and a callback-wrapped baseline replacement seam for transport resyncs or out-of-band replicated-state replacement without coupling those flows to a socket implementation yet.
- `src/network/` restore-holder presence wiring now also exposes a mixed logger bundle helper that composes optional restore-wiring text and line loggers through one ordered shared sink, a transport-facing reconfigure-and-log helper that snapshots previous-versus-next detached holder presence around one presence reconfigure, returns detached summary plus restore-wiring line and text output, and optionally forwards cloned restore-wiring lines through that shared bundle, plus a reusable callback factory that captures one holder and optional shared bundle into a detached presence reconfigure-and-log function while leaving live logger and presence inputs at call time, plus a reconfiguration helper that rebuilds one detached presence reconfigure-and-log callback from updated holder wiring plus optional shared bundle state for later holder reuse, plus a callback state-holder helper that keeps one detached `reconfigureAndLogFromPresenceSnapshot(...)` seam stable while `reconfigure(...)` swaps the underlying restore-holder target plus optional restore-wiring logger bundle, `refreshLoggerBundle(...)` reapplies one updated shared bundle against the stored restore-holder target, and `refreshHolder(...)` reapplies that stored bundle against updated restore-holder target state.
- `src/world/playerEquipment.ts`: save-owned standalone-player equipment-slot state, starter armor definitions, slot validation, total-defense aggregation, and flat hostile-contact damage-reduction helpers.
- `src/world/playerItemCatalog.ts`: shared searchable item-catalog helpers derived from the player-inventory item registry for debug panel queries.
- `src/world/playerRecipeCatalog.ts`: shared searchable recipe-catalog helpers derived from the crafting registry plus shared item and station labels for debug panel queries.
- `src/ui/equipmentPanel.ts`: in-world debug-surface equipment summary and slot-toggle UI for the standalone player's `head`, `body`, and `legs` starter-armor slots.
- `src/ui/itemCatalogPanel.ts`: in-world debug-surface searchable item-and-recipe catalog UI that keeps item spawning and recipe reference cards in one panel.
- `src/mainWorldSave.ts`: versioned top-level world-save envelope helpers that validate and normalize `TileWorld` snapshots, including their deterministic per-world seed, together with standalone-player state including saved `maxHealth`, detached standalone-player death-countdown state, standalone-player hotbar inventory state with an empty-hotbar fallback for missing payloads, standalone-player equipment-slot state, dropped-item entity stacks, camera-follow offset, and migration metadata for persistence, export, and restore wiring.
- `src/mainWorldSessionSave.ts`: session-owned save export helper that reads the current world snapshot through a provider such as `Renderer.createWorldSnapshot()` and assembles it with standalone-player, standalone-player death-countdown, standalone-player hotbar inventory, standalone-player equipment-slot state, dropped-item entity stacks, and camera-follow session state into the top-level world-save envelope.
- `src/mainWorldSessionRestore.ts`: session-owned restore helper that re-normalizes decoded top-level world-save envelopes, consolidates overlapping same-item dropped-item stacks through the shared dropped-item cascade before restore callbacks consume them, returns the normalized envelope plus a flag when that consolidation changed the dropped-item payload, loads the nested `TileWorld` snapshot through an existing runtime target such as `Renderer.loadWorldSnapshot()`, and forwards cloned standalone-player, standalone-player death-countdown, standalone-player hotbar inventory, standalone-player equipment-slot state, dropped-item entity stacks, and camera-follow session state into session-owned restore callbacks without rebuilding the renderer or input stack.
- `src/mainWorldSessionShellProfile.ts`: versioned shell-profile envelope helper that snapshots live paused-session shell-toggle visibility together with the active in-world shell hotkey set into one detached JSON-safe export contract and strictly decodes imported shell-profile envelopes back into validated shell state plus hotkeys.
- `src/mainWorldSessionShellProfileDownload.ts`: browser download helper that serializes shell-profile envelopes into pretty-printed JSON, resolves deterministic filenames, and drives Blob URL download cleanup for paused-menu shell-profile export.
- `src/mainWorldSessionShellProfileImport.ts`: browser file-picker helper that selects JSON files, parses them, validates the top-level shell-profile envelope, and reports selected, canceled, or rejected import outcomes back to paused-menu runtime wiring.
- `src/mainWorldSessionGameplayState.ts`: versioned world-session gameplay-state helper that currently persists `peacefulModeEnabled`, applies default-off decoding for missing fields, and reports browser-saved versus session-only availability for paused-menu gameplay controls.
- `src/mainWorldSessionTelemetryState.ts`: versioned world-session telemetry-catalog helper that defines player, hostile-slime, world, and inspect collection or type ids, applies backward-compatible default-on decoding for missing persisted entries, summarizes persistence availability, and exposes collection or type toggle reducers plus readout-visibility checks shared by paused-menu shell controls and the debug HUD or compact status strip.
- `src/mainWorldSaveLocalPersistence.ts`: browser `localStorage` helper that saves, loads, validates, and clears persisted top-level world-save envelopes while dropping malformed stored payloads before runtime boot consumes them.
- `src/mainWorldSaveDownload.ts`: browser download helper that serializes top-level world-save envelopes into pretty-printed JSON, resolves deterministic filenames, and drives Blob URL download cleanup for paused-menu export.
- `src/mainWorldSaveImport.ts`: browser file-picker helper that selects JSON files, parses them, validates the top-level world-save envelope, and reports selected, canceled, or rejected import outcomes back to paused-menu runtime wiring.
- `src/core/`: camera math, camera-follow offset helpers, and fixed timestep loop.
- `src/input/`: input abstraction for keyboard, mouse, touch/pinch, standalone player intent extraction, canvas interaction mode plus queued play-mode player-item-use gestures, explicit camera-pan gesture gating so ordinary play-mode primary clicks stay available for hotbar actions while middle-mouse and debug-pan gestures still move the camera, hotbar selection plus stack-drop shortcut helpers, persisted in-world shell-action keybinding storage plus remap validation, and shortcut resolution helpers including paused-main-menu and in-world shell shortcut availability helpers, a shared runtime shortcut-context composer, and an in-world-only debug-edit keyboard-action guard used by runtime wiring.
- `src/gl/`: low-level WebGL2 utilities, authored-atlas loading plus layout-driven placeholder fallback generation, world rendering orchestration, renderer-owned `TileWorld` snapshot export plus snapshot load seams for session persistence whose restore path now rederives resident lighting from the loaded tile state before the replacement world attaches, an explicit tile-plus-liquid `setTileState()` seam for authoritative replay into that renderer-owned world, renderer-owned tile-edit listener fan-out that survives world replacement, world-query wrappers for standalone-player, hostile-slime, and passive-bunny fixed-step movement, the interpolated entity draw pass, the grounded-idle, grounded-walk, jump-rise, fall, wall-slide, and briefly latched ceiling-bonk standalone player placeholder draw path with nearby world-light modulation, and simple hostile-slime plus passive-bunny placeholder draw paths that reuse the same snapshot interpolation and nearby-light sampling pattern.
- `src/world/`: world data model, chunk math, collision queries, spawn and player-state helpers, narrow collision and spawn world-query view contracts, standalone-player hotbar inventory item and stack helpers, shared hotbar tile placement-range helpers, fixed-step healing-potion cooldown plus alive-and-injured consume helpers, heart-crystal max-health-upgrade helpers, starter sword swing-state plus per-target hit-cooldown, slime-damage, and knockback helpers, starter spear thrust-state plus aimed reach-clamp, one-hit-per-thrust active-window, slime-damage, and directional knockback helpers, starter pickaxe target-evaluation plus swing-state and eligible dirt/stone/copper-ore-refund helpers for terrain together with placed rope/torch/workbench/furnace/anvil utility targeting, starter bug-net swing-state plus nearest-bunny capture resolution helpers, passive-bunny nearby-ground release evaluation helpers, starter solid-block plus torch, rope, and single-tile workbench, furnace, or anvil placement or support-collapse helpers, shared recipe registry plus ingredient, output-capacity, and nearby workbench, furnace, or anvil crafting helpers, shared small-tree tile classification plus planted/grown footprint and anchor-resolution helpers plus planted-versus-grown anchored world-write helpers and grass-only planted-anchor plus deterministic above-anchor growth-site evaluation helpers together with shared acorn planting evaluation plus planted-sapling write helpers, detached standalone-player death-countdown helpers, hostile-slime state plus deterministic round-robin spawn-window helpers plus hop-chase locomotion plus player-contact combat resolution, passive-bunny state plus deterministic round-robin spawn-window helpers plus ambient hop-turn locomotion with grounded ledge avoidance, standalone-player render-snapshot cloning helpers that combine `PlayerState` with pose-driving wall or ceiling contact plus bonk-hold timing, a minimal entity registry that tracks per-entity fixed-step hooks plus previous/current render-state snapshots for future interpolation, a shared helper that blends world position from those snapshots via render alpha, and authoritative standalone-player plus hostile-slime plus passive-bunny entity state ownership including per-slime health, procedural generation through shared seeded terrain samplers that resolve grass, dirt, stone, underground cave air, surface cave mouths outside the protected origin corridor, and copper-ore pockets in untouched upper-underground stone, sparse edited-tile overrides that survive chunk streaming prune, runtime world snapshot export/load that preserves deterministic resident-chunk ordering plus sparse edited overrides and liquid-simulation parity on top of the chunk snapshot codec, explicit tile-state commits that preserve authoritative liquid levels for replay, clear unsupported adjacent starter torches when a solid-face support edit leaves them unattached, clear unsupported placed workbenches, furnaces, or anvils when their ground support breaks, and emit detached previous/next tile-plus-liquid edit notifications even on liquid-only changes, resident per-chunk liquid fill storage plus fixed-step loaded-chunk liquid simulation that scans only currently awake active-liquid chunks for downward transfers before sideways pair equalization across the resulting active-liquid chunk band plus immediate horizontal neighbors, sleeps unchanged liquid chunks after two quiet ticks to preserve alternating sideways-pair parity, wakes nearby resident liquid chunks again on local edits or neighboring transfers, shared normalized liquid-surface top-height resolution now consumed by chunk meshing for exposed partial-liquid tops and top-aligned partial-liquid UV crops, resident per-chunk light storage plus dirty-light local-column invalidation that widens zero-range non-emissive `blocksLight` edits to the edited column's immediate neighbors, further widens edge edits across loaded neighboring `chunkX` boundary columns and those boundary columns' immediate interior neighbors, and still widens nearby non-emissive blocker edits around reachable resident emissive sources, resident lighting recomputation that layers top-down sunlight plus neighboring `chunkX` boundary transport with local emissive falloff on dirty local columns and can report which resident chunk light fields actually changed for renderer invalidation, mesh construction, plus authored atlas-region layout data.
- `src/world/chunkSnapshot.ts`: versioned resident-chunk and edited-chunk snapshot schemas plus JSON-safe row-major dense RLE payload and sparse tile-index/value payload encoders/decoders now consumed by `TileWorld` world snapshots for future save/load work.
- `src/world/tileMetadata.json` + `src/world/tileMetadata.ts`: validated tile metadata registry (terrain autotile variant maps, liquid-render variant maps with liquid-only connectivity groups, gameplay flags like `solid` / `blocksLight` / `liquidKind` plus optional `emissiveLight`, and non-autotile render `atlasIndex` / `uvRect` metadata with optional animated `frames` / `frameDurationMs` sequences compiled into dense lookups and elapsed-frame resolvers backed by `src/world/authoredAtlasLayout.ts`; liquid variant animations compile into parallel per-tile-per-cardinal-mask frame lookups; renderer boot now validates authored atlas-index sources plus direct `uvRect` metadata, including liquid variants, against the loaded atlas dimensions and whole-pixel atlas edges).
- `src/gl/animatedChunkMesh.ts`: renderer-side helper that rewrites baked chunk UVs for animated non-terrain quads and animated liquid-variant quads keyed by their resolved liquid cardinal masks when elapsed time advances to a new metadata frame, while preserving the meshed partial-liquid top-height crop on animated liquid quads.
- `src/ui/`: app shell plus shell-state helper factories and selectors, explicit paused-menu `Overview` / `World Save` / `Shell` / `Recent Activity` / `Danger Zone` section view models, dedicated world-save plus shell plus recent-activity section-state helpers that fold shared summary rows and grouped controls into the paused overlay, the paused-menu shell-hotkey remap editor plus shell-profile preview/clear/apply/export controls plus paused-menu telemetry controls, and in-world shell chrome, hotbar overlay, crafting panel, mining and placement target preview overlays, debug DOM overlays, spawn marker, and touch-only player controls.

## Update loop

`GameLoop` uses:

- fixed update step (`60hz`) for deterministic simulation hooks,
- render interpolation alpha, now consumed by the renderer entity pass to place snapshot-backed entities between fixed updates.

A minimal `EntityRegistry` now steps inside that fixed-update path, rotates `previous/current`
render snapshots once per completed fixed tick, and owns the authoritative standalone-player
entity state. `src/world/entityRenderInterpolation.ts` now provides the shared world-position
blend helper that the renderer entity pass uses when drawing snapshot-backed entities.
`src/main.ts` assembles entity-pass entries directly from registry snapshots, each standalone-player
snapshot now carries pose-driving wall or ceiling contact plus bonk-hold timing beside the cloned
player state, render frames now reapply camera follow from that same interpolated standalone-player
snapshot focus point before pointer inspect and telemetry reads, the renderer resolves placeholder
pose from snapshot `current`, nearby-light telemetry now samples around the interpolated render
position instead of reading the authoritative player state separately, and the same fixed-step
phase now runs hostile-slime fixed-step locomotion inside the registry after the player step, lets
those slime hooks apply one overlap-driven contact hit into the player state before render snapshots
rotate when the player's hostile-contact invulnerability has expired, then runs a deterministic
hostile-slime spawner so slime entities can chase from the updated player
position, despawn outside their keep band, and respawn from round-robin surface windows without
bypassing the registry. That same post-player phase now also runs a deterministic passive-bunny
spawner so ambient bunnies can respawn from nearby surface windows, despawn outside their keep
band, and wander through fixed-step hop-turn locomotion with grounded wall and ledge avoidance
without bypassing the registry. The same registry now also carries dropped-item entities whose
fixed-step hooks auto-merge nearby stacks into the hotbar inventory and whose render snapshots feed
a small world-space pickup placeholder pass.

Bootstrap now mounts the app shell first, initializes renderer plus input behind the `boot` screen, restores the last persisted top-level world-save envelope into the existing renderer plus session state when browser storage contains a valid saved session, immediately rewrites that persisted save when restore-time dropped-item consolidation changes overlapping matching pickups, lets the renderer refresh resident lighting from the restored tile state before the paused preview reads that world, otherwise seeds the first-launch preview world through the shared random-world-seed helper before the shell first exposes `Enter World`, renders one static world preview for the `main menu`, and only starts the `GameLoop` after the shell first enters `in-world` in that tab. After that first start, the shell can return to `main menu` without rebuilding renderer or world state; fixed updates and in-world overlay visibility pause until the same session resumes, the current session-owned top-level save envelope is now written back through browser persistence when a session first starts, pauses to the main menu, resets to a fresh world, or the page hides, a paused-menu `Export World Save` action now downloads that same session-owned save envelope without mutating runtime state, the paused-menu `Shell Hotkeys` editor can now also download a detached versioned shell-profile JSON snapshot of the live paused-session shell toggles plus active shell hotkeys without touching persistence or runtime state, a paused-menu `Import Shell Profile` action now routes a selected JSON file through `src/mainWorldSessionShellProfileImport.ts`, validates the detached shell-profile envelope, and stages a paused-menu preview of the selected profile's shell toggles plus hotkeys before any runtime mutation, a paused-menu `Clear Shell Profile Preview` action can dismiss that staged shell-profile preview without mutating the live paused session, a paused-menu `Apply Shell Profile` action now reapplies that previewed shell layout plus hotkey set onto the live paused session without rebuilding renderer or input even when browser storage can only keep that applied profile for the current tab, a paused-menu `Import World Save` action now routes a selected JSON file through `src/mainWorldSaveImport.ts` and the existing paused-session restore runtime action only after the top-level envelope validates, a paused-menu `Clear Saved World` action now deletes the persisted top-level save envelope while leaving the paused runtime session in memory and keeps paused-menu `pagehide` autosave suppressed until that session resumes or a replacement save path runs, the paused-session restore runtime action can route decoded save envelopes through `src/mainWorldSessionRestore.ts` to swap world plus standalone-player death-countdown, standalone-player equipment, dropped-item entity, and hotbar session state without rebuilding renderer or input, a paused-menu `Reset Shell Toggles` action can clear persisted in-world shell-toggle visibility and restore the default-off shell layout for that paused session before the next resume, the paused menu can now rewrite the six in-world shell-action keybindings through single-letter remap inputs that validate via the shared keybinding helper before saving, a separate persisted world-session telemetry catalog now loads alongside those shell preferences so the paused-menu `Shell` telemetry controls can toggle collection-wide or per-type debug readouts while the live debug HUD and hidden-HUD compact status strip consult the same visibility state, and a separate paused-menu `New World` action replaces the renderer-owned world plus session-owned runtime state with a newly seeded procedural world before re-entering play. App-shell overlay states can now supply a concise headline plus optional structured menu-section cards so multi-action menus like the paused session overlay do not need to overload one status line and a flat detail list with all action consequences. In-world shell toggle visibility (debug HUD, edit panel, edit overlays, spawn marker, shortcuts overlay) now loads from local persistence with an all-off fallback default and writes back to persistence when toggles change, while the six in-world shell-action keybindings load once from validated local persistence before shortcut resolution, successful paused-menu shell-profile preview applies now reapply the live paused-session shell layout plus hotkey set through those same runtime-owned state paths, successful paused-menu remaps now rewrite that same persisted binding set before shell chrome labels, shortcuts overlay text, and the touch keyboard reference all refresh together, and telemetry catalog writes now use the same saved-versus-current-session fallback model before the paused-menu `Shell` state and in-world telemetry formatters refresh. Once active, the in-world shell chrome can independently return to the main menu, recenter the camera on the standalone player, or show or hide the text debug HUD, the full debug-edit control panel, the compact debug-edit overlay layer, and the standalone player spawn marker overlay while the current update phase first drains queued play-mode hotbar item-use requests, handles hotbar stack and single-item drops into world pickup entities, routes hidden-panel selected-item use through starter-sword swing start, starter-spear thrust start, starter-pickaxe swing start, starter-bug-net swing start, nearby-ground `Bunny` release, healing-potion consume, heart-crystal consume, and support-gated dirt-block, stone-block, torch, rope, workbench, furnace, or anvil placement helpers, advances session-owned starter-sword, starter-spear, starter-pickaxe, and starter-bug-net attack state plus healing-potion cooldown after the player step so combat, mining, capture, and consumable pacing stay fixed-step-owned, credits one `Bunny` item plus passive-bunny despawn when a bug-net capture lands with inventory capacity, can also spend one `Bunny` stack to respawn a passive bunny on nearby valid ground around the requested tile, applies hostile-slime contact damage through the current equipment-state defense reduction before player health updates, spawns merge-aware gel pickups when sword or spear defeats hostile slimes, spawns merge-aware dirt-block refund pickups after successful grass or placed-dirt mining edits plus merge-aware stone-block refund pickups after successful stone mining edits plus merge-aware copper-ore refund pickups after successful copper-ore mining edits, spawns torch, rope, workbench, furnace, and anvil refund pickup entities from the renderer-forwarded tile-edit stream whenever a placed utility tile is removed, including torch, workbench, furnace, and anvil support-collapse clears, commits the world edit and selected-stack consumption only after a successful placement, then applies debug tile-edit actions, spawn refresh after tile edits, a resident liquid-simulation step that scans only the current awake active-liquid chunks for downward transfers before sideways spreading across the resulting active-liquid chunk band plus immediate horizontal neighbors, sleeps settled chunks after two quiet ticks, wakes nearby resident liquid chunks on edits or transfers, and returns immediately when no chunk is currently awake, standalone-player entity spawn refresh plus embedded-player respawn recovery from the latest resolved spawn when edits trap the current AABB in solid terrain, standalone-player entity fixed-step movement through shared movement, gravity, overlapped-liquid sampling, buoyancy, lava damage, and collision helpers from mixed-device intent, fixed-step dropped-item proximity pickup attempts after the player step, a session-owned fixed-step death countdown that holds the player at the fatal position until respawn from the latest resolved spawn with restored contact invulnerability, and camera follow that targets the player body center while preserving manual pan or zoom offsets from pointer interaction.

That same renderer-forwarded removed-tile refund path now also covers placed workbench, furnace, and anvil cuts plus support-collapse clears, so pickaxe-driven station removal, support-loss cleanup, and debug tile breaks all reuse the same merge-aware dropped-item cascade without a second refund implementation.

## Player state foundation

- Standalone player simulation state currently lives in `src/world/playerState.ts`.
- `PlayerState.position` uses bottom-center world coordinates so spawn placement, collision AABB derivation, and future controller updates share one anchor convention.
- Shared helpers can initialize player state directly from spawn-search output, clone player state into detached render snapshots, clone standalone-player pose presentation into detached render snapshots, carry save-owned max health plus current health, breath plus drowning or lava damage tick cooldowns plus fall-damage recovery time plus hostile-contact invulnerability time, sample liquid overlap from per-tile fill levels including a head-region water check for breath, advance position from velocity on fixed steps, recover embedded state by respawning from the latest resolved spawn without healing or resetting survival-recovery state, resolve normalized movement intent into grounded walk acceleration or braking plus jump impulse plus optional glide-owned fall-speed clamping, expose a body-center camera focus point, apply gravity plus water buoyancy and drag before movement, apply fixed-step breath drain or air recovery plus lethal drowning damage and periodic lava damage before the collision move, then resolve lethal hard-landing damage plus recovery after collision-grounding without mixing render interpolation into the source-of-truth state. The short respawn countdown itself now lives in `src/world/playerDeathState.ts` as detached session state instead of inflating `PlayerState` with a save-owned timer field.
- `src/main.ts` now owns standalone-player entity orchestration rather than a parallel player-state variable: it seeds the player into `EntityRegistry` from the resolved spawn once, lets that entity's fixed-step hook pull shared desktop or touch movement intent plus selected-hotbar glide enrichment through a narrow renderer world-query wrapper only while the shell is `in-world`, updates standalone-player render-presentation state during that fixed-step path before registry snapshots rotate, replaces the entity-backed state through registry writes for embedded recovery, death-hold respawns, paused-session restore, fresh-world respawns, or boot-time persisted-session restore, folds manual pan or zoom camera deltas into a persistent follow offset, loads and saves in-world shell-toggle visibility through local persistence with all-off fallback defaults, persists the current top-level world-save envelope through local browser storage at session-start, pause, fresh-world, restore, and page-hide boundaries except while a paused-menu `Clear Saved World` action has intentionally removed the current paused session from browser storage, lets the shell either pause back to the main menu without discarding the current session, export that paused session through the shared top-level save-envelope plus browser-download helpers without mutating runtime state, import a paused session from a browser-picked JSON file through the shared top-level save-envelope decoder plus existing runtime restore seam, restore a paused session from a decoded save envelope through the shared session-restore helper while clearing session-only edit history, armed tools, and pinned inspect state without rebuilding renderer or input, clear the persisted paused-session world-save envelope from that paused menu while leaving the current paused runtime session intact until resume or replacement save, clear persisted in-world shell-toggle preferences from that paused menu while restoring the current session to the same default-off shell layout used on first start, apply valid paused-menu shell-hotkey remaps through the shared keybinding validator even when browser storage rejects the latest rewrite so the new hotkeys still fan out across shell and touch UI for the current paused session, abandon that paused session through a fresh-world reset that restores first-start runtime defaults while preserving initialized renderer/input and persisted debug-edit prefs, or recenter the camera by zeroing that offset and snapping back to the player's focus point, and now builds renderer entity-pass entries directly from registry snapshots whose standalone-player payload already includes sided wall contact, ceiling contact, and a short bonk-hold presentation latch for the temporary world-space placeholder draw until broader entity rendering exists while render-frame camera follow reads the same interpolated snapshot focus point as the placeholder draw.

## Render pipeline

Renderer initialization first attempts to fetch and decode the committed authored atlas image served from
`public/atlas/tile-atlas.png` at runtime through a compile-time URL derived from the active Vite base path.
If that asset is unavailable or decoding fails, initialization falls back to a generated placeholder atlas derived
from the authored atlas layout so the existing tile rendering path still boots with atlas-index regions in the same
places. After the atlas is loaded, renderer startup validates direct tile
`render.uvRect` metadata plus atlas-index-backed render, terrain, and liquid-variant sources against the runtime
atlas dimensions, stores warning telemetry, and emits a console warning if any static, animated, terrain, or liquid
variant source falls outside the source image or any direct `uvRect` source lands between whole atlas pixels.

1. Ensure canvas backbuffer matches CSS size x `devicePixelRatio`.
2. Build camera matrix (`world -> clip`) for orthographic projection.
3. Compute visible chunk bounds from camera viewport and tile scale.
4. Ensure draw-range chunks are resident, then recompute dirty resident chunk light fields by propagating sunlight top-down from exposed resident chunk tops only within resident chunk columns that currently contain dirty light chunks, writing sunlight onto the first blocking tile before terminating each lit column, then transport sunlight across loaded neighboring `chunkX` boundary columns on dirty edge columns, then light blocking tiles in dirty columns that are cardinally adjacent to sunlit non-blocking tiles (including one-tile recessed vertical air pockets whose horizontal neighbors are sunlit), then merge local emissive source falloff over that sunlight base while also writing emissive light onto the first blocking tile reached by each falloff path before terminating that path, while limiting those passes to the dirty local sunlight columns collected for each resident `chunkX` column before chunk rendering work consumes those caches.
5. Invalidate cached chunk meshes only for resident chunks whose light arrays actually changed during that relight pass, then queue visible (and nearby prefetch) chunk mesh builds and process a small per-frame build budget.
6. Patch ready animated chunk meshes to the current elapsed metadata frame when needed, including liquid-variant frame swaps keyed by the meshed liquid cardinal mask while preserving any meshed partial-liquid top-height crop, then draw chunk VAOs with a shared atlas shader that multiplies tile color by the baked per-vertex light value.
7. Draw the entity pass. The current standalone-player entry places its placeholder quad from the interpolated entity-snapshot world position for the current render alpha, resolves facing plus grounded-idle, grounded-walk, jump-rise, fall, wall-slide, and ceiling-bonk pose selection from snapshot `current` plus the snapshot-owned wall, ceiling, and bonk-hold presentation fields captured on the fixed-step path, and samples nearby resolved world light around that interpolated render-position AABB before passing the normalized factor into the placeholder shader; hostile-slime entries use the same interpolated snapshot position and nearby-light sampling pattern while their fixed-step snapshots now already carry hop-chase locomotion state, and passive-bunny entries use that same interpolation-plus-light pattern while their fixed-step snapshots carry ambient hop-turn locomotion, but both still route through simple placeholder shaders for this presentation slice.
8. Prune far chunk/world caches outside the retain ring.
9. Update debug overlay with frame timing and renderer telemetry.

## Chunk meshing

- Chunk data uses compact `Uint8Array` tile IDs plus parallel `Uint8Array` liquid fill levels.
- Mesher scans all tiles in a chunk.
- For each non-zero tile:
  - emits two triangles (6 vertices) for one quad,
  - writes per-vertex world position, UV, and resolved light level from the chunk light cache, with liquid quads lowering only their exposed top-left and top-right vertices from shared `0..8` fill-height resolution, cropping bottom-edge `v` coordinates from those same top heights so the authored liquid surface stays aligned, while same-kind-covered liquid tiles stay full-height,
  - records animated non-terrain quad offsets plus animated liquid quad offsets keyed by the resolved liquid cardinal mask and meshed liquid top heights so the renderer can patch only those UVs later without losing the partial-liquid crop.
- UVs are resolved through tile metadata (terrain autotile variant maps, optionally paired with a tile's own static render source override, or non-autotile static render metadata), with atlas indices translated through the authored atlas region layout instead of a synthetic grid cache.
- Optional animated render frames compile beside the static render lookup; chunk meshes still bake the static frame-zero UVs, and the renderer mutates only the recorded animated quad UVs when the elapsed frame changes.
- Liquid variant render metadata now compiles beside the base static render lookup, and chunk meshing resolves liquid UVs from sampled NESW liquid-connectivity masks with an isolated-mask fallback when neighborhood sampling is unavailable.
- Animated liquid variant frames compile into per-tile-per-mask lookups; chunk meshes still bake the static liquid frame-zero UVs, and the renderer mutates only the recorded liquid quad UVs when the elapsed frame changes.
- Static chunks upload once as static vertex data; chunks containing animated non-terrain or liquid quads keep a CPU-side vertex copy so the renderer can reupload UV-only changes on frame boundaries.

This is intentionally simple and easy to evolve (greedy meshing, layered tiles, occlusion rules can be added later).

### Placeholder terrain autotile layout

Terrain autotile placeholder variants currently occupy the first `4x4` block of the authored atlas and are addressed
by a row-major variant index derived from the normalized cardinal adjacency mask (`N/E/S/W` bits mapped to `1/2/4/8`).
Diagonal neighbors are sampled and normalized for corner-gating, but placeholder UV selection collapses to the
16 cardinal combinations for now. The current mapping is defined in `src/world/tileMetadata.json` and validated at
startup by `src/world/tileMetadata.ts`, while the atlas indices themselves resolve through the explicit authored
region list in `src/world/authoredAtlasLayout.ts`. The current authored layout also reserves one spare documented
unused region beneath the torch, widens the committed atlas to `128x64`, adds a second full `16x16` torch animation
region to the right, and keeps transparent exterior padding to the right of the authored-region bounds so ordinary
tile quads do not stretch the torch or rope while the unused-slot regression target remains available.

Non-autotile tiles also resolve UVs through the same metadata registry via explicit render metadata:
- `render.atlasIndex`: authored atlas region index.
- `render.uvRect`: normalized UV rectangle (`u0/v0/u1/v1`) for direct sub-rect mapping.

| Atlas row | Variant indices | Cardinal mask combinations |
| --- | --- | --- |
| 0 | `0 1 2 3` | `----`, `N---`, `-E--`, `NE--` |
| 1 | `4 5 6 7` | `--S-`, `N-S-`, `-ES-`, `NES-` |
| 2 | `8 9 10 11` | `---W`, `N--W`, `-E-W`, `NE-W` |
| 3 | `12 13 14 15` | `--SW`, `N-SW`, `-ESW`, `NESW` |
