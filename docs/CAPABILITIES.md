# Capabilities

This document describes the current project state. Unlike the changelog, it should read as a grouped inventory of what exists now.

## Core Runtime

- App shell now has explicit `boot`, `main menu`, and `in-world` states; renderer and input bootstrap run behind the boot screen, boot reloads the last persisted world, standalone-player, and camera-follow session into the paused main menu when browser storage holds a valid saved envelope, the first-launch main menu exposes an `Enter World` action once startup finishes and now renders a concise `Renderer ready.` headline plus structured `Enter World`, `Controls Preview`, and `Mixed-Device Runtime` guidance cards with visible shortcut or readiness metadata rows instead of flat detail bullets, with the controls-preview card listing desktop movement and jump bindings plus when the touch player pad appears after entering the world, later switches that same primary action to `Resume World (Enter)` after a session has been started and paused, lets desktop `Enter` trigger that paused-menu resume path, exposes button-only paused-menu `Export World Save`, `Import World Save`, and `Clear Saved World` actions so the current top-level save envelope can be downloaded as JSON, a selected JSON save can replace the paused session only after validation plus successful runtime restore, or the browser-saved paused session can be deleted without discarding the live paused world in the current tab, keeps `New World (N)` as a separate paused-menu destructive action with the matching desktop `N` shortcut, and now renders paused sessions through a concise `World session paused.` headline plus structured `Resume World`, `Export World Save`, optional `Export Result`, `Import World Save`, optional `Import Result`, `Clear Saved World`, optional `Clear Saved World Result`, optional `Saved World Status`, `Reset Shell Toggles`, optional `Reset Shell Toggles Result`, `Persistence Summary`, and `New World` guidance cards, with the export-result card surfacing either the last downloaded world-save filename after a successful export or the thrown failure reason when the JSON download fails while the paused session stays unchanged, the import-result card surfacing whether a selected JSON save was accepted, rejected with a validation reason, canceled when the picker closed without a file, failed before any selection result when the browser JSON picker itself throws, failed during runtime restore after validation, or restored only in the current tab while browser-resume persistence still failed, the clear-saved-world-result card surfacing when deleting the browser resume envelope fails and leaves the paused session browser-saved, the saved-world-status card surfacing both explicit `Clear Saved World` unsaved sessions and imported sessions whose restore succeeded in memory but still are not browser-saved until a later browser-save rewrite succeeds, the reset-shell-toggles-result card either confirming that the current paused session now resumes from the default-off shell layout or warning that browser shell storage could not be cleared and the reset is current-session-only until a later shell save succeeds, the persistence card surfacing visible `Status`, `Resumes`, `Saved On`, `Saved Off`, `Cleared by`, and `Binding Set` rows plus current `Main Menu`, `Recenter`, `Debug HUD`, `Edit Panel`, `Edit Overlays`, and `Spawn Marker` hotkey rows, and a paused-menu `Shell Hotkeys` editor directly below now provides single-letter remap fields for those six in-world shell actions with inline duplicate/reserved-key validation plus immediate save feedback so the next resume previews both the actual persisted in-world shell layout and the active remappable shell-action binding set; that editor also exposes `Reset Shell Hotkeys`, which rewrites the default `Q/C/H/G/V/M` set through the same save-and-refresh path used by per-row remaps, refreshes shell-dependent hotkey consumers immediately, and clears stale load-fallback warnings after a successful default rewrite; that same summary now explicitly explains when invalid saved shell-action bindings were rejected during load and the recovered safe set is what resumed instead; it now explicitly falls back to `Session-only fallback` when browser shell storage is unavailable or could not be updated; the action cards keep their visible `Shortcut` and `Consequence` metadata instead of relying on a long paused-menu status line, flat detail bullets, or button tooltips alone; paused-menu shell button tooltips now preserve the existing resume, export, import, clear-saved-world, and destructive reset guidance, and the fixed-step simulation starts only after the first `in-world` transition in that tab.
- Choosing `New World` from the paused main menu abandons the current world session, resets world, player-entity, camera, and undo runtime state plus shell visibility toggles back to first-start defaults, and immediately boots a fresh procedural world without reinitializing the already-loaded renderer or discarding persisted debug-edit preferences.
- The paused main menu also exposes `Reset Shell Toggles`, which clears saved in-world shell visibility preferences and reapplies the default-off HUD, edit-panel, edit-overlay, spawn-marker, and shortcuts visibility layout before the next `Resume World (Enter)` without resetting the current world session, then shows a `Reset Shell Toggles Result` card that either confirms that next-resume default-off layout or warns that browser shell storage could not be cleared and the reset currently applies only to the live paused session in this tab.
- The paused main menu also exposes `Import World Save`, which opens a JSON picker, validates the selected top-level `deep-factory.world-save` envelope, restores valid imports through the existing paused-session runtime restore path without rebuilding renderer or input, leaves the paused session unchanged when the picker is canceled or the envelope is rejected, now waits through the browser picker refocus window before treating the action as canceled so valid file selections do not silently drop on return to the paused menu, and shows a paused-menu `Import Result` card with either a canceled status when the picker closes without a file, a picker-failed status plus reason when the browser JSON picker throws before any file selection result exists, the last accepted filename after valid restores, the last rejected filename plus validation reason after rejected picks, the last selected filename plus thrown restore reason when runtime restore fails after validation, or the last selected filename plus browser-resume persistence warning when the restore succeeds only in the current tab, while a matching `Saved World Status` warning persists until a later browser-save rewrite succeeds for that imported session.
- The paused main menu also exposes `Clear Saved World`, which deletes the persisted local paused-session world-save envelope while keeping the current paused world active in the current tab, keeps paused-menu `pagehide` autosave suppressed until that session resumes or a replacement save path rewrites browser storage, now shows a warning `Saved World Status` card while the paused session has no browser-resume envelope after a successful clear, and instead shows a warning `Clear Saved World Result` card with the surfaced failure reason when deleting browser resume data fails while the paused session remains browser-saved.
- Once in-world, the shell keeps lightweight top-right `Main Menu`, `Recenter Camera`, `Show/Hide Debug HUD`, `Show/Hide Edit Panel`, `Show/Hide Edit Overlays`, `Show/Hide Spawn Marker`, and `Shortcuts (?)` actions; by default those six shell actions use `Q/C/H/G/V/M`, but the paused-menu `Shell Hotkeys` editor can now remap the first six through validated persisted single-letter overrides, and invalid, duplicate, reserved, or unreadable saved values fall back to that default set while the shell chrome, shortcuts overlay, and touch keyboard reference all stay in sync with the active bindings. The `Main Menu` action returns to the main menu without rebuilding the initialized renderer or world session, `Recenter Camera` snaps back to the standalone player's body-center focus and clears any accumulated manual follow offset, the HUD toggle shows or hides the text telemetry overlay, the edit-panel toggle shows or hides the full `Debug Edit` control surface independently from the compact overlays, the edit-overlay toggle hides or restores the compact status strip, hovered or pinned inspect outlines, and one-shot preview overlays, the spawn-marker toggle hides or restores the resolved standalone player spawn AABB, support, and anchor overlay without discarding the active world session, and the shortcuts toggle plus matching desktop `?` shortcut show or hide an in-world desktop and touch controls reference that stays open when pausing to the main menu and resuming the same world session; these five in-world shell toggle states now persist through local storage, and missing, invalid, or unreadable persisted state falls back to all five toggles off.
- WebGL2 renderer with shader utilities, buffer helpers, texture loading, and DPR-aware resize.
- Renderer atlas initialization ships with an authored atlas at `public/atlas/tile-atlas.png` exposed through a compile-time runtime URL derived from the active Vite base path, so local builds fetch `/atlas/tile-atlas.png`, project-site builds fetch `/deep-factory/atlas/tile-atlas.png`, and the renderer still falls back to a generated placeholder atlas whose canvas size and painted regions derive from `src/world/authoredAtlasLayout.ts` if that asset cannot be fetched or decoded.
- Renderer boot validates authored atlas-index regions plus direct tile `render.uvRect` metadata against the loaded atlas dimensions, logs a warning when any atlas-backed source falls outside the source image or any direct static or animated `uvRect` edge lands between atlas pixels, and surfaces the warning count plus first warning in debug telemetry.
- Renderer animates non-terrain tiles and resolved liquid variants from metadata-driven frame sequences at draw time by patching chunk UVs only when an animated tile or liquid variant's elapsed frame changes; partial-liquid variants now keep their top-edge surface texels anchored while those frame swaps recrop only the bottom-edge `v` coordinates from the already-meshed liquid heights, and terrain autotile plus other static tile UV resolution remain unchanged.
- Renderer now runs a separate entity pass after chunk draws; the only current entity entry is the facing-aware standalone-player world-space placeholder with grounded idle, alternating grounded-walk, jump-rise, fall, airborne wall-slide, and airborne ceiling-bonk silhouettes instead of a client-space DOM marker, and that pass places the placeholder quad from the interpolated entity-snapshot world position between fixed ticks, resolves pose selection from snapshot `current` plus snapshot-owned wall contact, ceiling contact, and bonk-hold timing captured during fixed updates, mirrors the wall-slide lean away from the blocking wall side, keeps the ceiling-bonk pose briefly latched after contact clears so fixed-step head hits still read visually, and modulates placeholder color by the brightest nearby resolved world-light sample around the interpolated render-position AABB so temporary player brightness tracks surrounding terrain lighting.
- Orthographic camera with anchored zoom, pointer pan controls, and standalone-player follow that tracks the player body center while preserving manual inspection offsets; preview and in-world render frames now resolve that follow target from the same interpolated standalone-player entity snapshot used for placeholder drawing so the camera and visible player stay locked together between fixed ticks.
- Fixed-step game loop (`60hz`) with separate render interpolation alpha, now consumed by the renderer entity pass to smooth snapshot-backed entity placement between fixed updates.
- The standalone player's authoritative runtime state now lives in the shared entity registry; initial spawn bootstrap, fixed-step movement, lava and embedded respawn recovery, camera follow, recenter, preview rendering, and fresh-world resets all route through that one registry-backed ownership path, and preview plus in-world rendering now submit registry-backed entity-pass entries whose standalone-player snapshots combine cloned player state, snapshot-owned wall or ceiling plus bonk presentation state, interpolated entity-snapshot world position, snapshot-backed pose selection, interpolated-position nearby-light sampling, and render-frame camera follow aligned to that same interpolated snapshot focus point.
- Chunked world model with `32x32` tiles per chunk and `16px` tiles.
- Procedural terrain fill stub backing the current resident world.

## World Streaming And Meshing

- Chunk visibility culling with padded chunk streaming retention.
- Chunk pruning can evict resident world chunks from memory without losing edited tile values; sparse per-chunk tile overrides are reapplied when those chunks stream back in.
- Resident world chunks now also own zeroed per-chunk `Uint8Array` light fields plus dirty-light flags and dirty-local-column masks, and newly streamed chunks start with all local sunlight columns dirty so resident lighting caches can be rebuilt from tile state; draw-range chunks are made resident before the renderer's relight pass so visible stream-back chunks do not build once with zeroed dirty light and then immediately rebuild.
- Tile edits now clear and dirty only the loaded local sunlight columns affected by that edit whenever the edited tile changes `blocksLight` or emissive-light state; emissive source edits widen that invalidation to nearby columns within the edited source's emissive radius, zero-range non-emissive `blocksLight` edits now also invalidate the edited column's immediate neighbors so one-tile-gap blocker-face relighting recomputes adjacent columns, non-emissive `blocksLight` edits still widen invalidation when nearby resident emissive sources can still reach the edit, and non-emissive edge edits additionally invalidate loaded neighboring `chunkX` boundary columns plus their immediately adjacent interior columns so horizontal sunlight transport and boundary-adjacent solid-face relighting recompute both sides of resident chunk edges.
- Dirty resident light fields are recomputed during rendering by propagating sunlight top-down from exposed resident chunk tops only within resident `chunkX` columns that currently contain dirty light chunks, then transporting sunlight across loaded neighboring `chunkX` boundary columns when dirty edge columns are recomputed from sunlight-only boundary visibility rather than retained emissive falloff in clean neighbors, then writing sunlight onto blocking tiles that are cardinally adjacent to sunlit non-blocking tiles in those dirty columns (including recessed one-tile vertical air pockets whose horizontal neighbors are sunlit), then layering local emissive-source cardinal falloff over that sunlight base for those same dirty local columns; both sunlight and emissive recomputation write onto the first blocking tile in each lit path so exposed or adjacent solid faces stay visible while gameplay `blocksLight` metadata still terminates further propagation behind those blockers before rebuilt local columns are marked clean.
- World chunk rendering now modulates tile atlas color by resolved per-tile light values baked into chunk-mesh vertices, and dirty-light chunk recomputation now invalidates only cached chunk meshes whose light arrays actually changed, including neighboring boundary chunks touched by horizontal sunlight transport, so lighting-only changes reupload without broad relight-driven queue churn.
- Resident water and lava now keep per-tile `0..8` fill levels in world state and step in the fixed `60hz` update by transferring downward only from currently awake resident active-liquid chunks first, then sideways through deterministic loaded-chunk pair equalization that only scans the resulting active-liquid chunk band plus immediate horizontal neighbors; settled liquid chunks now stay resident but sleep after two quiet ticks so the alternating sideways-pair parity is preserved before they drop out of the active set, and local edits or nearby liquid transfers wake neighboring resident liquid chunks again. `TileWorld` still rebuilds awake liquid membership from resident chunk contents on snapshot load or stream-back, so completely dry resident worlds and fully sleeping settled pools both skip the liquid step until a disturbance reintroduces awake work. Hovered and pinned inspect telemetry surfaces that `liquidLevel:x/8` state, the same-kind liquid-surface input levels (`north`, `west`, `center`, `east`), the resolved liquid-surface branch classification (`empty`, `north-covered`, or `exposed`), the resolved normalized `liquidTopLeft` and `liquidTopRight` surface heights, the current liquid-variant frame-top and frame-bottom bounds (`liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`), the direct full-frame-height readout (`liquidFrameHeightV`, `liquidFramePixelHeight`), the resolved bottom-edge `liquidBottomLeftV` and `liquidBottomRightV` crop coordinates, the matching `liquidBottomLeftPxY` and `liquidBottomRightPxY` atlas pixel-row endpoints, the resolved visible-frame-height deltas plus per-side percentages (`liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPercentage`, `liquidVisibleRightPercentage`, `liquidVisibleLeftPixelHeight`, `liquidVisibleRightPixelHeight`), the cropped-off remainder deltas plus per-side percentages (`liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPercentage`, `liquidRemainderRightPercentage`, `liquidRemainderLeftPixelHeight`, `liquidRemainderRightPixelHeight`), and paired visible-plus-cropped per-side totals (`liquidCoverageLeftTotalPercentage`, `liquidCoverageRightTotalPercentage`, `liquidCoverageLeftTotalPixelHeight`, `liquidCoverageRightTotalPixelHeight`) for the current liquid variant frame directly, and chunk meshing now lowers exposed liquid top vertices from those fill levels while top-aligning the visible liquid frame crop so partial fills render flat or sloped without vertically squashing their authored surface texels, while same-kind-covered tiles remain full-height.
- A shared liquid-surface helper converts same-kind `north`, `west`, `center`, and `east` fill levels into a branch classification plus normalized `topLeft` and `topRight` heights by clamping raw levels, forcing full-height tops when liquid continues above, and averaging exposed shared corners with same-kind side neighbors so adjacent tiles resolve the same boundary height; the liquid meshing path and inspect telemetry now consume that helper-owned branch rule.
- Budgeted per-frame mesh build queue with visible-first scheduling and nearby prefetch.
- Tile edit events trigger edge and corner neighbor-chunk mesh invalidation.
- Chunk meshing currently emits one quad per non-empty tile with per-vertex world position, UV, and resolved light level; liquid quads can lower their top-left and top-right vertex heights from shared surface-fill resolution and crop their bottom-edge `v` coordinates from those same heights so the authored liquid surface stays top-aligned, while static chunks still upload once and chunks with animated non-terrain or liquid quads keep CPU-side vertex copies so UV-only updates can be reuploaded on frame boundaries.
- Liquid tiles now carry separate liquid-render metadata and chunk meshing resolves their `variantRenderByCardinalMask` entries from sampled NESW liquid connectivity, falling back to the isolated mask when neighborhood sampling is unavailable.
- `buildChunkMesh` pre-counts non-empty tiles and writes directly into an exact-sized `Float32Array`.
- Chunk meshing supports reusable `TileNeighborhood` scratch sampling so terrain meshing avoids per-tile neighborhood allocations.

## Save/Load Foundations

- `src/world/chunkSnapshot.ts` now defines a version `1` chunk snapshot contract for save/load work, and `TileWorld` can now create or load JSON-safe world snapshots on top of it: resident chunks serialize dense row-major `tiles`, `liquidLevels`, and `lightLevels` through run-length/value pairs plus explicit light dirtiness metadata, sparse edited chunk overrides serialize sorted tile-index/value pairs for tile and liquid edits, top-level world snapshots preserve deterministic chunk ordering plus `liquidSimulationTick` parity for post-load liquid behavior, and focused regressions cover round-trips, load validation, streamed-out edited overrides, and post-load liquid continuation.
- `src/mainWorldSave.ts` now defines the version `1` top-level `deep-factory.world-save` envelope for session persistence work: it wraps a validated `TileWorld` snapshot with standalone-player session state, camera follow offset, and paired migration metadata, deep-clones the payload during create or decode, and rejects malformed session or nested world data before export, local persistence, or restore paths consume it.
- `src/mainWorldSessionSave.ts` now assembles a live session-owned export payload by reading the current world snapshot, standalone-player state, and camera follow offset into that top-level save envelope, while `Renderer.createWorldSnapshot()` exposes the renderer-owned `TileWorld` state without handing out the mutable world instance, and the paused main menu now uses `src/mainWorldSaveDownload.ts` to export that envelope as a pretty-printed downloadable JSON file without mutating the active session.
- `src/mainWorldSaveLocalPersistence.ts` now provides the matching browser `localStorage` adapter for that envelope, and `src/main.ts` uses it to restore the last valid saved session at boot, persist the current session when play first starts, when the session pauses, when a fresh world or paused-session restore replaces the runtime state, and when the page hides, while malformed stored payloads are cleared before boot falls back to the first-launch main menu; a paused-menu `Clear Saved World` action can remove that stored envelope while temporarily suppressing paused-menu autosave until the session resumes or another save-replacement path runs.

## Networking Scaffolding

- `src/network/protocol.ts` now defines the version `1` transport-agnostic networking contract for current scaffolding: player-input messages carry fixed-step `tick` plus normalized `moveX`/`jumpPressed` intent, chunk-tile-diff messages carry chunk coordinates plus row-major `tileIndex` edits with tile and liquid state, entity-snapshot messages carry fixed-step `tick` plus deterministic entity-id-ordered position, velocity, and flat scalar state bags, and focused create/decode helpers normalize local payloads while rejecting malformed or out-of-order wire data before later transport work lands.
- `src/network/chunkDiffBatching.ts` now batches authoritative same-tick tile edits into deterministic y-major chunk-diff message arrays, collapses repeated writes for one tile down to the final same-tick result, omits tiles whose final state matches their same-tick starting state, and rejects incoherent previous-state chains before transport work turns those batches into wire sends.
- `src/network/tileEditCapture.ts` now provides a transport-agnostic authoritative tile-edit capture helper that records detached previous and next tile-plus-liquid state from world edit notifications, can discard pending notifications on reset, and drains those captured edits back through the same-tick chunk-diff batching helper at tick boundaries without importing `TileWorld` or renderer internals into networking code.
- `src/network/replicationStaging.ts` now stages one deterministic authoritative replicated-state batch per tick by validating that a caller-supplied entity-snapshot message matches the staged tick before any drain occurs, draining captured tile edits into deterministic chunk-diff messages, cloning that entity snapshot, and appending it after the chunk diffs so later transports can emit one stable chunk-first/entity-last batch.
- `src/network/replicationBaseline.ts` now provides a transport-facing authoritative resync helper that routes one caller-supplied world replacement callback and one entity-snapshot baseline through `AuthoritativeReplicatedNetworkStateReplayer.replaceAuthoritativeBaseline()`, applies the entity baseline without tick-guard rejection, and returns the applied entity replacement summary before the replay guards are cleared so later transports can log resync outcomes without rebuilding that baseline path.
- `src/network/replicationBaselineSummary.ts` now reduces those applied baseline results into concise `baseline.tick` and `baseline.entityCount` metadata plus spawned, updated, and removed entity counts so transport resync logs or future diagnostics do not need to retain the raw replacement id arrays.
- `src/network/replicationResyncDiagnostics.ts` now accumulates those baseline summaries into per-client running spawned, updated, and removed totals while keeping the latest applied baseline tick and entity-count metadata separate from the cumulative counters, so transport resync logging can retain long-lived diagnostics without losing the last accepted baseline boundary.
- `src/network/interestSet.ts` now derives client interest sets from camera-centered viewport size and zoom plus configurable chunk and entity padding, returns deterministic y-major chunk coord lists and ascending entity-id membership for relevant entities, and can diff consecutive interest snapshots into chunk and entity enter/exit arrays for later replication transport.
- `src/network/snapshotFilter.ts` now trims authoritative payloads against the current client interest set before replay: out-of-interest chunk-diff messages are dropped entirely, entity-snapshot payloads keep only currently relevant entity ids, and fully trimmed entity snapshots still replay as empty replacements so local replicated entities clear when client interest moves away from them.
- `src/network/replicationBatchFilter.ts` now provides the transport-facing send-side counterpart to that interest filter: it maps one staged authoritative chunk-first/entity-last batch through the current client interest set, returns only the forwarded messages that should be sent to that client, and records deterministic per-message `dropped`/`kept`/`trimmed` diagnostics without losing the empty entity snapshot replacement that interest moves still need.
- `src/network/replicationBatchFilterSummary.ts` now reduces one client send batch's filter diagnostics into shared staged tick metadata plus chunk and entity `dropped`/`trimmed`/`forwarded` message counts, with `forwarded` intentionally including trimmed entity snapshots because they still travel over the transport.
- `src/network/replicationSendDiagnostics.ts` now accumulates those batch-filter summaries into per-client running chunk and entity `dropped`/`trimmed`/`forwarded` totals while keeping the latest staged tick metadata separate from the cumulative counters, and empty summaries that add no staged batch preserve the last known staged tick instead of erasing it during idle send ticks.
- `src/network/stateReplay.ts` now provides the matching client-side replay path for authoritative state: chunk-tile-diff messages map their row-major tile indices into any local `setTileState()` target, entity-snapshot messages replace a local `ReplicatedEntitySnapshotStore` keyed by authoritative entity id, and an `AuthoritativeReplicatedNetworkStateReplayer` now guards that replay by tracking last-applied ticks per chunk plus per entity-snapshot stream so duplicate or stale authoritative messages are skipped instead of rolling local replicated state backward. Both the store and the replayer now also expose explicit replay-guard reset helpers for transport resync or out-of-band baseline replacement, and the replayer can now wrap one caller-supplied replicated-state baseline replacement callback before clearing both guard sets so resync code does not need to remember a second manual reset step. Those reset paths clear only tick bookkeeping while leaving the current replicated contents intact. `TileWorld` and `Renderer` now expose `setTileState()` so replayed or simulated liquid-only changes preserve explicit liquid levels and still invalidate chunk meshes through the existing tile-edit listener path.
- `src/network/replicationDispatch.ts` now composes interest filtering with guarded authoritative replay for whole message batches: out-of-interest chunk diffs report `dropped` without mutating local state, entity snapshots report when interest trimming reduced the forwarded payload, and every forwarded chunk/entity message also reports whether guarded replay `applied` it or `skipped` it as stale or duplicate so later transport code can keep per-client diagnostics without reimplementing the filter-plus-replay flow.
- `src/network/replicationDispatchSummary.ts` now reduces those authoritative dispatch results into deterministic per-batch chunk and entity `dropped`/`trimmed`/`applied`/`skipped` counters so later transport code can log or accumulate per-client filter and replay outcomes without walking every per-message result again.
- `src/network/replicationDiagnostics.ts` now accumulates those dispatch summaries into per-client running chunk and entity `dropped`/`trimmed`/`applied`/`skipped` totals while keeping the latest processed fixed-step tick metadata separate from the running counters, so transport logging can retain long-lived diagnostics without losing the most recent batch boundary.
- `src/network/replicationDiagnosticsSnapshot.ts` now combines per-client replay, send, and resync diagnostics into one detached JSON-safe snapshot with explicit `replay`, `send`, and `resync` sections, so transport logging or later registry helpers can capture the full per-client replication picture without flattening mismatched counter schemas or mutating the live accumulators.
- `src/network/replicationDiagnosticsReset.ts` now resets that combined replay/send/resync snapshot back to zeroed totals plus cleared metadata in one detached step, so reconnect or client-replacement flows can reuse the shared sectioned shape without manually zeroing each section or mutating the stored snapshot in place.
- `src/network/replicationDiagnosticsRegistry.ts` now stores those combined diagnostics snapshots by client id in a detached per-client registry, can reset one client entry back to a zeroed snapshot for reconnect or client-replacement flows, and can remove one client entry for disconnect flows while `getSnapshot()` and `setSnapshot()` both keep callers from mutating the registry’s internal snapshot state.
- `src/network/replicationDiagnosticsRegistrySnapshot.ts` now exports that registry into detached `{ clientId, snapshot }` entries sorted by deterministic ascending client-id order, so periodic multi-client logging and later aggregate helpers can consume a stable ordered list instead of depending on `Map` insertion order.
- `src/network/replicationDiagnosticsAggregate.ts` now reduces that ordered client list into detached multi-client `clientCount` plus aggregate replay/send/resync totals, so periodic transport log headers can report fleet-wide replication counters without inventing merged per-client last-tick metadata.
- `src/network/replicationDiagnosticsLogPayload.ts` now combines those aggregate totals with a detached ordered `clients` snapshot list into one JSON-safe periodic multi-client summary payload, so later text formatting or emission helpers can consume one stable log object instead of recomputing totals or re-cloning client snapshots.
- `src/network/replicationDiagnosticsLogLineFormatting.ts` now exposes transport-facing aggregate-header and per-client diagnostics line helpers that render shared replay/send/resync totals plus one ordered client snapshot entry into deterministic labeled lines with explicit `n/a` placeholders for missing client metadata, so alternative log sinks can reuse the same header and client-section output without rebuilding their line order or placeholder rules.
- `src/network/replicationDiagnosticsLogLineFormatting.ts` now also exposes a whole-log line helper that assembles the fixed `ReplicationDiagnostics` header, aggregate section, `Clients:` or `Clients: none` divider, and ordered per-client sections into one detached deterministic string array before any newline join, so transport code can consume the shared log order without splitting formatter output back into lines.
- `src/network/replicationDiagnosticsLogFormatter.ts` now renders that payload into deterministic multi-line console text by emitting the aggregate section first, then each client section in payload order, while using explicit `n/a` placeholders when per-client replay, send, or resync metadata has not been observed yet.
- `src/network/replicationDiagnosticsLogEmission.ts` now snapshots that per-client registry once per emission, builds the shared log payload, formats deterministic multi-line output, and returns the frozen `payload` beside detached `logLines`, joined `logText`, and `nextDueTick = tick + intervalTicks`, so transport polling loops and later structured sinks can reuse one detached emission without re-snapshotting, reformatting, or rebuilding payload data.
- `src/network/replicationDiagnosticsLogCadence.ts` now tracks one transport-owned replication diagnostics `nextDueTick`, returns silent `{ emitted: false, payload: null, logLines: null, logText: null }` results while fixed-step polling remains before that boundary, and emits at most one formatted log through `src/network/replicationDiagnosticsLogEmission.ts` once a poll reaches or passes the due tick, forwarding the same detached `payload`, `logLines`, and `logText` while rescheduling from the emitted tick instead of replaying missed intervals.
- `src/network/replicationDiagnosticsLogSink.ts` now wraps that cadence with one injected due-only sink callback, forwarding `nextDueTick`, joined `logText`, and cloned detached `payload` plus `logLines` only when polling emits, so transport adapters can attach side effects without reimplementing cadence checks or letting sink-side payload or line mutation leak back into the returned poll result.
- `src/network/replicationDiagnosticsFanOutSink.ts` now composes multiple due-only sink callbacks into one declaration-ordered sink that clones each emitted payload and line array per downstream sink, so text, line, and future payload logger adapters can share one emission without one sink's mutation leaking into another sink.
- `src/network/replicationDiagnosticsPayloadLogger.ts` now adapts one structured payload logger into that due-only sink contract by forwarding only emitted detached `payload`, so transport code can plug in one JSON-oriented logger without consuming joined text or detached lines that other logger adapters may still need.
- `src/network/replicationDiagnosticsLineLogger.ts` now adapts one line-array logger into that due-only sink contract by forwarding only emitted detached `logLines`, so transport code can plug in one line-oriented logger without splitting joined text while the returned cadence result still keeps `logText` available for other logger adapters.
- `src/network/replicationDiagnosticsTextLogger.ts` now adapts one console-style joined-text logger into that due-only sink contract by forwarding only emitted `logText`, so transport code can plug in one `console.log`-style callback without rejoining `logLines` or taking detached line access away from other sinks.
- `src/network/replicationDiagnosticsLoggerBundle.ts` now composes any configured text, line, and payload logger callbacks into one due-only sink by wiring the existing adapters through the shared fan-out helper in fixed text, line, then payload order; omitted logger types stay out of the bundle, and an explicitly empty bundle still acts as a silent no-op sink when a caller uses it directly.
- `src/network/replicationDiagnosticsLoggerRunner.ts` now composes one existing diagnostics cadence with one existing mixed logger bundle into a ready-to-poll runner that exposes `getNextDueTick()` plus `poll()`, while reusing the underlying sink wrapper so due emissions still clone payload and line data before mixed logger callbacks can mutate them.
- `src/network/replicationDiagnosticsLoggerFactory.ts` now accepts cadence settings plus optional text, line, and payload logger callbacks, returns `null` when logging is disabled, and otherwise builds that same ready-to-poll diagnostics runner through the shared cadence, bundle, and runner helpers, so transport code can toggle periodic replication diagnostics without instantiating an unused silent runner.
- `src/network/replicationDiagnosticsLoggerPoll.ts` now turns one nullable diagnostics logger runner into a fixed-step callback that no-ops while logging is disabled and otherwise delegates raw `tick` values into the shared runner poll path, so update-loop transport wiring can keep one narrow `poll(tick)` shape without special-casing the disabled runner state at every call site.
- `src/network/replicationDiagnosticsLoggerReconfiguration.ts` now rebuilds one nullable diagnostics logger runner plus its matching nullable fixed-step poll callback from updated cadence settings and optional text, line, and payload logger callbacks, so later transport holders can swap diagnostics logging on, off, or between callback sets through the shared factory and poll composition path without duplicating disabled-state branching.
- `src/network/replicationDiagnosticsLoggerStateHolder.ts` now owns the current nullable diagnostics poll callback reconfiguration, exposes a transport-facing `poll(tick)` method that silently no-ops while disabled and otherwise delegates into the active callback, exposes `getScheduleSnapshot()` so later holder helpers can distinguish disabled logging from an enabled holder's current `nextDueTick`, exposes a separate `getCallbackPresenceSnapshot()` with `hasTextLogger`, `hasLineLogger`, and `hasPayloadLogger` flags so later holder helpers can inspect configured logger types without widening the schedule snapshot contract, exposes `getConfigurationSnapshot()` so transport code can export one detached JSON-safe `{ schedule, callbacks }` view with disabled-vs-enabled schedule state, active `intervalTicks`, current `nextDueTick`, and callback-presence flags without reading runner internals, exposes `reconfigureFromConfigurationSnapshot(...)` so transport code can reapply one detached configuration snapshot onto an existing holder using a supplied registry plus live logger callbacks while disabled snapshots stay disabled and enabled snapshots only attach the callback types flagged by the snapshot, exposes `refreshSchedule(...)` so one enabled holder can swap `nextDueTick` without resetting its current `intervalTicks` or current logger callbacks, exposes `refreshCallbacks(...)` so one enabled holder can swap text, line, and payload logger callbacks without resetting its current due tick, exposes `refreshScheduleAndCallbacks(...)` so one enabled holder can swap `nextDueTick` plus text, line, and payload logger callbacks together without resetting its current `intervalTicks`, exposes `refreshScheduleAndCadence(...)` so one enabled holder can swap `nextDueTick` plus `intervalTicks` together without resetting its current logger callbacks, exposes `refreshScheduleAndCadenceAndCallbacks(...)` so one enabled holder can swap `nextDueTick`, `intervalTicks`, plus text, line, and payload logger callbacks together through one holder-owned refresh path, exposes `refreshCadence(...)` so one enabled holder can swap `intervalTicks` without resetting its current due tick or current logger callbacks, exposes `refreshCadenceAndCallbacks(...)` so one enabled holder can swap `intervalTicks` plus text, line, and payload logger callbacks together without resetting its current due tick, and reconfigures that callback through the shared reconfiguration helper so later transport wiring can toggle or swap diagnostics logging without carrying separate runner and callback variables.
- `src/network/replicationDiagnosticsLoggerConfigurationSnapshotDecode.ts` now decodes unknown persisted or transport-supplied diagnostics logger `{ schedule, callbacks }` payloads into the same detached configuration-snapshot contract emitted by the holder, rejecting disabled payloads that retain cadence data or callback flags and enabled payloads that lack a positive cadence, non-negative due tick, or any callback-presence flag before restore code touches live diagnostics state.
- `src/network/replicationDiagnosticsLoggerConfigurationSnapshotCallbackValidation.ts` now validates supplied live text, line, and payload diagnostics logger callbacks against one detached configuration snapshot's callback-presence flags and returns only the callback types that snapshot actually enables, so transport restore code can reject missing required callbacks before holder reapply while harmless extra supplied callbacks stay ignored.
- `src/network/replicationDiagnosticsLoggerConfigurationChangeSummary.ts` now compares two detached diagnostics logger `{ schedule, callbacks }` snapshots and returns grouped change flags for schedule enablement, cadence, due-tick, and text/line/payload callback-presence differences so transport lifecycle code can detect configuration drift without reading holder internals or comparing live callbacks.
- `src/network/replicationDiagnosticsLoggerConfigurationChangeLineFormatting.ts` now renders those detached configuration-diff summaries into deterministic `ConfigurationChange`, `ConfigurationScheduleDiff`, and `ConfigurationCallbackDiff` line arrays, listing only changed schedule fields in `disabled`, `intervalTicks`, `nextDueTick` order and changed callback fields in `hasTextLogger`, `hasLineLogger`, `hasPayloadLogger` order while unchanged groups emit explicit `none` placeholders for later lifecycle logging.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLineFormatting.ts` now renders one restored detached diagnostics logger configuration snapshot plus one detached configuration-diff summary into deterministic `ConfigurationRestore`, `ConfigurationRestoreSchedule`, and `ConfigurationRestoreCallbacks` line arrays, inlining schedule and callback diff labels beside the restored values while disabled cadence fields surface as explicit `n/a` placeholders for later lifecycle text formatting.
- `src/network/replicationDiagnosticsLoggerConfigurationLifecycleTextFormatting.ts` now joins detached configuration-change and configuration-restore lifecycle line arrays into deterministic multi-line console text, keeping the full change section ahead of the restore section and adding no extra separators when either side is empty so later transport lifecycle sinks can reuse one console-ready string without rebuilding those arrays.
- `src/network/replicationDiagnosticsLoggerConfigurationLifecycleTextLogger.ts` now adapts one lifecycle text logger into that shared line-array sink contract by formatting the supplied configuration-change and configuration-restore lines on demand, so transport lifecycle fan-out can forward joined text to one callback without consuming the detached line arrays that sibling loggers may still need.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLifecycleEmission.ts` now snapshots the previous detached diagnostics logger configuration before restore, reapplies one unknown payload through the shared restore helper, diffs the previous and restored snapshots, and returns detached lifecycle payload data, change-and-restore line arrays, plus joined lifecycle text in one caller-owned emission object for later payload, line, or text logger fan-out.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLifecyclePayloadLogger.ts` now adapts one restore-lifecycle payload logger into that emission-sink contract by cloning and forwarding only the emitted lifecycle payload data, so payload logger mutation cannot leak back into the shared line arrays or joined lifecycle text that sibling sinks may still need.
- `src/mainWorldSessionRestore.ts` now provides the matching session-owned restore helper for decoded world-save envelopes, `src/mainWorldSaveImport.ts` now provides the browser JSON picker plus top-level envelope validation helper for paused-menu imports, and `src/main.ts` now exposes a paused-session `restorePausedWorldSessionFromSaveEnvelope()` runtime action that routes decoded envelopes through that helper, swaps the renderer-owned `TileWorld`, restores standalone-player plus camera-follow session state without rebuilding renderer or input wiring, clears session-only debug history, armed tools, and pinned inspect state, rerenders the paused preview, and writes the replacement session back through local persistence.

## Tile Metadata And Terrain Resolution

- Tile definitions live in validated JSON metadata at `src/world/tileMetadata.json`.
- Tile metadata covers render data, terrain autotile data, liquid-render variant maps plus liquid-only connectivity groups, material tags, and gameplay flags such as `solid`, `blocksLight`, `liquidKind`, and optional `emissiveLight`.
- Render metadata can optionally define animated frame sequences through `frames` plus `frameDurationMs`, while the current meshing path continues to use the base static `atlasIndex` or `uvRect` as frame-zero fallback.
- The default tile set now includes an animated `debug_blink` brush tile that exercises the renderer-side frame resolver against the authored atlas.
- The default tile set also includes an animated `debug_panel_blink` brush tile that exercises the same renderer-side frame resolver through direct `render.uvRect` frames backed by committed atlas pixels and now emits local light (`emissiveLight: 12`) as the first default non-liquid emissive debug source.
- The default tile set now also includes placeholder `water` and `lava` brush tiles with dedicated `liquidRender` metadata, separate liquid connectivity groups, and animated liquid frame sequences; water resolves distinct placeholder surface, edge, body, corner, and T-junction sources (including dedicated `NES-`, `N-SW`, and `-ESW` masks) from liquid masks through two-frame direct-`uvRect` animation in dedicated authored regions, and lava now resolves matching surface, edge, body, corner, and T-junction source classes (including dedicated `NES-`, `N-SW`, and `-ESW` masks) plus a dedicated isolated-body `----` source, dedicated `N---`, `-E--`, `--S-`, and `---W` single-side sources, dedicated `-E-W` and `N-S-` straight-through sources, and dedicated `NE-W` and `NESW` cross or fully surrounded sources by animating mask-specific direct-`uvRect` crops over the existing authored lava frames.
- Committed-asset regressions also verify that every default animated atlas-index frame differs from its prior committed PNG frame, so shipped atlas-backed animations do not silently collapse into repeated art.
- Atlas-backed render metadata resolves through explicit authored atlas region definitions in `src/world/authoredAtlasLayout.ts`, while the generated placeholder fallback atlas paints those same authored regions and direct sub-rect metadata can still use normalized `uvRect` values that are runtime-validated against whole atlas-pixel edges.
- Runtime atlas validation and committed-asset regressions also walk liquid variant render sources, so shipped liquid metadata stays inside the authored atlas and whole-pixel direct-UV bounds.
- Committed-asset regressions also verify that every default direct `render.uvRect` source covers at least one non-transparent pixel in the shipped atlas PNG, so blank authored sub-rects do not silently ship through the direct-UV path.
- Committed-asset regressions also verify that every static default direct `render.uvRect` source stays fully inside the explicit authored atlas regions, so shipped direct-UV tiles cannot silently sample uncovered atlas canvas.
- Committed-asset regressions also verify that every default animated direct `render.uvRect` frame stays aligned to whole atlas pixels in the shipped PNG, so committed direct-frame animations remain sampling-safe against the authored asset dimensions.
- Committed-asset regressions also verify that every default animated direct `render.uvRect` frame differs from its prior committed PNG frame, so shipped direct-UV animations do not silently collapse into repeated art.
- Committed-asset regressions also verify that every default animated direct `render.uvRect` frame stays fully inside the explicit authored atlas regions, so animated direct-UV metadata cannot silently sample uncovered atlas canvas.
- Committed-asset regressions also verify that every default direct `render.uvRect` source, including animated frames, stays clear of the fully transparent exterior padding strip beyond authored regions, so shipped direct-UV metadata cannot silently consume that spill-detection band.
- Those animated frame-difference regressions ignore RGB drift under pixels that are fully transparent in both compared committed PNG frames, so invisible padding churn does not count as a shipped animation change.
- The authored atlas layout also records any intentionally unused committed slots, currently reserves one documented spare region, and keeps a fully transparent exterior padding strip outside the authored region bounds; committed-asset regressions verify that whole strip stays blank so real empty atlas space remains available for spill detection.
- Committed-asset regressions also verify that non-transparent shipped atlas pixels stay inside authored regions touched by default metadata or documented as intentionally unused, so uncovered atlas canvas cannot silently accumulate visible spill.
- Committed-asset regressions also verify that every explicitly unused authored atlas region stays fully transparent in the shipped PNG, so documented spare slots cannot silently pick up stray art.
- Committed-asset regressions also verify that every explicitly unused authored atlas region stays unreferenced by shipped `atlasIndex` metadata and default direct `render.uvRect` sources, so reserved blank slots cannot silently become live metadata targets.
- Committed-asset regressions require every authored region index to be either referenced by default tile metadata or documented there as deliberately unused.
- Terrain autotile adjacency supports cross-chunk 8-neighbor sampling plus normalization helpers.
- Terrain autotile connectivity uses metadata-driven connectivity groups first, then shared material tags for seam compatibility.
- Placeholder terrain autotile layout still uses 16 cardinal-mask variants documented in [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md), but those atlas indices now resolve through the authored atlas region layout.

## Collision Foundations

- World collision helpers expose metadata-backed `isSolidAt` tile queries, world-space solid-overlap checks for half-open AABBs, and single-axis AABB sweep results that clamp movement to the first blocking tile.
- Player spawn queries can scan near a chosen origin tile for grounded standing headroom, skip standing AABBs that overlap water or lava fill, and return the resolved standing AABB plus supporting solid tile.
- Standalone player-state helpers track feet-centered `position`, `velocity`, explicit `size`, `grounded`, `facing`, current `health`, and the remaining lava-damage tick cooldown, can seed that state from spawn output, derive the collision AABB plus one-step fixed-update integration from the same model, expose a body-center camera focus point, probe adjacent support, wall, and ceiling contacts from the current AABB while tagging wall contacts with a resolved `left` or `right` side, sample overlapped partial-liquid fill area from world liquid levels, resolve normalized movement intent into grounded walk acceleration or braking plus a grounded jump impulse, apply gravity plus fall-speed clamping together with water buoyancy and swim drag, advance through x-then-y collision sweeps that zero blocked velocity and recompute grounded support after movement, and apply periodic lava contact damage inside that same shared fixed-step step.
- The fixed update loop now keeps a spawned standalone player state alive through shared keyboard and touch movement intent, so desktop keys and touch buttons can move and jump the player while water slows and floats the player, repeated lava contact drains health on deterministic damage ticks, lethal lava contact respawns from the latest resolved spawn through the same reset path used for other standalone-player discontinuities, debug edits still refresh spawn placement, edits that trap the player inside solid tiles still respawn it from the latest resolved valid spawn, and the renderer receives the current wall and ceiling contact state plus a short ceiling-bonk presentation latch so airborne wall collisions can swap the placeholder into a wall-slide pose while upward collisions can swap it into a ceiling-bonk pose that remains readable after the immediate contact frame.

## Entity Foundation

- `src/world/entityRegistry.ts` now provides a simulation-owned entity registry that assigns stable numeric IDs, runs per-entity fixed-step hooks in spawn order, keeps `previous` and `current` render-state snapshots ready for interpolation, and lets external state replacement resynchronize those snapshots immediately so spawn, teleport, and respawn-style discontinuities can avoid interpolation smear; the standalone player is the first live gameplay actor on that layer, and the renderer now consumes registry-backed entity-pass entries built from those snapshots.

## Hot-Path Lookup Strategy

- Gameplay metadata compiles into dense lookup arrays for collision, light-blocking, emissive-light, and liquid queries.
- Terrain connectivity compiles into dense connectivity-group and material-tag lookup tables.
- Tile render metadata compiles into dense static UV and terrain variant atlas-index lookup tables.
- Liquid connectivity compiles into dense per-tile group ID and label lookups, and liquid variant metadata compiles into per-tile cardinal-mask render lookups consumed directly by the liquid meshing path.
- Optional animated render metadata compiles into dense per-tile frame start, frame count, and frame duration tables plus a flattened UV-frame list for renderer-time elapsed-frame sampling.
- Optional animated liquid variant metadata also compiles into dense per-tile-per-cardinal-mask frame start, frame count, and frame duration tables plus a flattened UV-frame list for renderer-time liquid elapsed-frame sampling.
- Placeholder terrain autotile resolution uses precomputed lookup tables for normalized adjacency masks and raw adjacency masks.
- Authored-atlas region UV rect objects are precomputed and reused by atlas-index and terrain-autotile resolution paths.

## Picking, Debugging, And Inspection

- Shared DPR-aware screen, canvas, world, and tile picking utilities centralize pointer coordinate conversion.
- Camera and picking tests cover viewport round-tripping, DPR-aware selection, and negative-world tile flooring.
- Debug overlay shows FPS, rendered chunk count, mesh telemetry, resident dirty-light chunk counts, loaded atlas source kind plus atlas dimensions, atlas validation status, the resolved standalone player spawn tile plus world coordinates, supporting solid tile coordinates, support chunk coordinates, support chunk-local tile coordinates, support tile ID, and liquid-safety status, live standalone player state telemetry (`position`, body world tile coordinates, body chunk coordinates, body chunk-local tile coordinates, `velocity`, `grounded`, `facing`), the current standalone player placeholder pose label chosen from the renderer-facing pose resolver and now aligned to the snapshot-owned wall, ceiling, and bonk presentation fields, the renderer-side ceiling-bonk presentation latch state while that brief post-contact hold remains active, live standalone-player nearby-light sample telemetry (`level`, `factor`, source world tile coordinates, source chunk coordinates, and source chunk-local tile coordinates consumed directly from renderer telemetry fields), the latest standalone player grounded-transition event (`jump`, `fall`, or `landing` with resolved position and velocity), the latest standalone player facing-transition event (`left->right` or `right->left` with resolved position and velocity), the latest standalone player auto-respawn event when embedded-tile recovery or lava death reuses the resolved spawn (`embedded` or `lava` with spawn tile, resolved support chunk and chunk-local coordinates, resolved support tile ID, resolved spawn liquid-safety status, position, and velocity), the latest standalone player wall-contact and ceiling-contact transition events (`blocked` or `cleared` with tile, position, and velocity, plus resolved `left` or `right` side for wall-contact events), live standalone player collision AABB telemetry (`min`, `max`, `size`), live standalone player camera-follow telemetry (`cam`, `camTile`, `camChunk`, `camLocal`, `zoom`, `focus`, `focusTile`, `focusChunk`, `focusLocal`, `offset`), live standalone player collision-contact telemetry (`support`, `wall`, `ceiling` with tile coordinates and IDs when present, and resolved `left` or `right` side for wall contacts), live standalone player input-intent telemetry (`move`, `jumpHeld`, `jumpPressed`), live pointer inspect data (`client`, `canvas`, `world`, hovered tile name plus tile ID, `tile`, `chunk`, `chunk-local`, `solid`, `light`, `liquid`, `liquidLevel`, `liquidSurfaceIn`, `liquidSurfaceBranch`, `liquidTopLeft`, `liquidTopRight`, `liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`, `liquidFrameHeightV`, `liquidFramePxH`, `liquidBottomLeftV`, `liquidBottomRightV`, `liquidBottomLeftPxY`, `liquidBottomRightPxY`, `liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPct`, `liquidVisibleRightPct`, `liquidVisibleLeftPxH`, `liquidVisibleRightPxH`, `liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPct`, `liquidRemainderRightPct`, `liquidRemainderLeftPxH`, `liquidRemainderRightPxH`, `liquidCoverageLeftPct`, `liquidCoverageRightPct`, `liquidCoverageLeftPxH`, `liquidCoverageRightPxH`), plus resolved liquid connectivity-group labels, liquid cardinal masks, liquid animation frame indices, frame counts, frame durations, elapsed-in-frame times, normalized frame-progress percentages, remaining-in-frame times, loop durations, elapsed-in-loop times, normalized loop-progress percentages, remaining-in-loop times, and current liquid variant source labels, UV rects, and atlas pixel bounds for liquid tiles, and a separate pinned tile metadata line when inspect pin is active.
- Debug overlay also reports resident animated chunk mesh counts plus recorded animated-quad totals with separate `nonLiquid` and `liquid` quad breakdowns, alongside per-frame animated chunk UV upload cost as upload count, changed animated-quad totals, matching `nonLiquid` and `liquid` upload breakdowns, total uploaded bytes, the current awake active-liquid chunk count, the current sleeping-liquid chunk count, the current awake active-liquid chunk bounds, the derived last-step liquid phase summary (`none`, `downward`, `sideways`, `both`), and the last fixed-step liquid simulation counters for downward-scanned active chunks, sideways-scanned active-band candidate chunks, sideways-tested active-band pairs, and split downward-versus-sideways transfer counts, so both steady-state authored animation footprint and current liquid-step workload stay visible while settled sleeping pools no longer read the same as completely dry resident worlds.
- Hovered tile cursor overlay renders in client space from world tile coordinates and keeps pinned and hovered inspect outlines visible together when they differ.
- A dedicated spawn marker overlay outlines the resolved spawn AABB, support tile, and feet anchor in client space and refreshes after tile edits so spawn placement can be validated during active world sessions.
- The shell-level `Spawn Marker` toggle hides that spawn overlay without interrupting world controls, spawn refresh, or simulation.
- On-canvas one-shot preview badges show live anchor plus endpoint tile coordinates, inclusive span dimensions, and affected-tile estimates for active previews and stay clipped to the visible canvas bounds on small or offset canvases, while anchored touch previews keep the endpoint, span, and affected-count text pending until the second point is chosen and show a persistent anchor label with the armed tool name, `Brush` or `Break` action text, and anchor tile coordinates that stays clamped inside the visible canvas bounds near viewport edges.
- Compact debug-edit status strip mirrors shared mode, active brush, armed tool state, mixed-device hints, lets summary chips wrap long brush or tool values inside the chip on narrow mobile viewports, shows active one-shot preview anchor plus endpoint tile coordinates with live inclusive span dimensions and estimated affected tile counts while preview state is live, surfaces the current standalone player placeholder pose label plus the renderer-side `BonkHold` latch state and keeps both aligned to the snapshot-owned wall, ceiling, and bonk presentation fields, live world position, live world tile coordinates, live body chunk coordinates, live body chunk-local tile coordinates, the latest resolved spawn tile plus world coordinates, supporting solid tile coordinates, support chunk coordinates, support chunk-local tile coordinates, support tile ID, and liquid-safety status, live collision AABB `min`, `max`, and `size`, live camera world position, live camera world tile coordinates, live camera world chunk coordinates and chunk-local tile coordinates, live camera-follow focus point world position, world tile coordinates, world chunk coordinates, and chunk-local tile coordinates, live camera-follow offset, live camera zoom level, resident dirty-light chunk counts, awake and sleeping liquid chunk counts plus awake-liquid bounds, standalone-player nearby-light sample telemetry (`level`, `factor`, source world tile coordinates, source chunk coordinates, and source chunk-local tile coordinates consumed directly from renderer telemetry fields), live `grounded` flag, live facing direction, live horizontal move input axis, live horizontal and vertical velocity, live speed magnitude, live `jumpHeld` input state, live `jumpPressed` edge state, live support-contact tile, live wall-contact tile and resolved `left` or `right` side, and the live ceiling-contact tile when the text debug HUD is hidden, also surfaces the latest grounded-transition, facing-transition, auto-respawn, wall-contact, and ceiling-contact transition events in that hidden-HUD state, including respawn-event support chunk and chunk-local coordinates plus the resolved support tile ID and spawn liquid-safety status beside the existing spawn tile, position, and velocity fields, includes the resolved `left` or `right` side on hidden-HUD wall-contact transition telemetry, wraps preview, inspect, empty-hover guidance, shortcut hint details, and multiline player or event telemetry into segmented rows to stay inside narrow mobile viewports, stacks inspect action buttons vertically before they clip on narrow canvases, shows separate pinned plus hovered tile metadata lines plus a hovered-to-pinned offset when both inspect targets differ, labels deduplicated same-tile inspect state as shared, and includes world plus chunk and chunk-local tile coordinates in inspect metadata plus the simulated liquid fill level, same-kind liquid-surface input levels, the resolved `liquidSurfaceBranch` classification, resolved normalized `liquidTopLeft` and `liquidTopRight` surface heights, current frame bounds telemetry (`liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`, `liquidFrameHeightV`, `liquidFramePxH`), resolved `liquidBottomLeftV` and `liquidBottomRightV` crop coordinates, matching `liquidBottomLeftPxY` and `liquidBottomRightPxY` atlas pixel rows, resolved visible-frame-height deltas plus percentages (`liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPct`, `liquidVisibleRightPct`, `liquidVisibleLeftPxH`, `liquidVisibleRightPxH`), cropped-off remainder deltas plus percentages (`liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPct`, `liquidRemainderRightPct`, `liquidRemainderLeftPxH`, `liquidRemainderRightPxH`), paired visible-plus-cropped percentage and pixel accounting (`liquidCoverageLeftPct`, `liquidCoverageRightPct`, `liquidCoverageLeftPxH`, `liquidCoverageRightPxH`), resolved liquid connectivity-group label, liquid cardinal mask, liquid animation frame index, frame count, frame duration, elapsed-in-frame time, normalized frame-progress percentage, remaining-in-frame time, loop duration, elapsed-in-loop time, normalized loop-progress percentage, remaining-in-loop time, and current liquid variant source, UV rect, and atlas pixel bounds for liquid tiles.
- The shell-level `Edit Overlays` toggle and matching desktop `V` shortcut hide those compact inspect and preview overlays without disabling inspect pinning, one-shot tool state, painting, movement, or simulation.
- The shell-level `Show Edit Panel (G)` or `Hide Edit Panel (G)` toggle hides or restores the full `Debug Edit` control surface independently from the compact overlays, and its shell-button tooltip now repeats the matching desktop `G` shortcut alongside the existing show or hide copy; first-run fallback state starts the panel hidden until it is toggled on.
- Compact status-strip inspect action buttons retain the last mouse hover comparison state while the pointer is over the action row, so pinned plus hovered metadata does not flap when using `Pin Click` or `Repin Click`.
- Hovered and touched inspect readouts include tile name, tile ID, world plus chunk and chunk-local coordinates, solid, light-blocking, and liquid flags, plus the simulated liquid fill level, same-kind liquid-surface input levels, the resolved `liquidSurfaceBranch` classification, resolved normalized `liquidTopLeft` and `liquidTopRight` surface heights, current frame bounds telemetry (`liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`, `liquidFrameHeightV`, `liquidFramePxH`), resolved `liquidBottomLeftV` and `liquidBottomRightV` crop coordinates, matching `liquidBottomLeftPxY` and `liquidBottomRightPxY` atlas pixel rows, resolved visible-frame-height deltas plus percentages (`liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPct`, `liquidVisibleRightPct`, `liquidVisibleLeftPxH`, `liquidVisibleRightPxH`), cropped-off remainder deltas plus percentages (`liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPct`, `liquidRemainderRightPct`, `liquidRemainderLeftPxH`, `liquidRemainderRightPxH`), paired visible-plus-cropped percentage and pixel accounting (`liquidCoverageLeftPct`, `liquidCoverageRightPct`, `liquidCoverageLeftPxH`, `liquidCoverageRightPxH`), resolved liquid connectivity-group label, liquid cardinal mask, liquid animation frame index, frame count, frame duration, elapsed-in-frame time, normalized frame-progress percentage, remaining-in-frame time, loop duration, elapsed-in-loop time, normalized loop-progress percentage, remaining-in-loop time, and current liquid variant source, UV rect, and atlas pixel bounds for liquid tiles.
- Desktop and touch both support pinned tile inspection through the compact status strip workflow.

## Shared Debug Edit Model

- Desktop and touch share the same debug edit concepts: `Pan`, `Place`, and `Break` modes, active brush selection, undo and redo history, armed one-shot tools, and compact status reporting.
- Shared debug edit control state persists across reloads through local storage with a metadata-safe brush fallback.
- `Reset Prefs` restores default touch mode, brush, and panel visibility while clearing persisted debug-edit control state.
- Undo and redo operate on recorded per-stroke tile deltas.
- Flood fill, line, rectangle fill, rectangle outline, ellipse fill, and ellipse outline tools apply as single undoable strokes.
- Armed one-shot tools expose on-canvas status or preview overlays and support `Esc` cancellation.

## Desktop Controls

- App shell: click `Enter World` from the main menu after boot finishes, then use `Resume World (Enter)` after returning to the menu from an active session or after boot restores a saved paused session from browser storage, click `Export World Save` there to download the current paused session as JSON without changing world or player state, inspect the paused-menu `Export Result` card after export attempts to confirm either the last downloaded filename or the surfaced failure reason when the JSON download throws, click `Import World Save` to choose a JSON world-save file that replaces the paused session only when the top-level envelope validates and runtime restore succeeds, inspect the paused-menu `Import Result` card after import attempts to see whether the picker was canceled, the picker failed before any selection result and surfaced its thrown reason, the last accepted filename, the last rejected filename plus validation reason, the last restore-failed filename plus thrown reason, or the last selected filename plus browser-resume persistence warning when the imported session is live only in the current tab, and inspect the warning `Saved World Status` card until a later pause, page hide, replacement import, or fresh world rewrite succeeds in browser storage, click `Clear Saved World` to delete the browser-saved paused session while keeping the current paused world open in the tab until it is saved again, inspect the warning `Saved World Status` card while that paused session no longer has browser resume data until `Resume World`, `Import World Save`, or `New World` rewrites it after a successful clear, or inspect the warning `Clear Saved World Result` card when deleting browser resume data fails and the paused session stays browser-saved, click `Reset Shell Toggles` to clear saved in-world shell visibility and restore the default-off shell layout before the next resume, then inspect the `Reset Shell Toggles Result` card to confirm the next resume starts with all five shell toggles hidden or, when browser shell storage could not be cleared, to see that the reset currently applies only to the live paused session in this tab until a later shell save succeeds, edit the paused-menu `Shell Hotkeys` fields to remap `Main Menu`, `Recenter Camera`, `Debug HUD`, `Edit Panel`, `Edit Overlays`, and `Spawn Marker` through unique A-Z letters with inline duplicate/reserved-key feedback, click `Reset Shell Hotkeys` there to restore the default `Q/C/H/G/V/M` set through the same persistence path and clear any stale load-fallback warning, or use `New World (N)` to discard the current session and boot a fresh world immediately; the paused menu now groups those actions into dedicated guidance cards plus a `Persistence Summary` card whose `Status`, `Resumes`, `Saved On`, `Saved Off`, and `Cleared by` rows show whether shell visibility is browser-saved or session-only fallback, which shell toggles return on resume, which ones are currently persisted on or off, and which reset paths clear them, while additional `Main Menu`, `Recenter`, `Debug HUD`, `Edit Panel`, `Edit Overlays`, and `Spawn Marker` rows preview the current active in-world shell-action hotkeys beside the live remap editor, so session-continuation, export, import, clear-saved-world, shell-reset, hotkey-remap, fresh-world effects, and current shell bindings stay visible without repeating one long status line or depending on button tooltips; while those same `Resume World`, `Export World Save`, `Import World Save`, `Clear Saved World`, `Reset Shell Toggles`, and `New World` tooltips still provide the current action guidance, press `Enter` to trigger the same resume path from the keyboard or `N` to trigger the fresh-world reset path.
- In-world shell: by default, click `Main Menu (Q)` or press `Q` to pause back to the main menu without discarding the current world session, click `Recenter Camera (C)` or press `C` to snap the camera back to the standalone player and clear any preserved manual follow offset, click `Show Debug HUD (H)` or `Hide Debug HUD (H)` or press `H` to toggle the text telemetry overlay, click `Show Edit Panel (G)` or `Hide Edit Panel (G)` or press `G` to toggle the full `Debug Edit` control surface, click `Show Edit Overlays (V)` or `Hide Edit Overlays (V)` or press `V` to toggle the compact debug-edit overlays, click `Show Spawn Marker (M)` or `Hide Spawn Marker (M)` or press `M` to toggle the resolved spawn overlay, and click `Shortcuts (?)` or press `?` to toggle the in-world desktop and touch controls reference; the paused-menu `Shell Hotkeys` editor can replace the first six default in-world letters with validated persisted remaps while invalid or conflicting saved values fall back to `Q/C/H/G/V/M`.
- Move player: `A` and `D` or left and right arrows.
- Jump: `W`, up arrow, or `Space`.
- Mouse wheel: zoom in or out.
- `Shift` + mouse drag: pan without painting.
- Mouse drag: debug paint on hovered tiles (`left` place active brush, `right` break).
- Camera shortcut: `C` recenters on the standalone player without affecting movement input.
- Edit-panel shortcut: `G` toggles the in-world full `Debug Edit` panel without affecting movement input.
- Compact edit-overlay shortcut: `V` toggles the in-world compact debug-edit overlays without affecting movement input.
- Debug edit brush shortcuts: `1`-`0` select visible brush slots, `[` and `]` cycle active brush.
- Debug touch-mode shortcuts: `P` pan, `L` place, `B` break.
- Debug panel shortcut: `\` collapses or expands the shared debug-edit controls panel.
- Debug eyedropper shortcut: `I` sets the active brush from the hovered non-empty tile.
- Debug history shortcuts: `Ctrl/Cmd+Z` undo, `Ctrl/Cmd+Shift+Z` or `Ctrl/Cmd+Y` redo.
- Debug flood fill shortcuts: `F` arms brush flood fill, `Shift+F` arms break flood fill.
- Debug one-shot shape shortcuts: `N` and `Shift+N` line, `R` and `Shift+R` rectangle fill, `T` and `Shift+T` rectangle outline, `E` and `Shift+E` ellipse fill, `O` and `Shift+O` ellipse outline.
- Desktop inspect pinning uses compact status strip actions: `Pin Click`, `Repin Click`, `Clear Pin`, and `Esc` to cancel an armed pin.
- Desktop one-shot tools use mouse drag bounds or endpoints for placement and show matching preview overlays.

## Touch Controls

- Shared on-screen debug controls expose `Pan`, `Place`, and `Break` modes plus a shared brush palette.
- A separate on-screen touch player pad exposes hold-to-move left, hold-to-move right, and hold-to-jump buttons without disabling the debug edit panel.
- One-finger drag pans in `Pan` mode.
- One-finger drag paints in `Place` or `Break` mode with line-stepped per-tile dedupe.
- Two-finger pinch zoom works while touch debug edit modes are active.
- Two-finger tap triggers undo in `Pan` mode.
- Three-finger tap triggers redo in `Pan` mode.
- Long-press eyedropper in `Pan` mode sets the active brush from the touched non-empty tile.
- Tap-to-pin inspect in `Pan` mode locks tile metadata and outline state until cleared or repinned.
- Touch flood fill arms through on-screen controls, then applies on the next tap.
- Touch line, rectangle, and ellipse tools use two-point or two-corner workflows and show persistent start-anchor or corner-anchor indicators while armed.

## Debug Edit UI

- On-screen debug edit controls support brush selection, mode switching, undo, redo, collapse and expand, reset prefs, and one-shot tool arming.
- The full `Debug Edit` panel is now distinct from the compact inspect and preview overlay layer, so either surface can be hidden without forcing the other one off.
- The touch `Debug Edit` panel includes a `Keyboard` reference for desktop-parity shortcuts, including paused-menu `Enter` resume and `N` new-world guidance plus the current in-world shell-action bindings for main menu, recenter, HUD, edit panel, edit overlays, spawn marker, and `?` shortcuts alongside brush, history, eyedropper, and shape-tool shortcuts.
- Collapsed panel state still shows a compact header with current mode, active brush, and undo or redo counts.
- The compact status strip stays useful even when the larger panel is collapsed or out of the way.

## Tooling And Deployment

- Production Vite builds target the GitHub Pages project-site base path `/deep-factory/`, so emitted script, stylesheet, and static-asset URLs resolve correctly under `https://regillmore.github.io/deep-factory/` while local development stays rooted at `/`.
- Deployment regression coverage temp-builds the production app, verifies the `/deep-factory/` bundle URLs, scans emitted `index.html` to reject both root-relative `"/assets/..."` and double-prefixed `"/deep-factory/deep-factory/assets/..."` script and stylesheet URLs plus the exact root-relative `"/atlas/"` and `'/atlas/'` static-asset prefix literals, both exact double-prefixed `"/deep-factory/deep-factory/atlas/"` and `'/deep-factory/deep-factory/atlas/'` static-asset prefix literals, the exact root-relative single-quoted `'/atlas/tile-atlas.png'`, exact root-relative double-quoted `"/atlas/tile-atlas.png"`, and exact double-prefixed `"/deep-factory/deep-factory/atlas/tile-atlas.png"` authored-atlas runtime URL literals, scans every emitted CSS asset to reject the exact root-relative `"/assets/"` bundle literal, the exact double-prefixed `"/deep-factory/deep-factory/assets/"` bundle literal, those same exact root-relative `"/atlas/"` and `'/atlas/'` static-asset prefix literals, those same exact double-prefixed `"/deep-factory/deep-factory/atlas/"` and `'/deep-factory/deep-factory/atlas/'` static-asset prefix literals, and those same root-relative and double-prefixed authored-atlas runtime URL literals, scans every emitted JavaScript asset to reject the exact root-relative `"/assets/"` bundle literal, the exact double-prefixed `"/deep-factory/deep-factory/assets/"` bundle literal, both exact root-relative `"/atlas/"` and `'/atlas/'` static-asset prefix literals, both exact double-prefixed `"/deep-factory/deep-factory/atlas/"` and `'/deep-factory/deep-factory/atlas/'` static-asset prefix literals, any exact quoted root-relative `/atlas/tile-atlas.png` literal, and the exact double-prefixed `"/deep-factory/deep-factory/atlas/tile-atlas.png"` authored-atlas runtime URL literal while the exact joined runtime atlas fetch URL `/deep-factory/atlas/tile-atlas.png` still appears exactly once across the full emitted JS set, and byte-compares the copied authored atlas output at `dist/atlas/tile-atlas.png` against `public/atlas/tile-atlas.png`.
