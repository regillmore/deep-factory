# Capabilities

This document describes the current project state. Unlike the changelog, it should read as a grouped inventory of what exists now.

## Core Runtime

- App shell now has explicit `boot`, `main menu`, and `in-world` states; renderer and input bootstrap run behind the boot screen, boot reloads the last persisted world, standalone-player, and camera-follow session into the paused main menu when browser storage holds a valid saved envelope, the first-launch main menu exposes an `Enter World` action once startup finishes and renders concise `Enter World`, `Controls Preview`, `Mixed-Device Runtime`, and `Persistence Preview` guidance cards, and paused sessions now render through a responsive dashboard whose `Overview` page keeps `Resume World` plus navigation tiles for `World Save` and `Shell` while compact `Recent Activity` and warning-toned `Danger Zone` sections stay on the Overview page instead of a shared footer row. Touch and desktop now both keep that paused flow in one vertical single-column stack, while the desktop paused panel narrows to a single-column reading width instead of reflowing the dashboard into a multicolumn grid. Those paused-dashboard sections still expose stable labelled region anchors so desktop and touch accessibility flows can target the same `Overview`, `World Save`, `Shell`, `Recent Activity`, and `Danger Zone` destinations consistently, visible overview-page secondary sections still share `Jump to Overview` links, the dedicated `World Save` and `Shell` pages now return through explicit `Back to Overview` actions, and paused-dashboard keyboard focus now survives submenu page transitions plus `Recent Activity` show/hide rerenders by restoring the current section anchor or the nearest surviving section landmark. The default paused cards now stay metadata-first instead of relying on a global help toggle, the `World Save` page keeps shared browser-resume plus latest-action summary rows above grouped world-save actions, `Recent Activity` keeps only the latest world-save or shell-setting outcome plus any required follow-up warning, the `Danger Zone` section groups destructive reset and fresh-world consequences with shortcut metadata, and the dedicated `Shell` page keeps its `Active Layout`, `Binding Set`, and `Staged Preview` summary rows above the compact `Shell Hotkeys` editor, persistent `Gameplay Controls`, shell-profile import/export/apply tools, and `Telemetry Controls` card whose `Reset Telemetry` action still restores the full catalog to its default visible state while keeping the same saved-versus-session-only persistence feedback. The fixed-step simulation still starts only after the first `in-world` transition in that tab.
- Paused sessions now render a dedicated top-level `Overview` page, and its section-owned `Resume World` control keeps the current world-session save state, any persistent browser-resume warning, and the kept-session scope visible through compact metadata rows without extra paragraph help copy while an inline `Enter` shortcut badge keeps the moved action keyboard-discoverable inside the section card itself; that same page now also carries `World Save` and `Shell` navigation tiles so both tool groups can open as separate submenu pages instead of living inline above the other overview-page sections.
- Paused sessions now open `World Save` through that first Overview navigation tile into its own paused-menu page, and it keeps shared `Browser Resume`, `World Seed`, `Saved Again By`, `Last Export`, `Last Import`, and `Last Clear` rows visible beside grouped section-owned `Export World Save`, `Import World Save`, and `Clear Saved World` controls instead of repeating those items as separate paused-menu cards; the page also keeps an explicit `Back to Overview` action, the `Browser Resume` row now adds a compact `Saved` or `Missing` badge, the `World Seed` row stays aligned with the active paused session after fresh-world entry, import restore, and export flows, the latest export/import/clear rows now add compact outcome badges such as `Downloaded`, `Session only`, `Cleared`, or `Failed`, those three paused action cards still carry compact `Button only` heading badges so pointer-only affordances stay visually aligned with shortcut-badged actions elsewhere in the dashboard, and while the browser picker is active the `Import World Save` card switches into a local waiting state with a compact `Busy` heading badge so repeated taps stay debounced and the in-flight action stays scannable inside the section itself.
- Choosing `New World` from the paused main menu abandons the current world session, resets world, player-entity, camera, and undo runtime state plus shell visibility toggles back to first-start defaults, and immediately boots a fresh procedurally generated world with a new deterministic seed without reinitializing the already-loaded renderer or discarding persisted debug-edit preferences.
- Fresh worlds and untouched streamed chunks now generate from a shared rolling terrain sampler that lays grass on the exposed surface, keeps a shallow dirt band underneath, transitions into deeper stone, varies that terrain deterministically from a per-world seed, carves connected underground cave pockets only after a small stone overburden so near-surface spawn clearance still stays intact, and now also opens occasional surface-connected cave mouths outside a protected origin corridor so fresh spawn searches still stay dry and supported near the world start while seeding copper-ore pockets only into untouched upper-underground stone outside that same protected corridor. Default world-start spawn resolution now also anchors its vertical search to that seeded origin-surface height instead of raw `y=0`, so fresh sessions still start on surface ground even when seeded terrain shifts the origin well above or below the legacy flat-world band.
- The paused main menu also exposes `Reset Shell Toggles`, which clears saved in-world shell visibility preferences and reapplies the default-off HUD, edit-panel, edit-overlay, spawn-marker, and shortcuts visibility layout before the next `Resume World (Enter)` without resetting the current world session, then shows a `Reset Shell Toggles Result` card that either confirms that next-resume default-off layout or warns that browser shell storage could not be cleared and the reset currently applies only to the live paused session in this tab.
- The paused main menu `Shell Hotkeys` editor also exposes `Import Shell Profile`, which validates a detached `deep-factory.shell-profile` JSON file and stages its shell visibility toggles plus active shell hotkeys as a paused-menu preview with metadata-first `Toggle Changes` and `Hotkey Changes` rows plus grouped `Changed From Live` and `Matching Live` hotkey diffs against the live paused session; when either diff group has no rows, that empty state now collapses into a compact badge with explicit `No hotkey changes` or `All hotkeys changed` copy while the import button stays in a local waiting state during the browser picker. `Apply Shell Profile` commits that preview to the live paused session while the apply button stays in a matching local waiting state until any in-flight apply promise settles and now distinguishes no-op, toggle-only, hotkey-only, and mixed live-change apply results in its feedback copy plus compact `Saved` versus `Session only` badges, `Clear Shell Profile Preview` dismisses that staged preview without applying it, and `Export Shell Profile` downloads that same detached JSON snapshot, including session-only fallback layouts that may not currently exist in browser storage.
- Paused sessions now keep `World Save`, `Recent Activity`, and `Danger Zone` readable through concise titles plus metadata rows such as `Session`, `Reload`, `Replaces`, and `Next Resume`, so the default paused state no longer needs a `Show Help Text` toggle to expose card copy.
- Paused sessions now keep world-save and shell-setting feedback in a compact `Recent Activity` section instead of a collapsed `Results` accordion; the section shows only the latest recent outcome such as `Export Result`, `Import Result`, `Clear Saved World`, `Reset Shell Toggles Result`, or `Reset Telemetry Result`, adds compact `Info`, `Success`, or `Attention` badges to neutral, accent, and warning result cards so outcome state stays scannable without help copy, shows telemetry-reset feedback there only when `Reset Telemetry` actually changes the catalog, and when the latest world-save action still leaves an ongoing browser-resume warning in effect it also keeps that matching `Saved World Status` follow-up card visible below the latest activity instead of accumulating older feedback cards.
- Paused sessions now keep `Reset Shell Toggles` and `New World (N)` inside a dedicated `Danger Zone` section instead of the generic secondary card grid, and that section now owns both destructive buttons directly so consequence-heavy actions stay grouped under one warning summary while still surfacing compact `Warning` heading badges plus `Button only` versus desktop `N` access in their metadata rows and on the `New World` action's inline shortcut badge.
- Paused sessions now keep shell hotkey and shell-profile controls on a dedicated `Shell` page that opens from `Overview` and defaults to three summary rows only: `Active Layout`, which shows which shell toggles are currently on plus whether that layout is browser-saved or session-only; `Binding Set`, which shows whether the live hotkeys use the default or custom set while also surfacing recovered safe-set fallback or current-session-only hotkey status; and `Staged Preview`, which says whether a validated shell-profile preview would change layout, hotkeys, both, or neither. That page keeps the full hotkey editor, a `Gameplay Controls` card whose `Peaceful Mode` toggle persists a hostile-free mode that clears active slimes and blocks future hostile spawns while still distinguishing browser-saved versus current-session-only fallback state, shell-profile import/export/apply controls, and a `Telemetry Controls` card whose `Player`, `Hostile Slime`, `World`, and `Inspect` collections now cover spawn, combat, atlas, animated-mesh, lighting, liquid, pointer-hover, and pinned-inspect readouts through independent collection and per-type toggles; inspect telemetry now also surfaces non-liquid animated-tile frame indices plus current atlas-source bounds for torch and debug tiles in both the full debug HUD and the compact status strip. The same telemetry controls now also expose a `Reset Telemetry` action that restores every collection and type to the default visible state but stays disabled until at least one collection or type differs from the default catalog while its persistence metadata still distinguishes browser-saved versus current-session-only fallback state, the page returns through `Back to Overview`, and when a preview is staged the Shell page shows those details through compact metadata rows plus grouped changed-versus-matching hotkey diffs with badge-style empty states instead of repeating prose help copy in the generic paused-menu card grid.
- The paused main menu also exposes `Import World Save`, which opens a JSON picker, validates the selected top-level `deep-factory.world-save` envelope, restores valid imports through the existing paused-session runtime restore path without rebuilding renderer or input, leaves the paused session unchanged when the picker is canceled or the envelope is rejected, now waits through the browser picker refocus window before treating the action as canceled so valid file selections do not silently drop on return to the paused menu, keeps the `Import World Save` control locally disabled while that picker promise is active so repeated taps cannot queue duplicate imports, and shows a paused-menu `Import Result` card with either a canceled status when the picker closes without a file, a picker-failed status plus reason when the browser JSON picker throws before any file selection result exists, the last accepted filename after valid restores, the last rejected filename plus validation reason after rejected picks, the last selected filename plus thrown restore reason when runtime restore fails after validation, or the last selected filename plus browser-resume persistence warning when the restore succeeds only in the current tab, while the `World Save` section keeps any matching browser-resume warning visible until a later browser-save rewrite succeeds for that imported session.
- The paused main menu also exposes `Clear Saved World`, which deletes the persisted local paused-session world-save envelope while keeping the current paused world active in the current tab, keeps paused-menu `pagehide` autosave suppressed until that session resumes or a replacement save path rewrites browser storage, now keeps the `World Save` section warning rows in the missing-browser-resume state after a successful clear, and still shows a warning `Clear Saved World Result` card with the surfaced failure reason when deleting browser resume data fails while the paused session remains browser-saved.
- Once in-world, the shell keeps lightweight top-right `Main Menu`, `Recenter Camera`, `Show/Hide Debug HUD`, `Show/Hide Edit Panel`, `Show/Hide Edit Overlays`, `Show/Hide Spawn Marker`, and `Shortcuts (?)` actions; by default those six shell actions use `Q/C/H/G/V/M`, but the paused-menu `Shell Hotkeys` editor can now remap the first six through validated single-letter overrides that either rewrite browser storage immediately or stay live only for the current paused session when browser storage rejects the latest change, and invalid, duplicate, reserved, or unreadable saved values fall back to that default set while the shell chrome, shortcuts overlay, and touch keyboard reference all stay in sync with the active bindings. The `Main Menu` action returns to the main menu without rebuilding the initialized renderer or world session, `Recenter Camera` snaps back to the standalone player's body-center focus and clears any accumulated manual follow offset, the HUD toggle shows or hides the text telemetry overlay, the edit-panel toggle shows or hides the full `Debug Edit` control surface independently from the compact overlays, the edit-overlay toggle hides or restores the compact status strip, hovered or pinned inspect outlines, and one-shot preview overlays, the spawn-marker toggle hides or restores the resolved standalone player spawn AABB, support, and anchor overlay without discarding the active world session, and the shortcuts toggle plus matching desktop `?` shortcut show or hide an in-world desktop and touch controls reference that stays open when pausing to the main menu and resuming the same world session; these five in-world shell toggle states now persist through local storage, and missing, invalid, or unreadable persisted state falls back to all five toggles off.
- Standalone-player sessions now also carry a ten-slot hotbar inventory plus a selected slot index, but fresh sessions and save payloads that omit inventory now start with every hotbar slot empty instead of silently seeding a starter toolbelt. Those sessions still keep a separate `Head`/`Body`/`Legs` equipment state for `Starter Helmet`, `Starter Breastplate`, and `Starter Greaves`, and the debug `Item Catalog` plus later pickups or crafting can populate the same hotbar with carried items such as `Starter Axe`, `Acorn`, `Starter Pickaxe`, `Heart Crystal`, `Starter Sword`, `Umbrella`, `Bug Net`, `Starter Spear`, `Dirt Block`, `Dirt Wall`, `Stone Block`, `Wood Block`, `Wood Wall`, `Torch`, `Rope`, `Healing Potion`, `Copper Ore`, `Copper Bar`, `Gel`, `Wood`, `Bunny`, `Workbench`, `Furnace`, and `Anvil`. That state still renders through a fixed bottom in-world hotbar overlay that now also surfaces active healing-potion cooldowns directly on potion tiles through an animated vertical fill, shows selected heart-crystal blocked states as visible `DEAD` or `MAX` slot feedback, and shows selected starter-axe, starter-pickaxe, starter-sword, starter bug-net, or starter-spear windup, active, and recovery timing through phase-tagged `WIND`, `ACT`, or `REC` fills while those tools or attacks are in flight, expose shared `Left`, `Right`, `Drop 1`, and `Drop` actions plus desktop `Shift+[` / `Shift+]`, `Shift+Backspace`, and `Backspace` shortcuts so the selected slot can move one step left or right and one selected item or the full selected stack can be tossed into the world while cascading through nearby overlapping matching pickups before spawning any remainder, preserve hotbar state, equipped armor, and uncollected dropped-item stacks through world-save export/import and browser resume, collapse imported or browser-resumed overlapping matching world pickups into deterministic same-item stacks during restore, immediately rewrite the browser-resume save with that normalized pickup set when boot-time restore performs the collapse, and decode older saves that predate inventory, equipment, or dropped items to the same empty-hotbar baseline plus empty equipment and pickup state.
- The shared solid-block placement path now also recognizes stackable `Wood Block` hotbar slots, saves or restores them through the same inventory and world-snapshot paths as other carried stacks, and maps placed wood blocks onto a dedicated solid `wood_block` terrain tile that keeps its own terrain-autotile connectivity group instead of visually merging into dirt or stone terrain; that tile now also renders through its own committed authored-atlas sprite instead of the shared terrain placeholder block.
- Fresh world surface `grass_surface` tiles, placed `Dirt Block` terrain, upper-underground `stone`, and seeded `copper_ore` now each render through their own dedicated committed authored sprites instead of the shared terrain placeholder art, while keeping the same `ground` connectivity rules, terrain queries, and save or restore behavior that already backed surface generation, mining, and terrain placement.
- `TileWorld` snapshots now also preserve a separate zero-default background-wall layer alongside foreground tiles, and chunk meshing now resolves wall ids through dedicated wall metadata so `dirt_wall` plus placeholder `wood_wall` cells render through the shared chunk pass behind foreground tiles while resident chunks and pruned edited chunks round-trip deterministic wall ids through saves; play-mode `Dirt Wall` and `Wood Wall` use now write that same layer through enclosed-room placement checks instead of touching solid foreground tiles.
- The shared hotbar inventory, item catalog, dropped-item placeholders, and save validation now also recognize stackable `Acorn` and `Wood` slots, so debug-spawned, dropped, or chopped wood and acorn stacks round-trip through the same inventory, pickup, and browser-resume paths as other carried items.
- When the full `Debug Edit` panel is hidden, the selected hotbar stack also drives a simple play-mode item-use path: short left-clicks and touch taps queue item use against the hovered tile, the `Starter Sword` now starts a fixed-step windup/active/recovery swing aimed from the requested use target, shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected hotbar slot as the swing advances, applies deterministic per-slime hit cooldowns, knocks nearby hostile slimes away without consuming the weapon, and now leaves one merge-aware `Gel` pickup behind when a defeating hit removes a hostile slime, the `Starter Spear` now starts a fixed-step windup/active/recovery thrust aimed from the requested use target, clamps that thrust to a fixed maximum reach, shows that full reach through a hidden-panel preview line plus endpoint marker before or during the thrust, hits only slimes intersecting the aimed thrust line once per active window, and applies directional knockback plus defeat handling without consuming the weapon, the `Starter Axe` now shows distinct planted-sapling-versus-grown-tree chop preview states plus blocked feedback even when debug edit overlays are hidden, keeps that resolved growth-stage preview latched through successful active swings after the chopped tiles clear, starts a fixed-step windup/active/recovery swing against nearby planted saplings or grown small trees without consuming the tool, shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected hotbar slot as the swing advances, clears the full current planted or grown small-tree footprint through the shared anchor helper, and drops merge-aware `Wood` plus grown-tree `Acorn` pickups through the shared dropped-item path with `1` wood from planted saplings and `5` wood plus `1` acorn from grown trees, the `Starter Pickaxe` now shows a mineable-versus-blocked preview even when debug edit overlays are hidden, starts a fixed-step windup/active/recovery swing against nearby solid terrain or placed rope, torch, workbench, furnace, or anvil tiles without consuming the tool, and now shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected hotbar slot as the swing advances while breaking soft targets in one hit plus stone and copper ore in two hits and refunding merge-aware terrain or utility pickups through the shared dropped-item paths, the `Bug Net` now starts a fixed-step windup/active/recovery swing aimed from the requested use target, shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected hotbar slot as the swing advances, captures at most one nearby passive bunny during the active window, and adds one `Bunny` item to a matching or empty hotbar slot when inventory capacity is available without consuming the net, a selected `Bunny` stack now tints the hovered tile with can-release versus blocked feedback through that same hidden-panel preview path, adds a separate landing marker when nearby-ground fallback resolves to a different tile, and spends one item only when the same use path can release a passive bunny onto nearby valid ground around the requested tile, a selected `Healing Potion` now consumes through that same hidden-panel path without requiring placement, heals `30` health up to the current max without overheal, only works while the player is alive and below full health, starts a fixed-step `2s` reuse cooldown, and now shows blocked reuse through an animated vertical fill on healing-potion hotbar tiles until that cooldown clears, and a selected `Heart Crystal` now consumes through that same hidden-panel path to raise player max health by `20` up to `400` while also filling current health by the granted amount while surfacing visible `DEAD` or `MAX` hotbar feedback whenever use is blocked because the player is dead or already at that cap.

  Starter `Dirt Block`, `Stone Block`, `Wood Block`, `Dirt Wall`, `Wood Wall`, `Torch`, `Rope`, and crafted `Workbench`, `Furnace`, or `Anvil` stacks still tint that hovered tile with a placeable-versus-blocked preview, dirt, stone, and wood now all place one solid block only onto empty tiles with a solid cardinal neighbor, reject placements that would overlap the standalone player, and respect a default five-tile hotbar placement reach measured from the player body to the target tile cell, with `Wood Block` writing a dedicated `wood_block` terrain tile that keeps its own autotile seams, `Dirt Wall` and `Wood Wall` now place only into empty foreground cells whose up, down, left, and right directions each find nearby solid terrain so wall use stays inside enclosed pockets, torches keep their non-solid support-only rule while sharing that same reach gate, rope now places non-solid climbable rope tiles into empty cells whose direct tile-above anchor is solid or already rope or whose left or right neighbor is solid, and workbenches, furnaces, plus anvils now place single-tile non-solid stations into empty tiles with solid ground directly below while sharing that same reach gate and now clear immediately if that ground support later breaks; placed starter workbenches, furnaces, and anvils now each render through their own dedicated authored-atlas placeholder sprite instead of reusing the shared station placeholder slot, and using rope on an occupied rope tile keeps the touched segment as the preview and reach-check tile while the actual edit target resolves to the first non-rope tile below that contiguous rope column before support checks run.

  Pickaxe swings currently break grass, placed dirt, nearby placed rope, nearby placed torch, nearby placed workbench, nearby placed furnace, nearby placed anvil, and similar soft targets in one hit and stone plus seeded copper-ore terrain in two hits, clear the finished tile, and now refund one merge-aware `dirt-block` world pickup when grass-surface or placed dirt breaks plus one merge-aware `rope`, `torch`, `workbench`, `furnace`, or `anvil` world pickup when a placed utility segment or station is cut through the shared removed-tile refund path, one merge-aware `stone-block` world pickup when stone breaks, and one merge-aware `copper-ore` world pickup when copper ore breaks; those mined stone and copper-ore stacks use their own dropped-item placeholder palettes, flow through the same nearby-pickup merge path, and round-trip through save or restore.

  Successful placements still consume one stack item and persist both the placed terrain plus remaining counts through save or restore flows, removing a placed torch, rope, workbench, furnace, or anvil now refunds through the dropped-item path at that tile, those refund spawns first consolidate into overlapping matching torch, rope, workbench, furnace, or anvil pickups before any new entity appears, mined dirt, stone, and copper-ore refunds use that same merge-aware pickup path, torch, workbench, furnace, and anvil support-collapse clears use that same merge-aware pickup path, hostile-slime gel drops use that same merge-aware pickup path and their own dropped-item placeholder palette, imported or browser-resumed sessions now rebuild placed-torch emissive lighting immediately when the saved world loads, and upgraded player max health now persists through those same save or restore flows.
- When the full `Debug Edit` panel is visible in-world, a companion `Crafting` panel now shows separate nearby status lines for placed `Workbench`, `Furnace`, and `Anvil` stations and exposes twelve starter recipes: `Workbench` from `20 Dirt Block` anywhere, `Torch` from `1 Gel + 1 Wood` anywhere for `3` torches, `Wood Block` from `1 Wood` anywhere, `Wood Wall` from `1 Wood` anywhere for `4` walls, `Furnace` from `20 Stone Block + 4 Torch` while a placed workbench stays within the shared hotbar reach, `Healing Potion` from `2 Gel` at that same nearby workbench, `Copper Bar` from `3 Copper Ore` only while a placed furnace stays within the shared hotbar reach, `Anvil` from `5 Copper Bar` while a placed workbench stays within that same shared reach, `Starter Axe` from `9 Copper Bar` while a placed anvil stays within that shared reach, `Starter Pickaxe` from `12 Copper Bar` while a placed anvil stays within that shared reach, `Starter Spear` from `8 Copper Bar` while a placed anvil stays within that shared reach, and `Starter Sword` from `10 Copper Bar` while a placed anvil stays within that shared reach; each recipe card now also shows an inline `Ready to craft` or `Blocked: ...` status row instead of leaving blocker detail to button hover text alone.
- When the full `Debug Edit` panel is visible in-world, a companion `Item Catalog` panel now lets desktop and touch sessions filter one shared search across the hotbar item registry plus the crafting recipe registry, still spawn one matching item into the current hotbar whenever the shared inventory add rules can merge it into an existing stack or fit it into an empty slot, and also quick-craft matching recipe cards through the shared crafting path while those cards surface each recipe's output, ingredients, station requirement, and live ready-versus-blocked status.
- Standalone players now treat rope tiles on their body-center column as climbable traversal: with no vertical input they hold position on the rope while still overlapping that rope column, rope contact now applies grounded-style neutral-input horizontal braking so caught ropes bleed off lateral inertia before overshooting, that same neutral hold still gently recenters the player toward the rope column without flipping facing, sideways movement that clears the rope column resumes gravity on the same fixed step, a fresh jump press plus left or right input releases the rope into the normal jump arc even before the same fixed step fully clears the rope column, a short down tap followed by a held second `S`/`Down` or touch `Down` press temporarily releases rope hold into a quick drop until that held input releases or the rope bottom catches the player, and when that contiguous rope column ends on solid ground the quick drop now eases through the final rope tile so the landing stays free of fall damage; the full debug HUD and hidden-HUD compact status strip now also report whether that rope quick-drop is currently active plus whether the double-tap window is armed under player-motion telemetry, `W`, `Up Arrow`, or `Space` climbs upward, `S` or `Down Arrow` climbs downward, and the touch player pad now includes a matching `Down` hold button beside `Left`, `Right`, and `Jump`.
- WebGL2 renderer with shader utilities, buffer helpers, texture loading, and DPR-aware resize.
- Renderer atlas initialization ships with an authored atlas at `public/atlas/tile-atlas.png` exposed through a compile-time runtime URL derived from the active Vite base path, so local builds fetch `/atlas/tile-atlas.png`, project-site builds fetch `/deep-factory/atlas/tile-atlas.png`, and the renderer still falls back to a generated placeholder atlas whose canvas size and painted regions derive from `src/world/authoredAtlasLayout.ts` if that asset cannot be fetched or decoded. The authored atlas now spans `192x64`, keeps the torch centered inside full `16x16` regions `20` and `22`, still keeps region `21` blank as the spare slot, gives rope its own full `16x16` square with the strand centered inside transparent padding so the shared full-tile mesh no longer stretches that narrow utility sprite, keeps dedicated full-width `workbench` and `furnace` regions at `27` and `28`, gives `grass_surface`, `dirt_block`, `stone`, `copper_ore`, and `wood_block` dedicated terrain-art regions at `29` through `33`, renormalizes the shipped direct-`render.uvRect` metadata to that wider width without changing its pixel targets, and preserves transparent exterior padding to the right of `x=176`.
- Renderer world-snapshot loads now invalidate and recompute resident lighting from tile state before the restored world attaches, so stale serialized light caches cannot leave imported or browser-resumed emissive torch tiles dark during the first paused preview or resumed play frame.
- Renderer boot validates authored atlas-index regions plus direct tile `render.uvRect` metadata against the loaded atlas dimensions, logs a warning when any atlas-backed source falls outside the source image or any direct static or animated `uvRect` edge lands between atlas pixels, and surfaces the warning count plus first warning in debug telemetry.
- Renderer animates non-terrain tiles and resolved liquid variants from metadata-driven frame sequences at draw time by patching chunk UVs only when an animated tile or liquid variant's elapsed frame changes; partial-liquid variants now keep their top-edge surface texels anchored while those frame swaps recrop only the bottom-edge `v` coordinates from the already-meshed liquid heights, and terrain autotile plus other static tile UV resolution remain unchanged.
- Renderer now runs a separate entity pass after chunk draws; current supported entries are the facing-aware standalone-player world-space placeholder plus simple hostile-slime and passive-bunny placeholders. The player entry still uses grounded idle, alternating grounded-walk, jump-rise, fall, airborne wall-slide, and airborne ceiling-bonk silhouettes instead of a client-space DOM marker, places the quad from the interpolated entity-snapshot world position between fixed ticks, resolves pose selection from snapshot `current` plus snapshot-owned wall contact, ceiling contact, and bonk-hold timing captured during fixed updates, mirrors the wall-slide lean away from the blocking wall side, keeps the ceiling-bonk pose briefly latched after contact clears so fixed-step head hits still read visually, and modulates placeholder color by the brightest nearby resolved world-light sample around the interpolated render-position AABB so temporary player brightness tracks surrounding terrain lighting; hostile slimes use the same snapshot-backed entity pass and nearby-light modulation while still rendering as simple green blob placeholders, and passive bunnies now use that same snapshot-backed interpolation and nearby-light modulation while rendering as small brown ambient critter placeholders.
- Orthographic camera with anchored zoom, pointer pan controls, and standalone-player follow that tracks the player body center while preserving manual inspection offsets; desktop camera pan now starts from middle-mouse drag or debug-edit `Shift`-drag instead of reusing ordinary play-mode left click, converts client-space pointer deltas through the canvas backbuffer before world-space translation so movement stays consistent on DPR-scaled canvases, live drag or zoom camera deltas reconcile into the persistent follow offset before later follow reapplication or paused-session save snapshots, and preview plus in-world render frames now resolve the follow target from the same interpolated standalone-player entity snapshot used for placeholder drawing so the camera and visible player stay locked together between fixed ticks.
- Fixed-step game loop (`60hz`) with separate render interpolation alpha, now consumed by the renderer entity pass to smooth snapshot-backed entity placement between fixed updates.
- The standalone player's authoritative runtime state now lives in the shared entity registry; initial spawn bootstrap, fixed-step movement, camera follow, recenter, preview rendering, fresh-world resets, embedded recovery, and the new fixed-step death-hold plus respawn countdown all route through that one registry-backed ownership path, while paused-session save or restore keeps the detached death countdown beside the cloned player state. Preview plus in-world rendering now submit registry-backed entity-pass entries whose standalone-player snapshots combine cloned player state, snapshot-owned wall or ceiling plus bonk presentation state, interpolated entity-snapshot world position, snapshot-backed pose selection, interpolated-position nearby-light sampling, and render-frame camera follow aligned to that same interpolated snapshot focus point. Those same player-combat telemetry surfaces now show the live respawn countdown while a death hold is active and retain the latest death-hold status as `none`, `holding`, or `respawned` after recovery. That same registry now also owns dropped-item entities whose stacks preserve item id, amount, and world position, render as small world-space pickup placeholders, auto-merge into matching or empty hotbar slots on player proximity, and now absorb overlapping same-item player drops by filling nearby merge candidates in deterministic nearest-first order before any overflow remainder spawns as a new entity, plus hostile-slime entities whose fixed-step spawn helper round-robins deterministic surface windows around the player, keep per-slime health in authoritative entity state, despawn slimes once they leave a broader keep band or once sword or spear damage defeats them, now return one `Gel` pickup through that same nearest-first dropped-item cascade on defeat, and now step each live slime through grounded hop timing, player-biased chase turns, grounded short-range step-up hops for immediate one-tile rises, wall-flip recovery, landing retargets, and fixed-step player-contact combat without introducing a parallel entity list; passive-bunny entities now use that same registry to respawn from nearby deterministic surface windows, despawn outside their own keep band, and wander through short fixed-step hops that turn away from immediate walls or ledges before launch, while hidden-panel bug-net swings can now despawn one hit bunny there only when a matching or empty hotbar slot can accept the resulting `Bunny` stack and selected `Bunny` stacks can now respawn one passive bunny back into that same entity path only when nearby valid ground resolves around the requested tile; when paused-menu `Peaceful Mode` is on, only the hostile path clears active slimes and skips later hostile spawn or locomotion work until the mode is turned back off.
- Hard landings now apply fixed-step fall damage once landing speed rises above the safe threshold, may reduce health to `0`, start a brief fall-recovery window that suppresses immediate repeat landing damage, persist that post-fall health or recovery state through the existing world-save snapshot path, and now surface the latest hard-landing damage, exact impact speed, and current fall-recovery cooldown in the full debug HUD and hidden-HUD compact status strip under player combat telemetry.
- Hostile-slime overlap now applies fixed-step contact damage that may reduce the player to `0` health, starts a brief hostile-contact invulnerability window so the same overlap cannot tick every frame, persists that cooldown through the existing world-save snapshot path, and surfaces current/max health plus hostile-contact invulnerability time in the full debug HUD and hidden-HUD compact status strip. Those two telemetry surfaces now also keep the latest hostile-contact event result, including the applied damage amount after lethal-or-survived resolution, whether a later overlapping slime was blocked by the current invulnerability window, and the contacting slime's world tile plus facing direction when that event was recorded.
- The full debug HUD and hidden-HUD compact status strip now also surface hostile-slime spawn and locomotion telemetry: active slime count, next spawn cooldown, the next deterministic spawn-window index plus tile-offset target, and the nearest active hostile slime's world tile, horizontal and vertical chase offset from the player, current velocity, grounded state, chase-facing direction, remaining hop cooldown ticks, and active `standard-hop` versus `step-hop` launch kind while airborne.
- The full debug HUD and hidden-HUD compact status strip now route current player, hostile-slime tracker, world atlas or animated-mesh, lighting or liquid, and pointer or pinned inspect readouts through a persisted world-session telemetry catalog edited from the paused-menu `Shell` telemetry controls; collection-wide and per-type toggles default to enabled, survive browser storage when available, fall back to current-session-only state when storage writes fail, a paused-menu `Reset Telemetry` action restores that catalog to the all-enabled default state, stays disabled until a collection or type diverges from that default, and now also posts a saved-versus-session-only `Reset Telemetry Result` into paused-menu `Recent Activity` only when that reset actually changes the live catalog, and the compact strip now keeps its inspect chip plus pin actions visible even when pointer or pinned detail lines are hidden by those telemetry toggles.
- Water survival now tracks a fixed-step breath meter that drains only while the standalone player's head region is underwater, recovers breath once that head region clears water, applies drowning damage on a fixed cadence after breath runs out, persists current breath plus drowning-cooldown state through the existing world-save snapshot path, and now surfaces current breath, whether that head sample is presently submerged, the normalized water-overlap fraction across the player body, the live drowning cooldown, and the latest applied drowning tick damage in the full debug HUD and hidden-HUD compact status strip under player combat telemetry.
- Lava survival now keeps the shared fixed-step lava damage cadence in the player state, and the full debug HUD plus hidden-HUD compact status strip now surface whether the player currently overlaps lava, the normalized lava-overlap fraction, the live lava cooldown, and the latest applied lava tick damage under player combat telemetry.
- Player-combat telemetry now also keeps the current-session standalone-player death count plus the latest lethal damage source, applied damage, and lethal player world tile from fall, drowning, lava, or hostile-contact kills in both the full debug HUD and the hidden-HUD compact status strip, resolving that source through the same fixed-step damage order used by the live death-hold transition and capturing the death location from the same lethal fixed-step player state while leaving the death counter session-owned instead of save-owned.
- Chunked world model with `32x32` tiles per chunk and `16px` tiles.
- Shared procedural terrain baseline with rolling surface heights, shallow dirt bands, deeper stone, and a saved per-world deterministic seed backing untouched resident chunks.

## World Streaming And Meshing

- Chunk visibility culling with padded chunk streaming retention.
- Chunk pruning can evict resident world chunks from memory without losing edited tile values; sparse per-chunk tile overrides are reapplied when those chunks stream back in.
- Shared tile metadata and the committed atlas now also reserve dedicated non-solid `small_tree_sapling`, `small_tree_trunk`, and `small_tree_leaf` placeholder tiles, and the world layer now exposes shared helpers that resolve those stable sapling, trunk, and leaf IDs, classify small-tree tiles, pin canonical planted-sapling plus grown-tree local footprints with occupied-cell predicates, detect planted-versus-grown membership from sampled tree tiles, map canopy or trunk hits back to the planted support anchor only when the full anchored footprint is present, apply or replace those planted-versus-grown anchored layouts through one shared mutable `getTile`/`setTile` seam that clears prior-stage-only cells during transitions, evaluate grass-only planted anchors plus deterministic above-anchor growth obstructions before planted-to-grown replacement, rebuild tracked planted anchors from world snapshots, advance planted saplings through one detached fixed-step growth scheduler that only checks tracked anchors whose full tree footprint is currently resident, and now also clear planted or grown small trees immediately when their `grass_surface` anchor tile is removed or replaced while starter-axe chops reuse that same anchor contract to clear the full current planted or grown footprint and drop `Wood` from the resolved current stage plus `1` `Acorn` from grown trees. The current planted sapling now occupies `(0,-1)` above the support tile, and the current grown placeholder tree stays anchored at that same support cell while using trunk cells at `(0,-1)` and `(0,-2)` plus a three-leaf canopy across `(-1,-3)`, `(0,-3)`, and `(1,-3)` so future acorn-growth and starter-axe passes can target stable anchored tree layouts without reusing terrain autotile slots or changing current traversal behavior.
- A selected `Acorn` stack now reuses that same small-tree groundwork through the shared hidden-panel play-mode item-use path: desktop clicks and touch taps preview the requested grass anchor as plantable or blocked, spend one stack only on success, write one planted `small_tree_sapling` into the air tile directly above the targeted in-range `grass_surface` support, and persist that planted state through the ordinary world snapshot save or restore flow. Once planted, tracked saplings now recheck one deterministic fixed-step growth window every `60` ticks, only grow while that `grass_surface` support still exists and the grown canopy footprint stays clear, rebuild their tracked planted-anchor set from restored snapshots, and replace the sapling with the existing trunk-plus-canopy placeholder tree when their hashed anchor window comes due; if that support anchor is later removed or replaced, the planted sapling or grown tree now clears through the same world-edit path before save or restore snapshots are rebuilt, empty worlds and worlds with no tracked planted saplings skip that growth scheduler polling work entirely, and both planted and grown tree states round-trip through save or restore.
- Resident world chunks now also own zeroed per-chunk `Uint8Array` light fields plus dirty-light flags and dirty-local-column masks, and newly streamed chunks start with all local sunlight columns dirty so resident lighting caches can be rebuilt from tile state; draw-range chunks are made resident before the renderer's relight pass so visible stream-back chunks do not build once with zeroed dirty light and then immediately rebuild.
- Tile edits now clear and dirty only the loaded local sunlight columns affected by that edit whenever the edited tile changes `blocksLight` or emissive-light state; emissive source edits widen that invalidation to nearby columns within the edited source's emissive radius, zero-range non-emissive `blocksLight` edits now also invalidate the edited column's immediate neighbors so one-tile-gap blocker-face relighting recomputes adjacent columns, non-emissive `blocksLight` edits still widen invalidation when nearby resident emissive sources can still reach the edit, and non-emissive edge edits additionally invalidate loaded neighboring `chunkX` boundary columns plus their immediately adjacent interior columns so horizontal sunlight transport and boundary-adjacent solid-face relighting recompute both sides of resident chunk edges.
- Dirty resident light fields are recomputed during rendering by propagating sunlight top-down from exposed resident chunk tops only within resident `chunkX` columns that currently contain dirty light chunks, then transporting sunlight across loaded neighboring `chunkX` boundary columns when dirty edge columns are recomputed from sunlight-only boundary visibility rather than retained emissive falloff in clean neighbors, then writing sunlight onto blocking tiles that are cardinally adjacent to sunlit non-blocking tiles in those dirty columns (including recessed one-tile vertical air pockets whose horizontal neighbors are sunlit), then layering local emissive-source cardinal falloff over that sunlight base for those same dirty local columns; both sunlight and emissive recomputation write onto the first blocking tile in each lit path so exposed or adjacent solid faces stay visible while gameplay `blocksLight` metadata still terminates further propagation behind those blockers before rebuilt local columns are marked clean.
- World chunk rendering now modulates tile atlas color by resolved per-tile light values baked into chunk-mesh vertices, and dirty-light chunk recomputation now invalidates only cached chunk meshes whose light arrays actually changed, including neighboring boundary chunks touched by horizontal sunlight transport, so lighting-only changes reupload without broad relight-driven queue churn.
- Resident water and lava now keep per-tile `0..8` fill levels in world state and step in the fixed `60hz` update by transferring downward only from currently awake resident active-liquid chunks first, then sideways through deterministic loaded-chunk pair equalization that only scans the resulting active-liquid chunk band plus immediate horizontal neighbors; settled liquid chunks now stay resident but sleep after two quiet ticks so the alternating sideways-pair parity is preserved before they drop out of the active set, and local edits or nearby liquid transfers wake neighboring resident liquid chunks again. `TileWorld` still rebuilds awake liquid membership from resident chunk contents on snapshot load or stream-back, so completely dry resident worlds and fully sleeping settled pools both skip the liquid step until a disturbance reintroduces awake work. Hovered and pinned inspect telemetry surfaces that `liquidLevel:x/8` state, the same-kind liquid-surface input levels (`north`, `west`, `center`, `east`), the resolved liquid-surface branch classification (`empty`, `north-covered`, or `exposed`), the resolved normalized `liquidTopLeft` and `liquidTopRight` surface heights, the current liquid-variant frame-top and frame-bottom bounds (`liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`), the direct full-frame-height readout (`liquidFrameHeightV`, `liquidFramePixelHeight`), the resolved bottom-edge `liquidBottomLeftV` and `liquidBottomRightV` crop coordinates, the matching `liquidBottomLeftPxY` and `liquidBottomRightPxY` atlas pixel-row endpoints, the resolved visible-frame-height deltas plus per-side percentages (`liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPercentage`, `liquidVisibleRightPercentage`, `liquidVisibleLeftPixelHeight`, `liquidVisibleRightPixelHeight`), the cropped-off remainder deltas plus per-side percentages (`liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPercentage`, `liquidRemainderRightPercentage`, `liquidRemainderLeftPixelHeight`, `liquidRemainderRightPixelHeight`), and paired visible-plus-cropped per-side totals (`liquidCoverageLeftTotalPercentage`, `liquidCoverageRightTotalPercentage`, `liquidCoverageLeftTotalPixelHeight`, `liquidCoverageRightTotalPixelHeight`) for the current liquid variant frame directly, and chunk meshing now lowers exposed liquid top vertices from those fill levels while top-aligning the visible liquid frame crop so partial fills render flat or sloped without vertically squashing their authored surface texels, while same-kind-covered tiles remain full-height.
- A shared liquid-surface helper converts same-kind `north`, `west`, `center`, and `east` fill levels into a branch classification plus normalized `topLeft` and `topRight` heights by clamping raw levels, forcing full-height tops when liquid continues above, and averaging exposed shared corners with same-kind side neighbors so adjacent tiles resolve the same boundary height; the liquid meshing path and inspect telemetry now consume that helper-owned branch rule.
- Budgeted per-frame mesh build queue with visible-first scheduling and nearby prefetch.
- Tile edit events trigger edge and corner neighbor-chunk mesh invalidation.
- Chunk meshing currently emits one quad per non-empty tile with per-vertex world position, UV, and resolved light level; liquid quads can lower their top-left and top-right vertex heights from shared surface-fill resolution and crop their bottom-edge `v` coordinates from those same heights so the authored liquid surface stays top-aligned, while static chunks still upload once and chunks with animated non-terrain or liquid quads keep CPU-side vertex copies so UV-only updates can be reuploaded on frame boundaries.
- Liquid tiles now carry separate liquid-render metadata and chunk meshing resolves their `variantRenderByCardinalMask` entries from sampled NESW liquid connectivity, falling back to the isolated mask when neighborhood sampling is unavailable.
- `buildChunkMesh` pre-counts non-empty tiles and writes directly into an exact-sized `Float32Array`.
- Chunk meshing supports reusable `TileNeighborhood` scratch sampling so terrain meshing avoids per-tile neighborhood allocations.

## Save/Load Foundations

- `src/world/chunkSnapshot.ts` now defines a version `1` chunk snapshot contract for save/load work, and `TileWorld` can now create or load JSON-safe world snapshots on top of it: resident chunks serialize dense row-major `tiles`, `liquidLevels`, and `lightLevels` through run-length/value pairs plus explicit light dirtiness metadata, sparse edited chunk overrides serialize sorted tile-index/value pairs for tile and liquid edits, top-level world snapshots preserve the authoritative per-world seed together with deterministic chunk ordering plus `liquidSimulationTick` parity for post-load liquid behavior, and focused regressions cover round-trips, load validation, streamed-out edited overrides, and post-load liquid continuation.
- `src/mainWorldSave.ts` now defines the version `1` top-level `deep-factory.world-save` envelope for session persistence work: it wraps a validated `TileWorld` snapshot, including that deterministic per-world seed, with standalone-player session state, standalone-player equipment state, camera follow offset, and paired migration metadata, deep-clones the payload during create or decode, and rejects malformed session or nested world data before export, local persistence, or restore paths consume it.
- `src/mainWorldSessionSave.ts` now assembles a live session-owned export payload by reading the current world snapshot, standalone-player state, standalone-player equipment state, dropped-item entity stacks, and camera follow offset into that top-level save envelope, while `Renderer.createWorldSnapshot()` exposes the renderer-owned `TileWorld` state without handing out the mutable world instance, and the paused main menu now uses `src/mainWorldSaveDownload.ts` to export that envelope as a pretty-printed downloadable JSON file without mutating the active session.
- `src/mainWorldSaveLocalPersistence.ts` now provides the matching browser `localStorage` adapter for that envelope, and `src/main.ts` uses it to restore the last valid saved session at boot, persist the current session when play first starts, when the session pauses, when a fresh world or paused-session restore replaces the runtime state, and when the page hides, while malformed stored payloads are cleared before boot falls back to the first-launch main menu; a paused-menu `Clear Saved World` action can remove that stored envelope while temporarily suppressing paused-menu autosave until the session resumes or another save-replacement path runs.

## Networking Scaffolding

- `src/network/protocol.ts` now defines the version `1` transport-agnostic networking contract for current scaffolding: player-input messages carry fixed-step `tick` plus normalized `moveX`/`jumpPressed` intent, chunk-tile-diff messages carry chunk coordinates plus row-major `tileIndex` edits with tile and liquid state, entity-snapshot messages carry fixed-step `tick` plus deterministic entity-id-ordered position, velocity, and flat scalar state bags, and focused create/decode helpers normalize local payloads while rejecting malformed or out-of-order wire data before later transport work lands.
- `src/network/chunkDiffBatching.ts` now batches authoritative same-tick tile edits into deterministic y-major chunk-diff message arrays, collapses repeated writes for one tile down to the final same-tick result, omits tiles whose final state matches their same-tick starting state, and rejects incoherent previous-state chains before transport work turns those batches into wire sends.
- `src/network/tileEditCapture.ts` now provides a transport-agnostic authoritative tile-edit capture helper that records detached previous and next tile-plus-liquid state from world edit notifications, can discard pending notifications on reset, and drains those captured edits back through the same-tick chunk-diff batching helper at tick boundaries without importing `TileWorld` or renderer internals into networking code.
- `src/network/replicationStaging.ts` now stages one deterministic authoritative replicated-state batch per tick by validating that a caller-supplied entity-snapshot message matches the staged tick before any drain occurs, draining captured tile edits into deterministic chunk-diff messages, cloning that entity snapshot, and appending it after the chunk diffs so later transports can emit one stable chunk-first/entity-last batch.
- `src/network/replicationBaseline.ts` now provides a transport-facing authoritative resync helper that routes one caller-supplied world replacement callback and one entity-snapshot baseline through `AuthoritativeReplicatedNetworkStateReplayer.replaceAuthoritativeBaseline()`, applies the entity baseline without tick-guard rejection, and returns the applied entity replacement summary before the replay guards are cleared so later transports can log resync outcomes without rebuilding that baseline path.
- `src/network/replicationBaselineSummary.ts` now reduces those applied baseline results into concise `baseline.tick` and `baseline.entityCount` metadata plus spawned, updated, and removed entity counts so transport resync logs or future diagnostics do not need to retain the raw replacement id arrays.
- `src/network/replicationResyncDiagnostics.ts` now accumulates those baseline summaries into per-client running spawned, updated, and removed totals while keeping the latest applied baseline tick and entity-count metadata separate from the cumulative counters, so transport resync logging can retain long-lived diagnostics without losing the last accepted baseline boundary.
- `src/network/interestSet.ts` now derives client interest sets from camera-centered viewport size and zoom plus configurable chunk and entity padding, returns deterministic y-major chunk coord lists and ascending entity-id membership for relevant entities, and can diff consecutive interest snapshots into chunk and entity enter/exit arrays for later replication transport.
- `src/network/snapshotFilter.ts` now trims authoritative payloads against the current client interest set before replay: out-of-interest chunk-diff messages are dropped entirely, entity-snapshot payloads keep only currently relevant entity ids, and fully trimmed entity snapshots still replay as empty replacements so local replicated entities clear when client interest moves away from them.
- `src/network/replicationBatchFilter.ts` now provides the transport-facing send-side counterpart to that interest filter: it maps one staged authoritative chunk-first/entity-last batch through the current client interest set, returns only the forwarded messages that should be sent to that client, and records deterministic per-message `dropped`/`kept`/`trimmed` diagnostics without losing the empty entity snapshot replacement that interest moves still need.
- `src/network/replicationBatchFilterSummary.ts` now reduces one client send batch's filter diagnostics into shared staged tick metadata plus chunk and entity `dropped`/`trimmed`/`forwarded` message counts, with `forwarded` intentionally including trimmed entity snapshots because they still travel over the transport.
- `src/network/replicationSendDiagnostics.ts` now accumulates those batch-filter summaries into per-client running chunk and entity `dropped`/`trimmed`/`forwarded` totals while keeping the latest staged tick metadata separate from the cumulative counters, and empty summaries that add no staged batch preserve the last known staged tick instead of erasing it during idle send ticks.
- `src/network/stateReplay.ts` now provides the matching client-side replay path for authoritative state: chunk-tile-diff messages map their row-major tile indices into any local `setTileState()` target, entity-snapshot messages replace a local `ReplicatedEntitySnapshotStore` keyed by authoritative entity id, and an `AuthoritativeReplicatedNetworkStateReplayer` now guards that replay by tracking last-applied ticks per chunk plus per entity-snapshot stream so duplicate or stale authoritative messages are skipped instead of rolling local replicated state backward. Both the store and the replayer now also expose explicit replay-guard reset helpers for transport resync or out-of-band baseline replacement, and the replayer can now wrap one caller-supplied replicated-state baseline replacement callback before clearing both guard sets so resync code does not need to remember a second manual reset step. Those reset paths clear only tick bookkeeping while leaving the current replicated contents intact. `TileWorld` and `Renderer` now expose `setTileState()` so replayed or simulated liquid-only changes preserve explicit liquid levels and still invalidate chunk meshes through the existing tile-edit listener path.
- `src/network/replicationDispatch.ts` now composes interest filtering with guarded authoritative replay for whole message batches: out-of-interest chunk diffs report `dropped` without mutating local state, entity snapshots report when interest trimming reduced the forwarded payload, and every forwarded chunk/entity message also reports whether guarded replay `applied` it or `skipped` it as stale or duplicate so later transport code can keep per-client diagnostics without reimplementing the filter-plus-replay flow.
- `src/network/replicationDispatchSummary.ts` now reduces those authoritative dispatch results into deterministic per-batch chunk and entity `dropped`/`trimmed`/`applied`/`skipped` counters so later transport code can log or accumulate per-client filter and replay outcomes without walking every per-message result again.
- `src/network/replicationDiagnostics.ts` now accumulates those dispatch summaries into per-client running chunk and entity `dropped`/`trimmed`/`applied`/`skipped` totals while keeping the latest processed fixed-step tick metadata separate from the running counters, so transport logging can retain long-lived diagnostics without losing the most recent batch boundary.
- `src/network/replicationDiagnosticsSnapshot.ts` now combines per-client replay, send, and resync diagnostics into one detached JSON-safe snapshot with explicit `replay`, `send`, and `resync` sections, so transport logging or later registry helpers can capture the full per-client replication picture without flattening mismatched counter schemas or mutating the live accumulators.
- `src/network/replicationDiagnosticsReset.ts` now resets that combined replay/send/resync snapshot back to zeroed totals plus cleared metadata in one detached step, so reconnect or client-replacement flows can reuse the shared sectioned shape without manually zeroing each section or mutating the stored snapshot in place.
- `src/network/replicationDiagnosticsRegistry.ts` now stores those combined diagnostics snapshots by client id in a detached per-client registry, can reset one client entry back to a zeroed snapshot for reconnect or client-replacement flows, and can remove one client entry for disconnect flows while `getSnapshot()` and `setSnapshot()` both keep callers from mutating the registry’s internal snapshot state.
- `src/network/replicationDiagnosticsRegistrySnapshot.ts` now exports that registry into detached `{ clientId, snapshot }` entries sorted by deterministic ascending client-id order, so periodic multi-client logging and later aggregate helpers can consume a stable ordered list instead of depending on `Map` insertion order.
- `src/network/replicationDiagnosticsAggregate.ts` now reduces that ordered client list into detached multi-client `clientCount` plus aggregate replay/send/resync totals, so periodic transport log headers can report fleet-wide replication counters without inventing merged per-client last-tick metadata.
- `src/network/replicationDiagnosticsLogPayload.ts` now combines those aggregate totals with a detached ordered `clients` snapshot list into one JSON-safe periodic multi-client summary payload, so later text formatting or emission helpers can consume one stable log object instead of recomputing totals or re-cloning client snapshots.
- `src/network/replicationDiagnosticsLogLineFormatting.ts` now exposes transport-facing aggregate-header and per-client diagnostics line helpers that render shared replay/send/resync totals plus one ordered client snapshot entry into deterministic labeled lines with explicit `n/a` placeholders for missing client metadata, so alternative log sinks can reuse the same header and client-section output without rebuilding their line order or placeholder rules.
- `src/network/replicationDiagnosticsLogLineFormatting.ts` now also exposes a whole-log line helper that assembles the fixed `ReplicationDiagnostics` header, aggregate section, `Clients:` or `Clients: none` divider, and ordered per-client sections into one detached deterministic string array before any newline join, so transport code can consume the shared log order without splitting formatter output back into lines.
- `src/network/replicationDiagnosticsLogFormatter.ts` now renders that payload into deterministic multi-line console text by emitting the aggregate section first, then each client section in payload order, while using explicit `n/a` placeholders when per-client replay, send, or resync metadata has not been observed yet.
- `src/network/replicationDiagnosticsLogEmission.ts` now snapshots that per-client registry once per emission, builds the shared log payload, formats deterministic multi-line output, and returns the frozen `payload` beside detached `logLines`, joined `logText`, and `nextDueTick = tick + intervalTicks`, so transport polling loops and later structured sinks can reuse one detached emission without re-snapshotting, reformatting, or rebuilding payload data.
- `src/network/replicationDiagnosticsLogCadence.ts` now tracks one transport-owned replication diagnostics `nextDueTick`, returns silent `{ emitted: false, payload: null, logLines: null, logText: null }` results while fixed-step polling remains before that boundary, and emits at most one formatted log through `src/network/replicationDiagnosticsLogEmission.ts` once a poll reaches or passes the due tick, forwarding the same detached `payload`, `logLines`, and `logText` while rescheduling from the emitted tick instead of replaying missed intervals.
- `src/network/replicationDiagnosticsLogSink.ts` now wraps that cadence with one injected due-only sink callback, forwarding `nextDueTick`, joined `logText`, and cloned detached `payload` plus `logLines` only when polling emits, so transport adapters can attach side effects without reimplementing cadence checks or letting sink-side payload or line mutation leak back into the returned poll result.
- `src/network/replicationDiagnosticsFanOutSink.ts` now composes multiple due-only sink callbacks into one declaration-ordered sink that clones each emitted payload and line array per downstream sink, so text, line, and future payload logger adapters can share one emission without one sink's mutation leaking into another sink.
- `src/network/replicationDiagnosticsPayloadLogger.ts` now adapts one structured payload logger into that due-only sink contract by forwarding only emitted detached `payload`, so transport code can plug in one JSON-oriented logger without consuming joined text or detached lines that other logger adapters may still need.
- `src/network/replicationDiagnosticsLineLogger.ts` now adapts one line-array logger into that due-only sink contract by forwarding only emitted detached `logLines`, so transport code can plug in one line-oriented logger without splitting joined text while the returned cadence result still keeps `logText` available for other logger adapters.
- `src/network/replicationDiagnosticsTextLogger.ts` now adapts one console-style joined-text logger into that due-only sink contract by forwarding only emitted `logText`, so transport code can plug in one `console.log`-style callback without rejoining `logLines` or taking detached line access away from other sinks.
- `src/network/replicationDiagnosticsLoggerBundle.ts` now composes any configured text, line, and payload logger callbacks into one due-only sink by wiring the existing adapters through the shared fan-out helper in fixed text, line, then payload order; omitted logger types stay out of the bundle, and an explicitly empty bundle still acts as a silent no-op sink when a caller uses it directly.
- `src/network/replicationDiagnosticsLoggerRunner.ts` now composes one existing diagnostics cadence with one existing mixed logger bundle into a ready-to-poll runner that exposes `getNextDueTick()` plus `poll()`, while reusing the underlying sink wrapper so due emissions still clone payload and line data before mixed logger callbacks can mutate them.
- `src/network/replicationDiagnosticsLoggerFactory.ts` now accepts cadence settings plus optional text, line, and payload logger callbacks, returns `null` when logging is disabled, and otherwise builds that same ready-to-poll diagnostics runner through the shared cadence, bundle, and runner helpers, so transport code can toggle periodic replication diagnostics without instantiating an unused silent runner.
- `src/network/replicationDiagnosticsLoggerPoll.ts` now turns one nullable diagnostics logger runner into a fixed-step callback that no-ops while logging is disabled and otherwise delegates raw `tick` values into the shared runner poll path, so update-loop transport wiring can keep one narrow `poll(tick)` shape without special-casing the disabled runner state at every call site.
- `src/network/replicationDiagnosticsLoggerReconfiguration.ts` now rebuilds one nullable diagnostics logger runner plus its matching nullable fixed-step poll callback from updated cadence settings and optional text, line, and payload logger callbacks, so later transport holders can swap diagnostics logging on, off, or between callback sets through the shared factory and poll composition path without duplicating disabled-state branching.
- `src/network/replicationDiagnosticsLoggerStateHolder.ts` now owns the current nullable diagnostics poll callback reconfiguration, exposes a transport-facing `poll(tick)` method that silently no-ops while disabled and otherwise delegates into the active callback, exposes `getScheduleSnapshot()` so later holder helpers can distinguish disabled logging from an enabled holder's current `nextDueTick`, exposes a separate `getCallbackPresenceSnapshot()` with `hasTextLogger`, `hasLineLogger`, and `hasPayloadLogger` flags so later holder helpers can inspect configured logger types without widening the schedule snapshot contract, exposes `getConfigurationSnapshot()` so transport code can export one detached JSON-safe `{ schedule, callbacks }` view with disabled-vs-enabled schedule state, active `intervalTicks`, current `nextDueTick`, and callback-presence flags without reading runner internals, exposes `reconfigureFromConfigurationSnapshot(...)` so transport code can reapply one detached configuration snapshot onto an existing holder using a supplied registry plus live logger callbacks while disabled snapshots stay disabled and enabled snapshots only attach the callback types flagged by the snapshot, exposes `refreshSchedule(...)` so one enabled holder can swap `nextDueTick` without resetting its current `intervalTicks` or current logger callbacks, exposes `refreshCallbacks(...)` so one enabled holder can swap text, line, and payload logger callbacks without resetting its current due tick, exposes `refreshScheduleAndCallbacks(...)` so one enabled holder can swap `nextDueTick` plus text, line, and payload logger callbacks together without resetting its current `intervalTicks`, exposes `refreshScheduleAndCadence(...)` so one enabled holder can swap `nextDueTick` plus `intervalTicks` together without resetting its current logger callbacks, exposes `refreshScheduleAndCadenceAndCallbacks(...)` so one enabled holder can swap `nextDueTick`, `intervalTicks`, plus text, line, and payload logger callbacks together through one holder-owned refresh path, exposes `refreshCadence(...)` so one enabled holder can swap `intervalTicks` without resetting its current due tick or current logger callbacks, exposes `refreshCadenceAndCallbacks(...)` so one enabled holder can swap `intervalTicks` plus text, line, and payload logger callbacks together without resetting its current due tick, and reconfigures that callback through the shared reconfiguration helper so later transport wiring can toggle or swap diagnostics logging without carrying separate runner and callback variables.
- `src/network/replicationDiagnosticsLoggerConfigurationSnapshotDecode.ts` now decodes unknown persisted or transport-supplied diagnostics logger `{ schedule, callbacks }` payloads into the same detached configuration-snapshot contract emitted by the holder, rejecting disabled payloads that retain cadence data or callback flags and enabled payloads that lack a positive cadence, non-negative due tick, or any callback-presence flag before restore code touches live diagnostics state.
- `src/network/replicationDiagnosticsLoggerConfigurationSnapshotCallbackValidation.ts` now validates supplied live text, line, and payload diagnostics logger callbacks against one detached configuration snapshot's callback-presence flags and returns only the callback types that snapshot actually enables, so transport restore code can reject missing required callbacks before holder reapply while harmless extra supplied callbacks stay ignored.
- `src/network/replicationDiagnosticsLoggerConfigurationChangeSummary.ts` now compares two detached diagnostics logger `{ schedule, callbacks }` snapshots and returns grouped change flags for schedule enablement, cadence, due-tick, and text/line/payload callback-presence differences so transport lifecycle code can detect configuration drift without reading holder internals or comparing live callbacks.
- `src/network/replicationDiagnosticsLoggerConfigurationChangeLineFormatting.ts` now renders those detached configuration-diff summaries into deterministic `ConfigurationChange`, `ConfigurationScheduleDiff`, and `ConfigurationCallbackDiff` line arrays, listing only changed schedule fields in `disabled`, `intervalTicks`, `nextDueTick` order and changed callback fields in `hasTextLogger`, `hasLineLogger`, `hasPayloadLogger` order while unchanged groups emit explicit `none` placeholders for later lifecycle logging.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLineFormatting.ts` now renders one restored detached diagnostics logger configuration snapshot plus one detached configuration-diff summary into deterministic `ConfigurationRestore`, `ConfigurationRestoreSchedule`, and `ConfigurationRestoreCallbacks` line arrays, inlining schedule and callback diff labels beside the restored values while disabled cadence fields surface as explicit `n/a` placeholders for later lifecycle text formatting.
- `src/network/replicationDiagnosticsLoggerConfigurationLifecycleTextFormatting.ts` now joins detached configuration-change and configuration-restore lifecycle line arrays into deterministic multi-line console text, keeping the full change section ahead of the restore section and adding no extra separators when either side is empty so later transport lifecycle sinks can reuse one console-ready string without rebuilding those arrays.
- `src/network/replicationDiagnosticsLoggerConfigurationLifecycleTextLogger.ts` now adapts one lifecycle text logger into that shared line-array sink contract by formatting the supplied configuration-change and configuration-restore lines on demand, so transport lifecycle fan-out can forward joined text to one callback without consuming the detached line arrays that sibling loggers may still need.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLifecycleEmission.ts` now snapshots the previous detached diagnostics logger configuration before restore, reapplies one unknown payload through the shared restore helper, diffs the previous and restored snapshots, and returns detached lifecycle payload data, change-and-restore line arrays, plus joined lifecycle text in one caller-owned emission object for later payload, line, or text logger fan-out.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLifecyclePayloadLogger.ts` now adapts one restore-lifecycle payload logger into that emission-sink contract by cloning and forwarding only the emitted lifecycle payload data, so payload logger mutation cannot leak back into the shared line arrays or joined lifecycle text that sibling sinks may still need.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLifecycleLineLogger.ts` now adapts one restore-lifecycle line logger into that emission-sink contract by cloning and forwarding only the emitted configuration-change and configuration-restore line arrays, so line logger mutation cannot leak back into the shared payload data or joined lifecycle text that sibling sinks may still need.
- `src/network/replicationDiagnosticsLoggerConfigurationRestoreLifecycleTextLogger.ts` now adapts one restore-lifecycle text logger into that emission-sink contract by forwarding the emitted joined lifecycle text directly, so console-style lifecycle logging can share one restore emission without consuming the detached payload data or line arrays that sibling sinks may still need.
- `src/network/replicationDiagnosticsLoggerConfigurationSnapshotRestoreCallback.ts` now captures one diagnostics logger holder, registry, live runtime callback set, and optional restore-lifecycle mixed logger bundle into a reusable unknown-snapshot restore function backed by the shared restore-and-log helper, while `src/network/replicationDiagnosticsLoggerConfigurationSnapshotRestoreCallbackFactory.ts` now returns `null` when no restore-lifecycle text, line, or payload logger is configured and otherwise builds that shared restore-lifecycle bundle plus restore callback stack so transport lifecycle wiring can install restore logging only when lifecycle sinks are enabled.
- `src/mainWorldSessionRestore.ts` now provides the matching session-owned restore helper for decoded world-save envelopes, `src/mainWorldSaveImport.ts` now provides the browser JSON picker plus top-level envelope validation helper for paused-menu imports, and `src/main.ts` now exposes a paused-session `restorePausedWorldSessionFromSaveEnvelope()` runtime action that routes decoded envelopes through that helper, swaps the renderer-owned `TileWorld`, restores standalone-player plus standalone-player equipment plus dropped-item plus camera-follow session state without rebuilding renderer or input wiring, clears session-only debug history, armed tools, and pinned inspect state, rerenders the paused preview, and writes the replacement session back through local persistence.

## Tile Metadata And Terrain Resolution

- Tile definitions live in validated JSON metadata at `src/world/tileMetadata.json`.
- Tile metadata covers render data, terrain autotile data, liquid-render variant maps plus liquid-only connectivity groups, material tags, and gameplay flags such as `solid`, `blocksLight`, `liquidKind`, and optional `emissiveLight`.
- Render metadata can optionally define animated frame sequences through `frames` plus `frameDurationMs`, while the current meshing path continues to use the base static `atlasIndex` or `uvRect` as frame-zero fallback.
- The default tile set now includes an animated `debug_blink` brush tile that exercises the renderer-side frame resolver against the authored atlas.
- The default tile set also includes an animated `debug_panel_blink` brush tile that exercises the same renderer-side frame resolver through direct `render.uvRect` frames backed by committed atlas pixels and now emits local light (`emissiveLight: 12`) as the first default non-liquid emissive debug source.
- The default tile set now also includes an animated emissive `torch` tile that exercises the non-liquid atlas-index animation path through two committed full-width utility regions while leaving the documented spare slot blank.
- The default tile set now also includes placeholder `water` and `lava` brush tiles with dedicated `liquidRender` metadata, separate liquid connectivity groups, and animated liquid frame sequences; water resolves distinct placeholder surface, edge, body, corner, and T-junction sources (including dedicated `NES-`, `N-SW`, and `-ESW` masks) from liquid masks through two-frame direct-`uvRect` animation in dedicated authored regions, and lava now resolves matching surface, edge, body, corner, and T-junction source classes (including dedicated `NES-`, `N-SW`, and `-ESW` masks) plus a dedicated isolated-body `----` source, dedicated `N---`, `-E--`, `--S-`, and `---W` single-side sources, dedicated `-E-W` and `N-S-` straight-through sources, and dedicated `NE-W` and `NESW` cross or fully surrounded sources by animating mask-specific direct-`uvRect` crops over the existing authored lava frames.
- Committed-asset regressions also verify that every default animated atlas-index frame differs from its prior committed PNG frame, so shipped atlas-backed animations do not silently collapse into repeated art.
- Atlas-backed render metadata resolves through explicit authored atlas region definitions in `src/world/authoredAtlasLayout.ts`, while the generated placeholder fallback atlas paints those same authored regions and direct sub-rect metadata can still use normalized `uvRect` values that are runtime-validated against whole atlas-pixel edges.
- Runtime atlas validation and committed-asset regressions also walk liquid variant render sources, so shipped liquid metadata stays inside the authored atlas and whole-pixel direct-UV bounds.
- Committed-asset regressions also verify that every default direct `render.uvRect` source covers at least one non-transparent pixel in the shipped atlas PNG, so blank authored sub-rects do not silently ship through the direct-UV path.
- Committed-asset regressions also verify that every static default direct `render.uvRect` source stays fully inside the explicit authored atlas regions, so shipped direct-UV tiles cannot silently sample uncovered atlas canvas.
- Committed-asset regressions also verify that every default animated direct `render.uvRect` frame stays aligned to whole atlas pixels in the shipped PNG, so committed direct-frame animations remain sampling-safe against the authored asset dimensions.
- Committed-asset regressions also verify that every default animated direct `render.uvRect` frame differs from its prior committed PNG frame, so shipped direct-UV animations do not silently collapse into repeated art.
- Committed-asset regressions also verify that every default animated direct `render.uvRect` frame stays fully inside the explicit authored atlas regions, so animated direct-UV metadata cannot silently sample uncovered atlas canvas.
- Those animated frame-difference regressions ignore RGB drift under pixels that are fully transparent in both compared committed PNG frames, so invisible padding churn does not count as a shipped animation change.
- Committed-asset regressions also verify that every default direct `render.uvRect` source, including animated frames, stays clear of the fully transparent exterior padding strip beyond authored regions, so shipped direct-UV metadata cannot silently consume that spill-detection band.
- The authored atlas layout also records any intentionally unused committed slots, currently reserves one documented full-width spare region beneath the torch square while using a second full-width torch animation square to the right, and keeps a widened fully transparent exterior padding strip outside the authored region bounds; committed-asset regressions verify that both the spare slot and that strip stay blank so intentional empty atlas space remains available for spill and accidental-reference detection.
- Committed-asset regressions also verify that non-transparent shipped atlas pixels stay inside authored regions touched by default metadata or documented as intentionally unused, so uncovered atlas canvas cannot silently accumulate visible spill.
- Committed-asset regressions also verify that every explicitly unused authored atlas region stays fully transparent in the shipped PNG, so documented spare slots cannot silently pick up stray art.
- Committed-asset regressions also verify that every explicitly unused authored atlas region stays unreferenced by shipped `atlasIndex` metadata and default direct `render.uvRect` sources, so reserved blank slots cannot silently become live metadata targets.
- Committed-asset regressions require every authored region index to be either referenced by default tile metadata or documented there as deliberately unused.
- Terrain autotile adjacency supports cross-chunk 8-neighbor sampling plus normalization helpers.
- Terrain autotile connectivity uses metadata-driven connectivity groups first, then shared material tags for seam compatibility.
- Shared `wood_block` terrain autotile layout still uses 16 cardinal-mask variants documented in [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md), while `grass_surface`, `dirt_block`, `stone`, `copper_ore`, and `wood_block` now use dedicated authored-atlas overrides for their visible terrain art without changing the underlying connectivity lookups that still resolve through the authored atlas region layout.

## Collision Foundations

- World collision helpers expose metadata-backed `isSolidAt` tile queries, world-space solid-overlap checks for half-open AABBs, and single-axis AABB sweep results that clamp movement to the first blocking tile.
- Player spawn queries can scan near a chosen origin tile for grounded standing headroom, skip standing AABBs that overlap water or lava fill, and return the resolved standing AABB plus supporting solid tile.
- Standalone player-state helpers track feet-centered `position`, `velocity`, explicit `size`, `grounded`, `facing`, current `maxHealth`, current `health`, and the remaining lava-damage tick cooldown, can seed that state from spawn output, derive the collision AABB plus one-step fixed-update integration from the same model, expose a body-center camera focus point, probe adjacent support, wall, and ceiling contacts from the current AABB while tagging wall contacts with a resolved `left` or `right` side, sample overlapped partial-liquid fill area from world liquid levels, resolve normalized movement intent into grounded walk acceleration or braking plus a grounded jump impulse, apply gravity plus fall-speed clamping together with water buoyancy and swim drag, advance through x-then-y collision sweeps that zero blocked velocity and recompute grounded support after movement, and apply periodic lava contact damage inside that same shared fixed-step step.
- The fixed update loop now keeps a spawned standalone player state alive through shared keyboard and touch movement intent, so desktop keys and touch buttons can move and jump the player while water slows and floats the player, holding jump while the selected `Umbrella` is active now clamps airborne descent into a slow glide until that hold releases or the player lands, repeated lava contact drains health on deterministic damage ticks, any zero-health result from lava, drowning, hostile contact, or hard landings enters a short camera-held death state before respawning from the latest resolved spawn with restored hostile-contact invulnerability, debug edits still refresh spawn placement, edits that trap the player inside solid tiles still respawn it from the latest resolved valid spawn, and the renderer receives the current wall and ceiling contact state plus a short ceiling-bonk presentation latch so airborne wall collisions can swap the placeholder into a wall-slide pose while upward collisions can swap it into a ceiling-bonk pose that remains readable after the immediate contact frame.

## Entity Foundation

- `src/world/entityRegistry.ts` now provides a simulation-owned entity registry that assigns stable numeric IDs, runs per-entity fixed-step hooks in spawn order, keeps `previous` and `current` render-state snapshots ready for interpolation, and lets external state replacement resynchronize those snapshots immediately so spawn, teleport, respawn, and pickup-despawn discontinuities can avoid interpolation smear; the standalone player is the first live gameplay actor on that layer, and the renderer now consumes registry-backed entity-pass entries built from those snapshots.

## Hot-Path Lookup Strategy

- Gameplay metadata compiles into dense lookup arrays for collision, light-blocking, emissive-light, and liquid queries.
- Terrain connectivity compiles into dense connectivity-group and material-tag lookup tables.
- Tile render metadata compiles into dense static UV and terrain variant atlas-index lookup tables.
- Liquid connectivity compiles into dense per-tile group ID and label lookups, and liquid variant metadata compiles into per-tile cardinal-mask render lookups consumed directly by the liquid meshing path.
- Optional animated render metadata compiles into dense per-tile frame start, frame count, and frame duration tables plus a flattened UV-frame list for renderer-time elapsed-frame sampling.
- Optional animated liquid variant metadata also compiles into dense per-tile-per-cardinal-mask frame start, frame count, and frame duration tables plus a flattened UV-frame list for renderer-time liquid elapsed-frame sampling.
- Placeholder terrain autotile resolution uses precomputed lookup tables for normalized adjacency masks and raw adjacency masks.
- Authored-atlas region UV rect objects are precomputed and reused by atlas-index and terrain-autotile resolution paths.

## Picking, Debugging, And Inspection

- Shared DPR-aware screen, canvas, world, and tile picking utilities centralize pointer coordinate conversion.
- Camera and picking tests cover viewport round-tripping, DPR-aware selection, and negative-world tile flooring.
- Debug overlay shows FPS, rendered chunk count, mesh telemetry, resident dirty-light chunk counts, loaded atlas source kind plus atlas dimensions, atlas validation status, the resolved standalone player spawn tile plus world coordinates, supporting solid tile coordinates, support chunk coordinates, support chunk-local tile coordinates, support tile ID, and liquid-safety status, live standalone player state telemetry (`position`, body world tile coordinates, body chunk coordinates, body chunk-local tile coordinates, `velocity`, `grounded`, `facing`), the current standalone player placeholder pose label chosen from the renderer-facing pose resolver and now aligned to the snapshot-owned wall, ceiling, and bonk presentation fields, the renderer-side ceiling-bonk presentation latch state while that brief post-contact hold remains active, live standalone-player nearby-light sample telemetry (`level`, `factor`, source world tile coordinates, source chunk coordinates, and source chunk-local tile coordinates consumed directly from renderer telemetry fields), live hostile-slime spawn and locomotion telemetry (`active`, `nextSpawn`, `nextWindow`, `targetOffset`, and when a nearest active slime exists `tile`, player-relative `chaseOffset`, `vel`, `grounded`, `facing`, `hopCooldown`, and airborne `launch` kind), the latest standalone player grounded-transition event (`jump`, `fall`, or `landing` with resolved position and velocity), the latest standalone player facing-transition event (`left->right` or `right->left` with resolved position and velocity), the latest standalone player auto-respawn event when embedded recovery or a zero-health death respawn reuses the resolved spawn (`embedded` or `death` with spawn tile, resolved support chunk and chunk-local coordinates, resolved support tile ID, resolved spawn liquid-safety status, position, and velocity), the latest standalone player wall-contact and ceiling-contact transition events (`blocked` or `cleared` with tile, position, and velocity, plus resolved `left` or `right` side for wall-contact events), live standalone player collision AABB telemetry (`min`, `max`, `size`), live standalone player camera-follow telemetry (`cam`, `camTile`, `camChunk`, `camLocal`, `zoom`, `focus`, `focusTile`, `focusChunk`, `focusLocal`, `offset`), live standalone player collision-contact telemetry (`support`, `wall`, `ceiling` with tile coordinates and IDs when present, and resolved `left` or `right` side for wall contacts), live standalone player input-intent telemetry (`move`, `jumpHeld`, `jumpPressed`), live pointer inspect data (`client`, `canvas`, `world`, hovered tile name plus tile ID, `tile`, `chunk`, `chunk-local`, `solid`, `light`, `liquid`, `liquidLevel`, `liquidSurfaceIn`, `liquidSurfaceBranch`, `liquidTopLeft`, `liquidTopRight`, `liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`, `liquidFrameHeightV`, `liquidFramePxH`, `liquidBottomLeftV`, `liquidBottomRightV`, `liquidBottomLeftPxY`, `liquidBottomRightPxY`, `liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPct`, `liquidVisibleRightPct`, `liquidVisibleLeftPxH`, `liquidVisibleRightPxH`, `liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPct`, `liquidRemainderRightPct`, `liquidRemainderLeftPxH`, `liquidRemainderRightPxH`, `liquidCoverageLeftPct`, `liquidCoverageRightPct`, `liquidCoverageLeftPxH`, `liquidCoverageRightPxH`), plus resolved liquid connectivity-group labels, liquid cardinal masks, liquid animation frame indices, frame counts, frame durations, elapsed-in-frame times, normalized frame-progress percentages, remaining-in-frame times, loop durations, elapsed-in-loop times, normalized loop-progress percentages, remaining-in-loop times, and current liquid variant source labels, UV rects, and atlas pixel bounds for liquid tiles, and a separate pinned tile metadata line when inspect pin is active; the paused-menu telemetry catalog can now hide atlas, animated-mesh, spawn, pointer, and pinned overlay lines independently instead of leaving them permanently on.
- Debug overlay also reports resident animated chunk mesh counts plus recorded animated-quad totals with separate `nonLiquid` and `liquid` quad breakdowns, alongside per-frame animated chunk UV upload cost as upload count, changed animated-quad totals, matching `nonLiquid` and `liquid` upload breakdowns, total uploaded bytes, the current awake active-liquid chunk count, the current sleeping-liquid chunk count, the current awake active-liquid chunk bounds, the current sleeping-liquid chunk bounds, the last-step sideways candidate-band chunk bounds, the derived last-step liquid phase summary (`none`, `downward`, `sideways`, `both`), and the last fixed-step liquid simulation counters for downward-scanned active chunks, sideways-scanned active-band candidate chunks, sideways-tested active-band pairs, and split downward-versus-sideways transfer counts, so both steady-state authored animation footprint and current liquid-step workload stay visible while wake testing can compare awake bounds, sleeping settled-pool bounds, and the broader sideways candidate band.
- Hovered tile cursor overlay renders in client space from world tile coordinates and keeps pinned and hovered inspect outlines visible together when they differ.
- A dedicated spawn marker overlay outlines the resolved spawn AABB, support tile, and feet anchor in client space and refreshes after tile edits so spawn placement can be validated during active world sessions.
- The shell-level `Spawn Marker` toggle hides that spawn overlay without interrupting world controls, spawn refresh, or simulation.
- On-canvas one-shot preview badges show live anchor plus endpoint tile coordinates, inclusive span dimensions, and affected-tile estimates for active previews and stay clipped to the visible canvas bounds on small or offset canvases, while anchored touch previews keep the endpoint, span, and affected-count text pending until the second point is chosen and show a persistent anchor label with the armed tool name, `Brush` or `Break` action text, and anchor tile coordinates that stays clamped inside the visible canvas bounds near viewport edges.
- Compact debug-edit status strip mirrors shared mode, active brush, armed tool state, mixed-device hints, lets summary chips wrap long brush or tool values inside the chip on narrow mobile viewports, shows active one-shot preview anchor plus endpoint tile coordinates with live inclusive span dimensions and estimated affected tile counts while preview state is live, surfaces the current standalone player placeholder pose label plus the renderer-side `BonkHold` latch state and keeps both aligned to the snapshot-owned wall, ceiling, and bonk presentation fields, live world position, live world tile coordinates, live body chunk coordinates, live body chunk-local tile coordinates, the latest resolved spawn tile plus world coordinates, supporting solid tile coordinates, support chunk coordinates, support chunk-local tile coordinates, support tile ID, and liquid-safety status, live collision AABB `min`, `max`, and `size`, live camera world position, live camera world tile coordinates, live camera world chunk coordinates and chunk-local tile coordinates, live camera-follow focus point world position, world tile coordinates, world chunk coordinates, and chunk-local tile coordinates, live camera-follow offset, live camera zoom level, resident dirty-light chunk counts, awake and sleeping liquid chunk counts plus awake-liquid bounds and sleeping-liquid bounds, the last-step sideways candidate-band chunk bounds, the derived last-step liquid phase summary (`none`, `downward`, `sideways`, `both`), the last-step downward active-chunk scan count, the last-step sideways candidate-chunk scan count, the last-step sideways-tested liquid-pair count, the derived sideways pair-density metric from those candidate-chunk and tested-pair counts, split downward-versus-sideways liquid transfer counts, standalone-player nearby-light sample telemetry (`level`, `factor`, source world tile coordinates, source chunk coordinates, and source chunk-local tile coordinates consumed directly from renderer telemetry fields), live `grounded` flag, live facing direction, live horizontal move input axis, live horizontal and vertical velocity, live speed magnitude, live `jumpHeld` input state, live `jumpPressed` edge state, live support-contact tile, live wall-contact tile and resolved `left` or `right` side, the live ceiling-contact tile, live current/max health, live breath, live drowning cooldown, live fall-recovery cooldown, and live hostile-contact invulnerability time when the text debug HUD is hidden, now also includes hostile-slime spawn-window index plus tile-offset target, the tracked slime's player-relative horizontal and vertical chase offset, and the rest of the hidden-HUD hostile spawn cooldown and locomotion lines, also surfaces the latest grounded-transition, facing-transition, auto-respawn, hard-landing-damage plus impact-speed, drowning-tick, hostile-contact, wall-contact, and ceiling-contact events in that hidden-HUD state, including respawn-event support chunk and chunk-local coordinates plus the resolved support tile ID and spawn liquid-safety status beside the existing spawn tile, position, and velocity fields, includes the resolved `left` or `right` side on hidden-HUD wall-contact transition telemetry, wraps preview, inspect, empty-hover guidance, shortcut hint details, and multiline player or event telemetry into segmented rows to stay inside narrow mobile viewports, stacks inspect action buttons vertically before they clip on narrow canvases, shows separate pinned plus hovered tile metadata lines plus a hovered-to-pinned offset when both inspect targets differ, labels deduplicated same-tile inspect state as shared, and includes world plus chunk and chunk-local tile coordinates in inspect metadata plus the simulated liquid fill level, same-kind liquid-surface input levels, the resolved `liquidSurfaceBranch` classification, resolved normalized `liquidTopLeft` and `liquidTopRight` surface heights, current frame bounds telemetry (`liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`, `liquidFrameHeightV`, `liquidFramePxH`), resolved `liquidBottomLeftV` and `liquidBottomRightV` crop coordinates, matching `liquidBottomLeftPxY` and `liquidBottomRightPxY` atlas pixel rows, resolved visible-frame-height deltas plus percentages (`liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPct`, `liquidVisibleRightPct`, `liquidVisibleLeftPxH`, `liquidVisibleRightPxH`), cropped-off remainder deltas plus percentages (`liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPct`, `liquidRemainderRightPct`, `liquidRemainderLeftPxH`, `liquidRemainderRightPxH`), paired visible-plus-cropped percentage and pixel accounting (`liquidCoverageLeftPct`, `liquidCoverageRightPct`, `liquidCoverageLeftPxH`, `liquidCoverageRightPxH`), resolved liquid connectivity-group label, liquid cardinal mask, liquid animation frame index, frame count, frame duration, elapsed-in-frame time, normalized frame-progress percentage, remaining-in-frame time, loop duration, elapsed-in-loop time, normalized loop-progress percentage, remaining-in-loop time, and current liquid variant source, UV rect, and atlas pixel bounds for liquid tiles; spawn plus inspect detail lines now honor the same paused-menu telemetry catalog while the inspect chip and pin actions stay available when those detail rows are hidden.
- The shell-level `Edit Overlays` toggle and matching desktop `V` shortcut hide those compact inspect and one-shot preview overlays without disabling hotbar placement feedback, inspect pinning, one-shot tool state, painting, movement, or simulation.
- The shell-level `Show Edit Panel (G)` or `Hide Edit Panel (G)` toggle hides or restores the full `Debug Edit` control surface independently from the compact overlays, and its shell-button tooltip now repeats the matching desktop `G` shortcut alongside the existing show or hide copy; first-run fallback state starts the panel hidden until it is toggled on.
- Compact status-strip inspect action buttons retain the last mouse hover comparison state while the pointer is over the action row, so pinned plus hovered metadata does not flap when using `Pin Click` or `Repin Click`.
- Hovered and touched inspect readouts include tile name, tile ID, world plus chunk and chunk-local coordinates, solid, light-blocking, and liquid flags, plus the simulated liquid fill level, same-kind liquid-surface input levels, the resolved `liquidSurfaceBranch` classification, resolved normalized `liquidTopLeft` and `liquidTopRight` surface heights, current frame bounds telemetry (`liquidFrameTopV`, `liquidFrameTopPxY`, `liquidFrameBottomV`, `liquidFrameBottomPxY`, `liquidFrameHeightV`, `liquidFramePxH`), resolved `liquidBottomLeftV` and `liquidBottomRightV` crop coordinates, matching `liquidBottomLeftPxY` and `liquidBottomRightPxY` atlas pixel rows, resolved visible-frame-height deltas plus percentages (`liquidVisibleLeftV`, `liquidVisibleRightV`, `liquidVisibleLeftPct`, `liquidVisibleRightPct`, `liquidVisibleLeftPxH`, `liquidVisibleRightPxH`), cropped-off remainder deltas plus percentages (`liquidRemainderLeftV`, `liquidRemainderRightV`, `liquidRemainderLeftPct`, `liquidRemainderRightPct`, `liquidRemainderLeftPxH`, `liquidRemainderRightPxH`), paired visible-plus-cropped percentage and pixel accounting (`liquidCoverageLeftPct`, `liquidCoverageRightPct`, `liquidCoverageLeftPxH`, `liquidCoverageRightPxH`), resolved liquid connectivity-group label, liquid cardinal mask, liquid animation frame index, frame count, frame duration, elapsed-in-frame time, normalized frame-progress percentage, remaining-in-frame time, loop duration, elapsed-in-loop time, normalized loop-progress percentage, remaining-in-loop time, and current liquid variant source, UV rect, and atlas pixel bounds for liquid tiles.
- Desktop and touch both support pinned tile inspection through the compact status strip workflow.

## Shared Debug Edit Model

- Desktop and touch share the same debug edit concepts: `Pan`, `Place`, and `Break` modes, active brush selection, undo and redo history, armed one-shot tools, and compact status reporting.
- Shared debug edit control state persists across reloads through local storage with a metadata-safe brush fallback.
- `Reset Prefs` restores default touch mode, brush, and panel visibility while clearing persisted debug-edit control state.
- Undo and redo operate on recorded per-stroke tile deltas.
- Flood fill, line, rectangle fill, rectangle outline, ellipse fill, and ellipse outline tools apply as single undoable strokes.
- Armed one-shot tools expose on-canvas status or preview overlays and support `Esc` cancellation.

## Desktop Controls

- App shell: click `Enter World` from the main menu after boot finishes, then use the section-owned paused controls after returning from an active session or after boot restores a saved paused session from browser storage: `Resume World (Enter)` in `Overview`, `Export World Save`, `Import World Save`, and `Clear Saved World` on the `World Save` page, `Import Shell Profile`, `Apply Shell Profile`, `Clear Shell Profile Preview`, `Export Shell Profile`, and `Reset Shell Hotkeys` on the `Shell` page, and `Reset Shell Toggles` plus `New World (N)` in `Danger Zone`. `Export World Save` downloads the current paused session as JSON without changing world or player state; inspect the paused-menu `Export Result` card after export attempts to confirm either the last downloaded filename or the surfaced failure reason when the JSON download throws. `Import World Save` chooses a JSON world-save file and replaces the paused session only when the top-level envelope validates and runtime restore succeeds; while its browser picker is open the section-owned card stays in a waiting state with a compact `Busy` heading badge and ignores repeated taps, and you can inspect the paused-menu `Import Result` card after import attempts to see whether the picker was canceled, the picker failed before any selection result and surfaced its thrown reason, the last accepted filename, the last rejected filename plus validation reason, the last restore-failed filename plus thrown reason, or the last selected filename plus browser-resume persistence warning when the imported session is live only in the current tab. Inspect the warning `Saved World Status` card until a later pause, page hide, replacement import, or fresh world rewrite succeeds in browser storage, click `Clear Saved World` to delete the browser-saved paused session while keeping the current paused world open in the tab until it is saved again, inspect the warning `Saved World Status` card while that paused session no longer has browser resume data until `Resume World`, `Import World Save`, or `New World` rewrites it after a successful clear, or inspect the warning `Clear Saved World Result` card when deleting browser resume data fails and the paused session stays browser-saved. Click `Reset Shell Toggles` to clear saved in-world shell visibility and restore the default-off shell layout before the next resume, then inspect the `Reset Shell Toggles Result` card to confirm the next resume starts with all five shell toggles hidden or, when browser shell storage could not be cleared, to see that the reset currently applies only to the live paused session in this tab until a later shell save succeeds. Open the paused-menu `Shell` page to edit the `Shell Hotkeys` fields for `Main Menu`, `Recenter Camera`, `Debug HUD`, `Edit Panel`, `Edit Overlays`, and `Spawn Marker` through unique A-Z letters with inline duplicate/reserved-key feedback, inspect the compact `Keys` and `Persistence` rows there before editing, including the inline `Saved` or `Session only` persistence badge, then inspect the inline remap result copy and matching result badge to see whether that update was browser-saved or only kept for the current paused session because browser storage was not updated, click `Import Shell Profile` there to validate a `deep-factory.shell-profile` JSON file and load its `Shell Profile Preview` summary before any paused-session mutation while the import button stays locally disabled until the picker settles, inspect that preview card to review the selected profile's saved-on toggles, metadata-first `Toggle Changes` and `Hotkey Changes` rows, and grouped `Changed From Live` versus `Matching Live` hotkey diffs, click `Apply Shell Profile` there to commit the previewed toggles plus hotkeys without rebuilding the current paused session while the apply button stays locally disabled until the in-flight apply settles, and inspect the inline apply feedback plus matching result badge to see whether that apply was a no-op match, changed only shell visibility toggles, changed only shell hotkeys, changed both, or only updated the current tab because browser shell storage failed, click `Clear Shell Profile Preview` there to dismiss that staged profile without applying it, inspect the inline shell-profile import, clear, or apply feedback when the picker is canceled, the picker fails, validation rejects a file, the preview is dismissed, or the applied profile can only be kept in the current tab, click `Export Shell Profile` there to download the current shell visibility toggles plus active shell hotkeys as a `deep-factory.shell-profile` JSON file without changing the paused session, click `Reset Shell Hotkeys` there to restore the default `Q/C/H/G/V/M` set through the same persistence path and clear any stale load-fallback warning while the inline result now distinguishes an ordinary default rewrite, saving the recovered safe-set fallback back to storage, and a no-op press when the default set is already active, or use `New World (N)` to discard the current session and boot a fresh world immediately; the dedicated `World Save` and `Shell` pages return through `Back to Overview`, while visible `Jump to Overview` links in `Recent Activity` and `Danger Zone` return focus to the top paused section. The paused menu now groups those actions into dedicated guidance cards plus a `Persistence Summary` card whose `Status`, `Resumes`, `Saved On`, `Saved Off`, and `Cleared by` rows show whether shell visibility is browser-saved or session-only fallback, which shell toggles return on resume, which ones are currently persisted on or off, and which reset paths clear them, while the optional `Shell Profile Preview` card now keeps its selected-file, change-summary, and saved-on/off state in metadata rows plus grouped changed-versus-matching hotkey diffs beside the live remap editor, so session-continuation, export, import, clear-saved-world, shell-reset, hotkey-remap, shell-profile preview, and current shell bindings stay visible without repeating one long status line or depending on button tooltips; while those same `Resume World`, `Export World Save`, `Import World Save`, `Clear Saved World`, `Reset Shell Toggles`, and `New World` tooltips still provide the current action guidance, press `Enter` to trigger the same resume path from the keyboard or `N` to trigger the fresh-world reset path.
- Open the paused-menu `Shell` page to reach `Gameplay Controls`, then click `Enable Peaceful Mode` or `Disable Peaceful Mode`, inspect its `Mode`, `Hostiles`, `Persistence`, and `Scope` rows to confirm whether active slimes are cleared and future hostile spawns stay blocked, and note whether that hostile-free mode is browser-saved for later sessions or only active in the current tab because browser storage was not updated.
- Open the paused-menu `Shell` page to reach `Telemetry Controls`, then toggle whole telemetry collections or individual player, hostile-slime, and world readouts, click `Reset Telemetry` there to restore every collection and type to the default visible state once the button enables after a collection or type change, and inspect its persistence summary to confirm whether those telemetry preferences are browser-saved or only active for the current paused session in this tab.
- In-world shell: by default, click `Main Menu (Q)` or press `Q` to pause back to the main menu without discarding the current world session, click `Recenter Camera (C)` or press `C` to snap the camera back to the standalone player and clear any preserved manual follow offset, click `Show Debug HUD (H)` or `Hide Debug HUD (H)` or press `H` to toggle the text telemetry overlay, click `Show Edit Panel (G)` or `Hide Edit Panel (G)` or press `G` to toggle the full `Debug Edit` control surface, click `Show Edit Overlays (V)` or `Hide Edit Overlays (V)` or press `V` to toggle the compact debug-edit overlays, click `Show Spawn Marker (M)` or `Hide Spawn Marker (M)` or press `M` to toggle the resolved spawn overlay, and click `Shortcuts (?)` or press `?` to toggle the in-world desktop and touch controls reference; the paused-menu `Shell Hotkeys` editor can replace the first six default in-world letters with validated persisted remaps while invalid or conflicting saved values fall back to `Q/C/H/G/V/M`.
- Equipment: with the full `Debug Edit` panel visible, the in-world `Equipment` panel exposes `Head`, `Body`, and `Legs` starter-armor cards that can equip or unequip `Starter Helmet`, `Starter Breastplate`, and `Starter Greaves`, keeps a live `Total defense` readout visible, and reduces hostile-slime contact damage by flat equipped defense down to a minimum of `1`.
- Hotbar: click a bottom hotbar slot or press `1-0` while the full `Debug Edit` panel is hidden to select any carried stack such as `Starter Axe`, `Starter Pickaxe`, `Starter Sword`, `Umbrella`, `Bug Net`, `Starter Spear`, `Bunny`, or another item added through the catalog, pickups, or crafting, click `Left` / `Right` or press `Shift+[` / `Shift+]` to move the selected slot one step across the hotbar while keeping selection attached to that moved slot, click `Drop 1` or press `Shift+Backspace` to toss one selected item into the world, click `Drop` or press `Backspace` to toss the full selected stack, and both drop actions now cascade through nearby overlapping pickups of the same item before spawning any remainder; walk back into that pickup to auto-merge it into matching or empty hotbar slots, and when `Starter Sword` is selected the same hidden-panel click or tap path starts a fixed-step swing aimed toward the requested target, shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected slot, applies deterministic per-slime hit cooldowns, spends deterministic sword damage into per-slime health, despawns defeated hostile slimes immediately after lethal hits, leaves one `Gel` pickup behind through the shared nearest-first dropped-item cascade, and carries knockback into the next hostile-slime step for survivors without consuming the weapon.

  When `Starter Spear` is selected, the same hidden-panel click or tap path starts a fixed-step directional thrust aimed from the requested world point, shows the full fixed-reach thrust line plus endpoint marker through the hidden-panel preview layer, now shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected hotbar slot as the thrust advances through windup, active, and recovery, clamps that thrust to its fixed reach, spends deterministic spear damage into any slime whose AABB intersects that aimed thrust line, and carries directional knockback into the next hostile-slime step without consuming the weapon.

  When `Starter Axe` is selected, sampled sapling, trunk, or leaf tiles within the shared reach now mark as planted-sapling or grown-tree chop targets, stay blocked on non-tree or out-of-range tiles, and keep the resolved growth-stage preview visible through successful active swings even after the chopped tiles clear; left-clicking or tapping a nearby small tree still starts a fixed-step swing that shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected slot, clears the full planted or grown footprint through the shared anchor helper without consuming the tool, and refunds merge-aware `Wood` pickups with `1` wood from planted saplings and `5` wood plus `1` `Acorn` from grown trees.

  When `Starter Pickaxe` is selected the hovered terrain or placed rope, torch, workbench, furnace, or anvil tile now marks as mineable or blocked even with debug edit overlays hidden while left-clicking or tapping a nearby solid terrain or placed utility tile starts a fixed-step swing that shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected slot, breaks dirt, grass, rope, torch, and placed stations in one hit and stone plus seeded copper-ore terrain in two hits without consuming the tool, with broken stone and copper ore refunding merge-aware `stone-block` and `copper-ore` pickups while cut rope, torch, workbench, furnace, and anvil placements plus workbench, furnace, or anvil support-loss clears refund through the same merge-aware removed-tile pickup path as other utility removals.

  When `Bug Net` is selected, the same hidden-panel click or tap path starts a fixed-step swing aimed toward the requested target, now shows matching `WIND`, `ACT`, and `REC` timing feedback on that selected slot, captures at most one nearby passive bunny during the active window, and adds one `Bunny` item into a matching or empty hotbar slot when inventory capacity is available without consuming the net.

  When `Bunny` is selected, that same hidden-panel click or tap path now highlights the hovered tile as releasable or blocked before use, adds a separate landing marker when nearby-ground fallback resolves away from that hovered tile, and then releases one passive bunny onto the nearest valid ground near the requested tile while consuming one `Bunny` item only when the release succeeds.

  When `Umbrella` is selected, holding jump during an airborne descent clamps fall speed into a slow glide until that hold releases or the player lands, without consuming the item or rerouting primary click away from the shared hidden-panel use path used by other tools.

  When `Healing Potion` is selected, the same hidden-panel click or tap path consumes one potion only while the player is alive and injured, restores up to `30` health without overheal, respects a fixed-step `2s` reuse cooldown, and now shows that blocked reuse through a shrinking vertical fill on healing-potion hotbar tiles; when `Heart Crystal` is selected, the same hidden-panel click or tap path consumes one crystal only while the player is alive and below the `400` max-health cap, raises max health by up to `20`, fills current health by that granted amount, and now shows visible `DEAD` or `MAX` slot feedback whenever use is blocked because the player is dead or already at the cap; when `Dirt Block`, `Stone Block`, or `Wood Block` is selected the hovered tile marks as placeable or blocked while left-clicking an empty tile with a solid neighbor places one block only when the target stays within the default five-tile body-to-cell reach and would not overlap the player, with `Dirt Block` placing starter dirt tile `9`, `Stone Block` placing stone tile `1`, and `Wood Block` placing wood tile `19`; when `Dirt Wall` or `Wood Wall` is selected, the same hidden-panel click or tap path previews and places background walls only into empty foreground cells whose four cardinal directions each find nearby solid terrain within that same reach-gated local scan; when `Torch` is selected, the same hidden-panel click or tap path previews and places a light-emitting torch tile into an empty solid-face-supported cell within that same reach even if the player overlaps that tile; when `Rope` is selected, the same hidden-panel click or tap path previews and places a climbable rope tile into an empty cell whose direct tile-above anchor is solid or already rope or whose left or right neighbor is solid, and using rope on an occupied rope tile extends the bottom of that rope column while the hovered rope segment still supplies the visible preview and the range check; when the full panel is open, those same `1-0` keys keep their existing brush-slot behavior instead.
- Move player: `A` and `D` or left and right arrows.
- Jump or climb up rope: `W`, up arrow, or `Space`; while on a rope, press jump together with left or right to jump off, and while `Umbrella` is selected keep jump held during descent to glide.
- Descend rope: `S` or down arrow while overlapping rope; double tap and hold `S` or down arrow on a rope to quick-drop to the bottom.
- Mouse wheel: zoom in or out.
- Middle mouse drag: pan the camera.
- `Shift` + mouse drag: pan without painting.
- Mouse drag: with the full `Debug Edit` panel visible, debug paint on hovered tiles (`left` place active brush, `right` break).
- Camera shortcut: `C` recenters on the standalone player without affecting movement input.
- Edit-panel shortcut: `G` toggles the in-world full `Debug Edit` panel without affecting movement input.
- Compact edit-overlay shortcut: `V` toggles the in-world compact debug-edit overlays without affecting movement input.
- Debug edit brush shortcuts: `1`-`0` select visible brush slots, `[` and `]` cycle active brush.
- Debug touch-mode shortcuts: `P` pan, `L` place, `B` break.
- Debug panel shortcut: `\` collapses or expands the shared debug-edit controls panel.
- Debug eyedropper shortcut: `I` sets the active brush from the hovered non-empty tile.
- Debug history shortcuts: `Ctrl/Cmd+Z` undo, `Ctrl/Cmd+Shift+Z` or `Ctrl/Cmd+Y` redo.
- Debug flood fill shortcuts: `F` arms brush flood fill, `Shift+F` arms break flood fill.
- Debug one-shot shape shortcuts: `N` and `Shift+N` line, `R` and `Shift+R` rectangle fill, `T` and `Shift+T` rectangle outline, `E` and `Shift+E` ellipse fill, `O` and `Shift+O` ellipse outline.
- Desktop inspect pinning uses compact status strip actions: `Pin Click`, `Repin Click`, `Clear Pin`, and `Esc` to cancel an armed pin.
- Desktop one-shot tools use mouse drag bounds or endpoints for placement and show matching preview overlays.

## Touch Controls

- Shared on-screen debug controls expose `Pan`, `Place`, and `Break` modes plus a shared brush palette.
- A separate on-screen touch player pad exposes hold-to-move left, hold-to-climb-down, hold-to-move right, and hold-to-jump buttons without disabling the debug edit panel, combining `Jump` with `Left` or `Right` now releases rope hold into a normal jump arc, holding `Jump` while `Umbrella` is selected now also slows airborne descent into the shared glide clamp, and double tapping then holding `Down` on a rope triggers the same quick-drop-to-bottom behavior as desktop `S` or `Down Arrow`.
- A fixed bottom hotbar overlay exposes the current carried tool and item stacks, lets touch players tap a slot to select it, exposes shared `Left`, `Right`, `Drop 1`, and `Drop` buttons so the selected slot can move one step across the hotbar and one selected item or the full selected stack can be tossed into the world while cascading through nearby overlapping pickups of the same item before spawning any remainder, and with `Starter Sword`, `Starter Spear`, `Starter Axe`, `Starter Pickaxe`, `Bug Net`, `Bunny`, `Dirt Block`, `Stone Block`, `Wood Block`, `Dirt Wall`, `Wood Wall`, `Torch`, or `Rope` selected now marks the hovered tile as mineable, releasable, placeable, blocked, or for the axe specifically a planted-sapling versus grown-tree chop target before a short tap starts a nearby fixed-step swing or places one supported item into the target cell while the full `Debug Edit` panel is hidden, even if the debug edit overlays are off; sword taps now start the same fixed-step windup/active/recovery attack used on desktop while the selected sword slot also shows matching `WIND`, `ACT`, and `REC` timing feedback, and then apply deterministic per-slime hit cooldowns plus knockback without consuming the weapon, spear taps now show the same fixed-reach preview line and endpoint marker used on desktop before or during the same fixed-step directional thrust while the selected spear slot also shows matching `WIND`, `ACT`, and `REC` timing feedback, and then apply deterministic aimed slime hits plus directional knockback without consuming the weapon, axe taps now start the same fixed-step windup/active/recovery chop used on desktop while the selected axe slot also shows matching `WIND`, `ACT`, and `REC` timing feedback, resolve sampled sapling, trunk, or leaf hits through the shared small-tree anchor, keep that resolved growth-stage preview visible through successful active swings after the chop lands, and clear planted saplings for `1` `Wood` or grown trees for `5` `Wood` plus `1` `Acorn` through merge-aware world pickups without consuming the tool, pickaxe taps now start the same fixed-step windup/active/recovery mining swing used on desktop while the selected pickaxe slot also shows matching `WIND`, `ACT`, and `REC` timing feedback, break dirt, grass, placed rope, and placed torch in one hit and stone plus seeded copper-ore terrain in two hits, refunded dirt, stone, copper ore, rope, and torch now appear through merge-aware world pickups, bug-net taps now start the same fixed-step windup/active/recovery swing used on desktop while the selected bug-net slot also shows matching `WIND`, `ACT`, and `REC` timing feedback, and then capture at most one nearby passive bunny into a matching or empty `Bunny` hotbar stack when inventory capacity is available, selected `Bunny` taps now use that same hidden-panel item-use path to preview releasable versus blocked hovered tiles, add a landing marker when nearby-ground fallback resolves to a different tile, and then release one passive bunny onto nearby valid ground around the tapped tile while consuming one stack only on success, dirt, stone, and wood taps place the same support-gated solid-block path used by their respective terrain tiles, dirt-wall and wood-wall taps place the same enclosed-room wall path used on desktop without blocking the player, rope taps can still side-attach to solid tiles or extend the bottom of an existing rope column while keeping the tapped rope segment as the previewed in-range interaction tile, selected-umbrella touch play now reuses that same hold-to-jump pad to clamp fall speed during airborne descent without consuming the item, healing-potion taps now consume one potion through that same hidden-panel item-use path to restore up to `30` health without overheal when the alive player is below full health before starting a fixed-step `2s` reuse cooldown whose blocked time now animates directly inside healing-potion hotbar tiles, and heart-crystal taps now consume one crystal through that same path to raise max health by up to `20` toward the `400` cap while filling current health by the granted amount and now surface visible `DEAD` or `MAX` slot feedback when that use is blocked because the player is dead or already at the cap.
- The in-world `Equipment` panel remains tappable on touch while the full `Debug Edit` panel is visible, so touch sessions can equip or unequip `Starter Helmet`, `Starter Breastplate`, and `Starter Greaves` in the same `Head`, `Body`, and `Legs` slots while checking the shared `Total defense` readout before hostile-slime contact.
- One-finger drag pans in `Pan` mode.
- One-finger drag paints in `Place` or `Break` mode with line-stepped per-tile dedupe while the full `Debug Edit` panel is visible.
- Two-finger pinch zoom works while touch debug edit modes are active.
- Two-finger tap triggers undo in `Pan` mode.
- Three-finger tap triggers redo in `Pan` mode.
- Long-press eyedropper in `Pan` mode sets the active brush from the touched non-empty tile.
- Tap-to-pin inspect in `Pan` mode locks tile metadata and outline state until cleared or repinned.
- Touch flood fill arms through on-screen controls, then applies on the next tap.
- Touch line, rectangle, and ellipse tools use two-point or two-corner workflows and show persistent start-anchor or corner-anchor indicators while armed.

## Debug Edit UI

- On-screen debug edit controls support brush selection, mode switching, undo, redo, collapse and expand, reset prefs, and one-shot tool arming.
- The in-world full debug surface now also shares space with small `Item Catalog`, `Crafting`, and `Equipment` panels that keep debug item spawning, searchable recipe quick-craft cards with live blocker state, inline crafting-card ready-versus-blocked status rows, nearby-station recipe actions, plus starter-armor slot toggles and total defense visible while those controls stay open.
- The full `Debug Edit` panel is now distinct from the compact inspect and preview overlay layer, so either surface can be hidden without forcing the other one off.
- The touch `Debug Edit` panel includes a `Keyboard` reference for desktop-parity shortcuts, including paused-menu `Enter` resume and `N` new-world guidance, the current in-world shell-action bindings for main menu, recenter, HUD, edit panel, edit overlays, spawn marker, and `?` shortcuts, plus the hotbar-selection, hotbar-reordering, stack-drop, and hidden-panel play-mode item-use/build split that applies while the full `Debug Edit` panel is hidden alongside brush, history, eyedropper, and shape-tool shortcuts.
- Collapsed panel state still shows a compact header with current mode, active brush, and undo or redo counts.
- The compact status strip stays useful even when the larger panel is collapsed or out of the way.

## Tooling And Deployment

- Production Vite builds target the GitHub Pages project-site base path `/deep-factory/`, so emitted script, stylesheet, and static-asset URLs resolve correctly under `https://regillmore.github.io/deep-factory/` while local development stays rooted at `/`.
- Deployment regression coverage temp-builds the production app, verifies the `/deep-factory/` bundle URLs, scans emitted `index.html` to reject both root-relative `"/assets/..."` and double-prefixed `"/deep-factory/deep-factory/assets/..."` script and stylesheet URLs plus the exact root-relative `"/atlas/"` and `'/atlas/'` static-asset prefix literals, both exact double-prefixed `"/deep-factory/deep-factory/atlas/"` and `'/deep-factory/deep-factory/atlas/'` static-asset prefix literals, the exact root-relative single-quoted `'/atlas/tile-atlas.png'`, exact root-relative double-quoted `"/atlas/tile-atlas.png"`, and exact double-prefixed `"/deep-factory/deep-factory/atlas/tile-atlas.png"` authored-atlas runtime URL literals, scans every emitted CSS asset to reject the exact root-relative `"/assets/"` bundle literal, the exact double-prefixed `"/deep-factory/deep-factory/assets/"` bundle literal, those same exact root-relative `"/atlas/"` and `'/atlas/'` static-asset prefix literals, those same exact double-prefixed `"/deep-factory/deep-factory/atlas/"` and `'/deep-factory/deep-factory/atlas/'` static-asset prefix literals, and those same root-relative and double-prefixed authored-atlas runtime URL literals, scans every emitted JavaScript asset to reject the exact root-relative `"/assets/"` bundle literal, the exact double-prefixed `"/deep-factory/deep-factory/assets/"` bundle literal, both exact root-relative `"/atlas/"` and `'/atlas/'` static-asset prefix literals, both exact double-prefixed `"/deep-factory/deep-factory/atlas/"` and `'/deep-factory/deep-factory/atlas/'` static-asset prefix literals, any exact quoted root-relative `/atlas/tile-atlas.png` literal, and the exact double-prefixed `"/deep-factory/deep-factory/atlas/tile-atlas.png"` authored-atlas runtime URL literal while the exact joined runtime atlas fetch URL `/deep-factory/atlas/tile-atlas.png` still appears exactly once across the full emitted JS set, and byte-compares the copied authored atlas output at `dist/atlas/tile-atlas.png` against `public/atlas/tile-atlas.png`.
